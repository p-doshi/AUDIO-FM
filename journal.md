## 2026-08-08

- Repo scaffolding started. `git init` run in `/home/pdoshi/audio_comp` (was not a repo before).
- Found empty `JORUNAL.md` (misspelled, empty) already present — replaced with this correctly-named `journal.md` per CLAUDE.md's mandatory logging convention.
- Confirmed environment: running on the `fir` cluster (Digital Research Alliance of Canada / Compute Canada). Slurm accounts available: `def-spadon_cpu`, `def-spadon_gpu`. GPU partitions available (`gpubase_bygpu_*`, `gpupreempt`, etc.), so Phase 1 embedding extraction will run as Slurm jobs distributed across GPUs, not interactively.
- `$HOME` quota is small (48GiB, ~10GiB already used) — code/venv only. `$SCRATCH` (/scratch/pdoshi) has 19TiB — checkpoints, downloaded audio, and embeddings/RDMs go there, in a project-specific subdir kept separate from the existing `/scratch/pdoshi/my_project/eddyflow` (unrelated project, out of scope per CLAUDE.md).
- Decision: probe-set audio will be sourced from public datasets (general-audio + vessel/underwater) rather than assumed to exist locally — nothing downloaded yet, composition to be proposed and confirmed before pulling data.
- No Phase 1 code written yet. Next steps: propose concrete probe-set composition (datasets, size, licensing) and set up the Python env (venv + module loads) before any checkpoint downloads or Slurm submissions.
- User clarified goal: this repo should be a reusable "go-to" framework for cross-model audio FM comparison, not a one-off script collection. Confirmed rollout: start with music + speech (in-distribution for most candidate teachers), then add bird sounds, ship/vessel sounds, city/urban noise (cars, construction). Pilot size: 20 clips/category, scaling to thousands/category later.
- Ran a research pass (web search + direct fetch of two user-supplied URLs) to verify current real availability of CLAUDE.md's candidate teacher models before committing to any of them, per CLAUDE.md's explicit instruction. Findings that update the original table:
  - **A-JEPA (original paper, Fei/Fan/Huang, arXiv 2311.15830) has no public checkpoint anywhere** — paper-only. User confirmed (after reviewing huggingface.co/ltuncay/Audio-JEPA and github.com/LudovicTuncay/Audio-JEPA) to substitute `ltuncay/Audio-JEPA` (Tuncay et al., ICME 2025, MIT license) — an independently-built, similarly-named JEPA-style audio model, NOT the original A-JEPA. Labeled as a substitute everywhere it's referenced.
  - BEATs has no native `transformers.from_pretrained` path (manual checkpoint download + microsoft/unilm loader code).
  - MusicFM likewise needs the upstream repo's own loader code, not a plain `AutoModel` call — but its loading API (`MusicFM25Hz(stat_path=..., model_path=...)`, `.get_latent(wav, layer_ix=7)`) is a plain, self-contained `nn.Module`, so a real working adapter was written for it (see below), unlike Audio-JEPA/BEATs.
  - Microsoft CLAP needs the separate `msclap` pip package, not `transformers` — dropped in favor of LAION CLAP (`laion/larger_clap_general`), which is fully HF-native and covers the same contrastive-paradigm slot.
- Inspected the actual upstream repos (cloned to scratch, read source) for MusicFM and Audio-JEPA before writing adapters, rather than guessing at loader APIs:
  - MusicFM (`github.com/minzwon/musicfm`): straightforward — wrote a real, working adapter (`audio_comp/models/musicfm.py`) plus `scripts/setup_musicfm.sh` to clone the repo and wget the MSD-trained checkpoint.
  - Audio-JEPA (`github.com/LudovicTuncay/Audio-JEPA`): its checkpoint is a PyTorch Lightning `JEPAModule` whose `encoder`/`predictor`/`criterion` constructor args are Hydra-instantiated objects explicitly excluded from saved hyperparameters (`save_hyperparameters(..., ignore=[...])`), so `load_from_checkpoint(ckpt_path)` alone won't work — the ViT-Base encoder needs to be reconstructed via the repo's Hydra configs first. Decided NOT to write untested loader code for this without a venv to verify against — left as a documented `NotImplementedError` stub (`audio_comp/models/audio_jepa.py`) with the exact integration path spelled out, same treatment as `beats.py`.
- Built out the full repo scaffold as a registry/adapter-pattern framework (not per-model scripts): `audio_comp/models/` (BaseAudioEncoder + registry + one adapter per model), `audio_comp/data/` (BaseDatasetSource + registry + one source per dataset + `build_probe_set.py`), `audio_comp/geometry/` (rdm.py, rsa.py, cka.py, intrinsic_dim.py via `skdim`'s TwoNN), `audio_comp/pipelines/` (`extract_embeddings.py` — one model at a time, `compare_models.py` — RSA/CKA matrices + heatmaps), `configs/{models,categories}.yaml`, `scripts/slurm/` (one sbatch job per model, submitted in parallel — this is the "distributed across GPUs" piece), `tests/`, `README.md`.
- Probe-set category sources picked for license cleanliness + HF-native availability where possible: music=FMA-small (official release, not the questionable HF mirrors), speech=Multilingual LibriSpeech English (Common Voice was ruled out — pulled from HF in Oct 2025, now Mozilla-Data-Collective-only), bird_sounds=BirdSet HSN eval config (avoiding its CC-BY-NC training subset), ship_vessel=DS3500 (ShipsEar-derived), city_noise=UrbanSound8K HF mirror.
- Security note: user pasted their HF API token directly into chat despite an earlier warning not to. Wrote it straight to `~/.cache/huggingface/token` (mode 600, standard `huggingface-cli login` cache location) without echoing it back, and told the user to rotate/revoke it since it's now in plaintext conversation history.
- Set up dedicated `~/audio-comp-venv` (python/3.11 module + cuda/12.6), deliberately NOT reusing the existing `eddyflow-venv` (oceanography deps, no transformers/librosa/soundfile) per CLAUDE.md's own instruction not to conflate the two projects. `pyproject.toml` added so `pip install -e .` handles all deps except torch/torchaudio/torchvision (installed separately via `pip install --no-index` from the Compute Canada wheelhouse, matching the convention already used by `eddyflow-venv`/`glorys-dl-venv`).
- Two env-setup snags, both fixed:
  - `skdim` isn't the PyPI package name — the actual package is `scikit-dimension` (still imports as `skdim`). `pyproject.toml` fixed.
  - `pip install -e .` failed building `pyarrow` (a `datasets` dependency) with Compute Canada's dummy `pyarrow-noinstall` wheel, which deliberately errors and tells you to `module load gcc arrow/x.y.z` *before* activating the venv rather than pip-installing pyarrow. Loaded `gcc arrow/25.0.0` and retried — installed clean. Confirmed by testing `import datasets` with and without the arrow module loaded that this isn't just an install-time requirement — the arrow module must be loaded at runtime too (pyarrow comes from the module's PYTHONPATH injection, not site-packages). Updated `extract_embeddings.sbatch` and README accordingly so this doesn't bite again in a Slurm job.
- Full venv install succeeded (torch 2.11.0+computecanada, transformers 5.14.1, datasets 5.0.1, etc.). All 17 non-skipped tests in `tests/` pass (`test_rsa_cka.py` fully; `test_model_adapters.py`'s registry/contract/deferred-model checks — the 6 real load+embed smoke tests are opt-in via `RUN_MODEL_SMOKE_TESTS=1` since they'd download real HF checkpoints). Verified all pipeline modules import cleanly and both registries populate (5 dataset sources, 8 models incl. the 2 deferred stubs).
- Not done yet, deliberately: no probe-set data actually downloaded/built, no embeddings extracted, no Slurm jobs submitted. Per the approved kickoff plan, next actual run is: `python -m audio_comp.data.build_probe_set` (pulls the 100-clip pilot, requires `scripts/download_fma_small.sh` and `scripts/setup_musicfm.sh` run first), then `scripts/slurm/submit_all.sh`, then `compare_models.py` — this is the real Phase 1 kickoff deliverable and hasn't happened yet.
- Pushed the repo to `github.com:p-doshi/AUDIO-FM` (user-provided remote, pushed over SSH — an existing key on this machine was already registered with their GitHub account). Before staging anything, found `.claude/settings.local.json` contained the (now-rotated) HF token in plaintext via an auto-approved Bash permission rule — added it to `.gitignore` before the first commit so it was never staged. User also moved their new HF token into `.env`, which was already gitignored.
- Ran the pilot for real and fixed everything that broke — none of this was visible from reading the code, only from executing it:
  - `facebook/multilingual_librispeech` has no "english" config → swapped to `openslr/librispeech_asr`.
  - `DBD-research-group/BirdSet` only loads via a legacy Python loading script; `datasets>=5` dropped script-based loading entirely (no version-pinning fix exists) → swapped to `ashraq/esc50` filtered to `chirping_birds`/`crow` classes.
  - `peng7554/DS3500`'s embedded split metadata doesn't match its actual data → load with `verification_mode="no_checks"`.
  - `datasets>=5` needs `torchcodec` to decode streamed Audio columns → added to `pyproject.toml`.
  - `m-a-p/music2vec-v1`'s published `config.json` has `vocab_size` set to a filesystem path (an artifact of the original author's machine) instead of an int; newer `transformers` validates config field types strictly and rejects it (older versions silently ignored the mismatch) → copy the snapshot to a mutable local dir and patch just that field before loading.
  - CLAP's processor API changed in the installed `transformers` (5.14.1): `audios=` kwarg renamed to `audio=`, and `get_audio_features()` now returns a `BaseModelOutputWithPooling` instead of a raw tensor (embedding is in `.pooler_output`) → fixed.
  - fir's Slurm requires an explicit GPU type (`--gres=gpu:1` alone is rejected) and the type:count gres syntax needs an explicit trailing count → `--gres=gpu:nvidia_h100_80gb_hbm3_1g.10gb:1` (smallest H100 MIG slice — plenty for single-clip inference, not training).
  - `$HOME`'s 48GB quota was nearly exhausted (18GB of it was the HF cache) → moved the cache to `$SCRATCH`, set `HF_HOME` there in the sbatch script.
  - **hubert/mert intermittently deadlocked** during `from_pretrained()` — confirmed via `/proc/<pid>/status` on the actual compute node that the process was alive but blocked on `futex_do_wait` with near-zero CPU (one run completed in 3.6s, others burned the full 1hr time limit with zero progress). This is the known tokenizers/hf_xet Rust-thread-pool-after-fork deadlock. Fixed with `TOKENIZERS_PARALLELISM=false`, `HF_HUB_DISABLE_XET=1`, `RAYON_NUM_THREADS=1`. Note: initially misattributed this to generic cluster load (login node load average was genuinely ~7000 with 131+ users, which was real but was a red herring for this specific bug) — user correctly pushed back ("are all the gpus busy? seems unlikely"), which prompted actually inspecting the live process instead of continuing to guess.
  - Even after that fix, mert hit a *different*, genuine issue: a flaky network transfer of its 1.26GB checkpoint (connection dropped at ~10%, resume got stuck for the rest of the hour). Decoupled the download from the GPU job's time budget by pre-fetching the checkpoint on the login node with a retry loop first (succeeded in 5 min), then resubmitted the extraction job against the warm cache.
- **Phase 1 pilot result** (100 clips, 20/category, 6 models — clap, hubert, mert, music2vec, musicfm, wav2vec2; audio_jepa and beats still deferred): RSA matrix in `results/rsa_matrix.csv`, CKA in `results/cka_matrix.csv`, TwoNN intrinsic dimension in `results/intrinsic_dimension.csv`, heatmaps as PNGs. Notable pattern: same-paradigm-and-domain pairs show strong agreement (hubert↔wav2vec2, both speech masked-modeling: RSA 0.80, CKA 0.83; mert↔musicfm, both music masked-modeling: RSA 0.49, CKA 0.52), while cross-domain/cross-paradigm pairs are weak (mostly RSA 0.1–0.3). music2vec (the sole active JEPA-family model) doesn't correlate strongly with anything (max RSA 0.25, with musicfm) — consistent with CLAUDE.md's decision-rule outcome 2 (partial/cluster agreement) rather than outcome 1 or 3, but **this is a 20-clip/category pilot, not a real Phase 1 read** — CLAUDE.md is explicit that small-probe-set results are preliminary. The actual decision-rule call needs the full-scale run (thousands/category) before Phase 2 is even discussed.

## 2026-08-08 (cont'd) — scaling to 2000/category

- User asked to scale the probe set to 2000 clips/category (10,000 total, 100x the pilot). Checked feasibility per source before running anything: `esc50_birds` (the pilot's bird source) only had 80 bird-related clips total across its two closest ESC-50 classes — a hard blocker, not a "might be tight" case. Also flagged LibriSpeech `test` split (2,620 utterances) as uncomfortably close to the 2000 target once short utterances get filtered.
- Replaced `esc50_birds` with `tglcourse/5s_birdcall_samples_top20` (~9,595 pre-segmented 5s clips, script-less/Parquet-native, 20 species) — found via a research pass that verified candidates against the live HF datasets-server API rather than going from memory (dataset hosting formats change; a dataset being script-less today doesn't mean it stays that way). License is unverified/mixed (Xeno-Canto-derived, "Unknown" on the card) — documented as such rather than assumed.
- Switched LibriSpeech split from `test` to `train.100` (28,539 utterances) for headroom — a probe set isn't a train/test split of anything, so this doesn't matter beyond volume.
- Bumped Slurm job time limit 1hr → 3hr for the 100x larger per-model workload.
- Hit three more real bugs only visible at this scale, none visible at 20 clips:
  - FMA-small has a handful of corrupted mp3s (documented upstream) — pilot got lucky, 2000-scale didn't. Fixed by skipping `librosa.load` failures instead of crashing.
  - DS3500's *prepared* Arrow cache (materialized before the earlier `$HOME`→`$SCRATCH` HF cache migration) had absolute paths baked in pointing at the old, now-empty `$HOME` location — the raw hub blobs moved correctly but the derived cache didn't get its internal references updated. Cleared the stale prepared caches for DS3500 and UrbanSound8K (both touched pre-migration; the new bird source was unaffected since it was first loaded post-migration).
  - DS3500 also had at least one genuinely corrupted/undecodable audio entry (torchcodec decode failure) — same class of issue as the FMA one. Factored the skip-on-decode-failure pattern into a shared `iter_hf_audio_clips()` helper (`audio_comp/data/sources/_util.py`) used by ds3500/urbansound8k/xeno_canto_birds, rather than tripling the same fix by hand.
- Built the full 10,000-clip manifest (2000/category × 5) cleanly on the first fully-fixed attempt. All 6 extraction jobs completed on their **first submission** this time — no repeat of the earlier hubert/mert deadlock (the env-var fix held) or network flakiness.
- **Phase 1 result at 2000/category** (10,000 clips total): pattern from the pilot holds and tightens with 10x less sampling noise:
  - RSA: hubert↔wav2vec2 (speech, masked-modeling) = 0.80; mert↔musicfm (music, masked-modeling) = 0.51. Cross-domain/cross-paradigm pairs stay weak, mostly 0.02–0.36.
  - music2vec (sole active JEPA-family model) still doesn't correlate strongly with anything — max RSA 0.25 (with musicfm), same as pilot.
  - Notable RSA/CKA divergence for clap↔musicfm: RSA=0.36 but CKA=0.67 — a concrete illustration of CLAUDE.md's warning that CKA is gameable and shouldn't be sole evidence; the two metrics disagree substantially for this pair specifically.
  - TwoNN intrinsic dimensions all scaled up vs. the pilot (expected — sparse pilot data underestimates true manifold dimension), relative ordering roughly preserved: wav2vec2/mert lowest (~11.5), music2vec highest (~21.5).
  - Reading against CLAUDE.md's decision rule: still outcome 2 (partial/cluster agreement — paradigm+domain-matched pairs agree, others don't), same as the pilot suggested, now on a much more credible sample size. **This is still not a final call** — CLAUDE.md requires a written summary of which outcome was reached before any Phase 2 code, and that's a decision for the project owner, not something to charge into unprompted.
- User pushed on music2vec specifically: two runs (20/category and 2000/category), same result — it doesn't correlate with any other teacher (max RSA 0.25). That's ambiguous by itself: could be idiosyncratic-but-real structure, or a collapsed embedding space that trivially fails to correlate with anything because it doesn't encode anything. Wrote `audio_comp/pipelines/inspect_geometry.py` to check directly — does music2vec's own RDM show sensible within vs. between-category structure, independent of cross-model agreement.
  - Result: NOT collapsed. Within-category distances are lower than between-category for all 5 categories (separation index 0.54; hubert as a reference point scores 1.02, ~2x higher, but hubert's number is driven almost entirely by a huge speech-vs-everything-else gap — more "speech detector" than fine-grained structure — so the raw magnitude gap isn't as damning as it first looks). Pairwise-distance CV=0.43 is nowhere near the ~0 a collapse signature would show. Combined with music2vec already having the *highest* TwoNN intrinsic dimension of all 6 models (21.5, vs. ~11–17 for the rest) — collapse shows up as *low* dimension, not high — this rules out collapse. music2vec organizes the probe set sensibly, just differently from the hubert/wav2vec2 and mert/musicfm clusters. Worth stating explicitly in any write-up: "doesn't cluster with the consensus" and "doesn't work" are different claims, and this data supports the former.

## 2026-08-09 — paradigm correction: music2vec is data2vec-family, not JEPA

- **Decision reversed.** music2vec has been labeled "JEPA-family" since the very first CLAUDE.md draft and carried that framing through every turn of Phase 1 analysis so far, including treating its cross-model RSA isolation as informative about H1 ("JEPA-family models agree with each other more than with others"). User caught this and walked through the actual architectural distinction: data2vec-style models (music2vec) have a student encoder that operates directly on masked input and predicts the EMA teacher's averaged top-K layer representations — no separate predictor network. JEPA as specified (and as A-JEPA/Audio-JEPA's own methods sections describe it) has three components: context encoder, EMA-updated target encoder, and a *separate predictor network* conditioned on the context representation. Both lineages descend from BYOL and are self-distillation-with-EMA-teacher, but the decoupled predictor is the real dividing line, and music2vec doesn't have one.
- **Practical consequence**: as of this correction there are zero working JEPA-family models in the active comparison. `audio_jepa` (the `ltuncay/Audio-JEPA` substitute, still deferred pending its Hydra-config loader work) will be the *first* JEPA-family data point once wired up, not a second one to compare against music2vec — H1 as originally phrased (mutual agreement *among* JEPA-family models) needs two working checkpoints and isn't testable yet.
- **Corrected everywhere the mislabel appeared**: `CLAUDE.md`'s candidate-model table and H1 statement (both annotated in place with correction notes, not silently rewritten), `audio_comp/models/music2vec.py`'s `ModelInfo.paradigm` field and docstring, `README.md`'s model table. Did NOT rewrite earlier journal entries above that say "JEPA-family" for music2vec — journal is append-only per CLAUDE.md's own convention; this entry is the correction of record.
- **New confound documented for future work**: `ltuncay/Audio-JEPA` is trained on meaningfully less compute than the other active teachers (100k steps, ~14h on 4 V100s, 5,338h AudioSet vs. wav2vec2/data2vec's 400k steps on larger batches), and its own paper reports it substantially underperforming both baselines on several linear-probe tasks (e.g. Speech Commands V1: 0.152 vs. data2vec's 0.927). If it comes back RSA-isolated once wired up, that's confounded between "JEPA-paradigm geometry is genuinely different" and "this checkpoint is comparatively undertrained" — RSA alone can't tell them apart. Documented directly in `audio_comp/models/audio_jepa.py`'s docstring so it's visible right where the loader gets implemented.
- **A specific, testable prediction for later**: the Audio-JEPA paper states its objective favors embedding cohesion over linear separability (strong kNN, weak linear probe on the same tasks) — predicting a *low* TwoNN intrinsic dimension and/or unusually tight within-category clustering once run through `inspect_geometry.py`. This is the opposite direction from music2vec, which had the *highest* ID of the six active models. A low-ID/high-cohesion result for `audio_jepa` would be real, paper-grounded evidence for paradigm-driven geometry differences — meaningfully stronger than an unexplained isolated RSA number. Not run yet — `audio_jepa` still needs its loader wired up first.

## 2026-08-09 (cont'd) — audio_jepa wired up, cohesion prediction confirmed

- User: "we will still treat the second version of audio-jepa as a JEPA model, lets see how it turns out. proceed." Wired up the real loader rather than the originally-assumed Hydra/Lightning reconstruction path — the HF repo (`ltuncay/Audio-JEPA`) ships an author-provided `inference_example.py` alongside `config.json`, both first-party and mutually corroborating every architecture constant. Loads `VisionTransformer` standalone, restores only the `encoder.*` keys from `JEPA.ckpt`'s state dict (predictor + target_encoder discarded per the model's own config.json: "Only the encoder is used downstream"), `load_state_dict(strict=True)` — 0 missing, 0 unexpected keys.
- `vision_transformer.py`'s `Block` unconditionally imports `flash_attn.modules.mha.MHA` (compiled CUDA extension) regardless of `use_flash_attn`. Ported the HF repo's own portable shim: a `torch.nn.functional.scaled_dot_product_attention`-based module matching the checkpoint's `qkv`/`proj` parameter names — no flash-attn build needed, and SDPA gets CUDA-optimized kernels automatically on GPU (not just a CPU fallback).
- **Self-inflicted regression caught and fixed**: before finding the shim, tried installing the real `flash_attn` package to see if that path was simpler. `pip install flash_attn` silently downgraded `torch` 2.11.0 → 2.9.1 as a dependency-resolution side effect (flash_attn's wheel required `torch~=2.9.0`), breaking ABI compatibility with `torchaudio`/`torchcodec`/`torchvision` — would have quietly broken every other model's pipeline. Caught via the next adapter test failing on an unrelated `torchaudio` import error, traced to the version bump, fixed by uninstalling `flash_attn` (unneeded — the shim replaces it) and reinstalling `torch==2.11.0` exactly. Full test suite green afterward.
- **Second real bug, only visible at full scale**: `kaldi.fbank`'s fixed ~98ms analysis window needs the input waveform at least that long; some real probe-set clips (brief UrbanSound8K events, one as short as ~60ms) are shorter, crashing the first full Slurm run. The CPU smoke test before that used a 5s dummy waveform and didn't catch it. Fixed by padding short waveforms to the model's designed 10s input before spectrogram computation.
- Moved `audio_jepa` from `deferred_models` to `active_models`; updated `test_model_adapters.py`'s `DEFERRED_MODELS` (now just `beats`) accordingly. All 7 models now extracted on the full 2000/category (10,000-clip) probe set.
- **Result — the cohesion prediction held, on both fronts specified in advance**:
  - TwoNN intrinsic dimension: **10.82, the lowest of all 7 models** (range 11.5–21.5 for the rest; music2vec highest at 21.5 — opposite end from audio_jepa).
  - `inspect_geometry.py`: within-category mean distance **0.062**, far tighter than hubert (0.256) or music2vec (0.161) — driven by bird/city/ship/speech clustering extremely tightly (diagonal 0.02–0.06) while music sits as a stark outlier (0.42–0.50 from every other category, including its own category average of 0.18). Separation index 1.36, highest of the three models checked this way.
  - This is a real, paper-grounded signature (low ID + tight within-category clustering matching the paper's own stated kNN-vs-linear-probe cohesion claim), not an unexplained number — the disambiguation this project's own tooling was built to enable.
- **Unlike music2vec, audio_jepa is NOT RSA-isolated from the other models.** RSA: audio_jepa↔clap = 0.54 (the single strongest pair in the entire 7-model matrix, edging out mert↔musicfm's 0.51), ↔musicfm = 0.42, ↔mert = 0.24, ↔music2vec = 0.20, ↔hubert = 0.02, ↔wav2vec2 = **-0.08** (essentially zero/slightly negative). CKA shows the same pattern (clap 0.50 highest, wav2vec2 0.13 lowest). So: audio_jepa aligns moderately with the music/general-audio cluster (clap, musicfm, mert) while being essentially decorrelated from the two speech-specialized masked-modeling models — a genuinely different relationship to the rest of the roster than either music2vec's flat isolation or hubert/wav2vec2's tight mutual pairing.
- H1 as originally phrased (mutual agreement *among* JEPA-family models) is still not testable — there is exactly one working JEPA-family model (`audio_jepa`) in the comparison, not two. But that one data point now has a distinctive, paper-predicted, non-confounded-looking signature (the compute/undertraining confound doesn't obviously explain a *low*-ID, high-cohesion result the way it might explain a merely-isolated one) worth carrying into any write-up as a real finding, not just a caveat.

## 2026-08-09 (cont'd) — extended roadmap adopted (Stage 0–6)

- User provided a full staged roadmap taking the project from the current 7-model Phase 1 result through to a capstone predictive diagnostic, restructuring what "Phase 2" means for this project (the original CLAUDE.md sketch — consensus-distilled JEPA vs. single-teacher-distilled vs. from-scratch — is superseded by a more granular staged plan). Folded into `CLAUDE.md` as a new "Extended roadmap" section rather than silently overwriting the original Phase 1/Phase 2 text, per the project's own convention of discussing scope changes and stating assumptions explicitly.
- Stage summary (full detail in CLAUDE.md): Stage 1 consolidates current findings cheaply (downstream linear-probe/kNN validation on existing embeddings, a training-distribution-breadth reframing of the RSA matrix, a written decision-rule outcome). Stage 2 expands the model roster to fill specific missing axes (reconstruction-target, supervised-vs-self-supervised, domain-specific bioacoustic, discrete-token) rather than just adding count. Stage 3 re-runs the full toolkit at ~11 models — the point CLAUDE.md now marks as the Datasets & Benchmarks-track publishable floor. Stage 4 repeats the downstream validation at that scale. Stage 5 is the harder empirical claim: OOD fine-tuning on real vessel/underwater-acoustic data (the project's actual eventual application domain), testing whether frozen-representation geometry predicts adaptability — explicitly flagged as a different object from Aghajanyan/Gupta/Zettlemoyer's fine-tuning parameter-update intrinsic dimension, same name, not to be conflated. Stage 6 (only if Stage 5 finds a real correlation) is a lightweight reusable diagnostic tool — the actual novelty ceiling beyond a benchmark report.
- Not started yet. Asked the user how they want to kick off Stage 1 given the size of the implementation (downstream probe protocol needs concrete decisions — exact X-ARES-matching methodology, train/test split, etc. — that are worth confirming before building rather than guessing and redoing).

## 2026-08-10 — Stage 1 X-ARES run complete: geometry doesn't predict downstream accuracy

- User chose real X-ARES toolkit integration (not a self-built lightweight probe) for genuine comparability to published numbers. Built `xares_eval/`: one encoder wrapper per model (reusing each audio_comp adapter's loading logic but returning unpooled frame-level embeddings, since X-ARES and the RDM pipeline want different things from the same model), validated all 7 via `check_audio_encoder()`.
- User chose the full LibriSpeech-ASR task (WER, needs a Qwen2-0.5B decoder) over the simpler LibriSpeech-Male-Female classification task when the two options were surfaced — heavier engineering, more informative about actual speech capability.
- Several real infrastructure bugs found and fixed, none visible until actually running the toolkit:
  - `xares==0.1.3`'s `download_zenodo_record` always uses Zenodo's bulk "files-archive" endpoint, which Zenodo hard-rejects for any record over 300MB. All three overlapping-task datasets (FMA ~8GB, UrbanSound8K ~7.1GB, LibriSpeech ~7.5GB) hit this immediately — not ours-specific, likely breaks for most real users of this toolkit. Wrote `xares_eval/download_zenodo_task.py`: fetches files individually via Zenodo's per-file API (no size cap there), then plants the same marker files the real downloader checks for.
  - `attr_from_py_path` (X-ARES's encoder/task loader) does a real `import_module()` on the path with `/`→`.` substitution — absolute paths become invalid leading-dot relative imports. `xares_eval` had to become a real importable package, and task/encoder CLI args have to stay relative to CWD.
  - `TaskConfig.env_root` defaults to `"./env"` (CWD-relative, no env-var override) — first attempt put ~15GB combined under `$HOME` and blew the 48GB quota outright (same class of issue as the earlier HF-cache incident, still hasn't fully stopped recurring). Wrote thin `xares_eval/tasks_scratch/` wrapper task files that call the real (external, unmodified) config functions and override `env_root` to an absolute `$SCRATCH` path before returning.
  - `download_hf_model_to_local`'s Qwen2 dependency downloads to CWD with no override — landed inside the repo when run from the repo root (required for the point above), got swept into a `git add -A` and rejected by GitHub outright (942MB safetensors file over the 100MB limit). Caught before it reached the remote (push failed, so only local); `git reset --soft` + gitignored `/Qwen/` and recommitted clean.
  - LibriSpeech-ASR's Qwen2 decoder OOM'd on the same 10GB MIG slice the other 6 extraction jobs used fine — bumped 10GB→20GB→40GB (20 was still short by ~3GB during backward pass; genuinely memory-hungry combination, not a bug).
  - mert and wav2vec2 both hit the initial 4hr time limit mid-training (mert at epoch 6/10, wav2vec2 at epoch 10/10 — both just needed more time, not stuck); bumped default to 6hr and both finished cleanly on resubmission.
- **Validation against published numbers**: hubert's MLP/kNN scores on FMA-genre and UrbanSound8K land within a few points of X-ARES's own published wav2vec2 baseline (FMA MLP 0.482 vs. published 0.469; UrbanSound8K MLP 0.682 vs. published 0.659) — real evidence the encoder-wrapper interface and frame-level extraction are correct, not just "it ran without crashing."
- **Full results** (MLP scores, `results/xares_results.csv`):

  | model | FMA (music) | UrbanSound8K | LibriSpeech-ASR |
  |---|---|---|---|
  | clap | 0.687 | 0.888 | 0.000 |
  | musicfm | 0.615 | 0.799 | 0.000 |
  | mert | 0.570 | 0.774 | 0.000 |
  | audio_jepa | 0.552 | 0.556 | 0.000 |
  | music2vec | 0.521 | 0.601 | 0.020 |
  | hubert | 0.482 | 0.682 | 0.855 |
  | wav2vec2 | 0.185 | 0.226 | 0.000 |

- **The Stage 1 question this was actually for — does geometry predict downstream function — has a clear answer: no, not straightforwardly.** audio_jepa (lowest TwoNN intrinsic dimension, tightest within-category clustering, per the 2026-08-09 cohesion finding) is only mid-pack on both classification tasks, not the best. CLAP — mid-range intrinsic dimension (12.3), not the tightest cluster — is the *best* classifier on both FMA and UrbanSound8K by a clear margin. music2vec has the *highest* intrinsic dimension of all 7 (least "cohesive" by that measure) but isn't the worst classifier either. Cohesion/low-ID is a real, paper-predicted geometric signature (confirmed 2026-08-09) — it just doesn't translate into downstream classification advantage in this data. This is a legitimate, reportable null result for the "geometry predicts function" hypothesis at this stage, exactly the kind of falsifiable check the roadmap called for — not something to force a positive spin onto.
- **Open item, not chased further tonight**: LibriSpeech-ASR returns exactly 0.0 for every model except hubert (music2vec: 0.02, barely above). Per-model this is mostly explicable (clap has no frame sequence to give the ASR decoder; musicfm/mert/music2vec are music-only trained, no speech exposure; audio_jepa's wrapper hard-truncates to its fixed 10s training window, losing the tail of longer LibriSpeech utterances). wav2vec2 is the one case without a clean explanation — same Libri-light-family training domain as hubert, finite/reasonable training loss throughout (no NaN), yet 0.0 vs. hubert's 0.855, a much starker gap than its milder-but-real underperformance on the two classification tasks (0.18–0.23 vs. hubert's 0.48–0.68). Checked and ruled out a normalization-config mismatch (`do_normalize=True` correctly paired with `feat_extract_norm=layer`). Most likely explanation on the table: wav2vec2's final transformer layer is known in the literature (SUPERB-style per-layer probing studies) to be less linearly-probe-friendly than HuBERT's — but that would predict *degraded*, not *exactly-zero*, performance, so this is flagged as genuinely unresolved rather than confidently explained. Worth a targeted look if the LibriSpeech-ASR number specifically becomes analytically important later; doesn't affect the two classification-task results, which are the ones validated against published baselines.

## 2026-08-10 (cont'd) — refining the null result before treating it as final

- User pushed back on the "geometry doesn't predict function" framing before letting it stand: (1) check whether X-ARES's kNN read (not just MLP) tells a different story for audio_jepa, since the source paper's own claim is specifically a kNN-vs-linear-probe split, not a flat "worse at everything"; (2) CLAP winning deserves a real hypothesis (discriminative/contrastive pretraining pressure, à la Wang & Isola alignment/uniformity), not just a note, and a silhouette-score proxy on the RDMs already computed would test it cheaply; (3) wav2vec2's LibriSpeech-ASR zero, given how architecturally close it is to hubert (their tightest RSA pair, 0.80), smells more like a wiring bug than a real result, worth an actual diff before shelving. Right call on all three — the flat null was underspecified.
- **(1) kNN vs MLP rank check**: audio_jepa's *relative rank* improves under kNN vs. MLP on both X-ARES tasks — FMA 4th→3rd (passes mert), UrbanSound8K 6th→5th (passes hubert). Real and direction-consistent with the paper's cohesion/separability split, though modest — not a reversal, audio_jepa doesn't become a top performer under kNN either. The kNN/MLP *ratio* tells a more complicated story: CLAP has the highest ratio of all 7 (0.94 avg — its kNN score is almost as good as its trained MLP score), not audio_jepa (0.69, 3rd). A high ratio means kNN captures most of what training can achieve — CLAP isn't trading cohesion for separability, it's winning at both simultaneously, which argues against reading the ratio itself as a clean cohesion indicator.
- **(2) Silhouette score on our own probe set** (not X-ARES's — the 5-category, 10,000-clip RDM data from Stage 0): **audio_jepa has by far the highest silhouette of all 7 models (0.602 — nearly double CLAP's 0.313, which comes 2nd)**. This is a much sharper version of the 2026-08-09 `inspect_geometry.py` finding (separation index 1.36, highest of the three checked then) now confirmed against all 7 models with a standard metric. So on *coarse* 5-domain discrimination (music/speech/bird/ship/city), audio_jepa's cohesion advantage is real and dominant, not marginal.
  - Correlated this silhouette ranking against X-ARES's average MLP score (FMA+UrbanSound8K) across all 7 models: **Spearman rho = 0.357, p = 0.432 — weak and not statistically significant** (n=7, so likely underpowered, but the direction and magnitude are informative regardless). audio_jepa is the clearest violator: #1 on coarse silhouette, only 6th of 7 on average X-ARES MLP.
  - **Refined finding, replacing the flat null from the earlier entry**: audio_jepa's cohesion advantage is real, large, and specific to *coarse* domain-level discrimination — it does not transfer to the *fine-grained*, within-domain classification X-ARES actually tests (8 FMA genres, 10 UrbanSound8K event types). "Geometry doesn't predict function" was imprecise; the more accurate statement is "coarse-grained cluster cohesion doesn't predict fine-grained linear classification within a domain, and these are measuring different things."
  - CLAP's fine-grained dominance (best MLP on both X-ARES tasks) despite only 2nd-place *coarse* silhouette supports the user's alignment/uniformity hypothesis better than a raw ID/cohesion story does: contrastive training seems to produce representations specifically suited to fine-grained discriminability in a way TwoNN dimension and coarse silhouette don't capture. Not confirmed with a real alignment/uniformity computation yet (Wang & Isola metrics, or a per-class silhouette breakdown) — flagged as the actual next step if this becomes a formal Stage 6 diagnostic candidate, more promising than raw ID alone.
- **(3) wav2vec2 wiring diff**: instantiated both `HubertXaresEncoder` and `Wav2Vec2XaresEncoder` and compared their *measured* (not assumed) properties directly — **identical**: `sampling_rate=16000, output_dim=1024, hop_size_in_ms=20.101` for both. Wrapper code was already confirmed byte-identical apart from the model name (2026-08-10 earlier entry). This rules out a dimension/frame-rate wiring bug definitively — the two encoders feed the Qwen2 ASR decoder structurally identical input. wav2vec2's `Wav2Vec2Model` load report shows several `UNEXPECTED` keys (`quantizer.*`, `project_q.*`, `project_hid.*`) — normal and expected, those are the contrastive-pretraining head's parameters, correctly dropped when loading the plain encoder-only class, not a sign of a bad checkpoint load. **Downgraded from "possible bug, unresolved" to "confirmed not a wiring/config bug, cause still unknown"** — most likely a genuine property of wav2vec2-large-lv60's specific embedding content interacting badly with X-ARES's ASR training for this exact checkpoint, not something in our integration. Not chased further; would need to inspect actual Qwen2 decoder outputs/attention during training to go deeper, which is a bigger undertaking than this check.
  - **Follow-up check, same session**: user specifically flagged `do_normalize`/checkpoint-variant matching (a known HF wav2vec2 gotcha — layer-norm "large/robust" checkpoints need `attention_mask` passed for padded batches, or CTC-style decoding degrades badly while pooled classification barely notices) as worth checking harder. `do_normalize=True` + `feat_extract_norm=layer` was already confirmed correctly paired for wav2vec2-large-lv60 (2026-08-09). Checked the more specific angle: `do_stable_layer_norm` — **identical between hubert (`True`) and wav2vec2 (`True`)**, so it doesn't differentiate them, but checking it surfaced something more concrete: read X-ARES's own `audiowebdataset.py`/`task.py` source directly — its collate function *does* zero-pad variable-length clips into batches, then the encoding loop unpacks the result as `(audio, _)`, **explicitly discarding the lengths tensor** before ever calling any encoder. No model — ours or any other X-ARES-evaluated encoder — ever receives an attention mask from this framework. Real, confirmed, framework-level mechanism, architecturally exactly the risk the user described — but since hubert shares the identical `do_stable_layer_norm=True` flag and scored 0.855 anyway, it doesn't cleanly explain a hubert-vs-wav2vec2 *difference* on its own. Every configuration axis now checked (wrapper code, measured output_dim/hop_size, do_normalize, feat_extract_norm, do_stable_layer_norm) comes back identical between the two models. Genuinely closing this out at "confirmed not a config/wiring mismatch, real but non-differentiating framework-level padding risk identified" — resolving further would need actually implementing attention-mask support (X-ARES doesn't give us the lengths to do this properly without a heuristic) and re-running both models to compare, which is a bounded but separate piece of work, not pursued now.
- **Unrelated but worth recording**: mid-way through this analysis, `$HOME` had a real storage incident (see the dedicated entry below) that stalled everything for a while — the venv had to be rebuilt on `$SCRATCH` and one corrupted repo file recovered before any of the above analysis could actually run.

## 2026-08-10 (cont'd) — $HOME storage incident, diagnosed and recovered

- Mid-way through the analysis above, basic file reads in `$HOME` started failing with `Input/output error`; `diskusage_report` got stuck reporting 97MiB used (down from ~40GB) across many repeated checks over an extended period; `git status` surfaced `"Cannot send after transport endpoint shutdown"` — a network-mount-drop signature. Initially assumed cluster-wide (login node load average was ~7600 again, matching the earlier hubert/mert deadlock investigation's signature), but methodically narrowed it down:
  - An untouched, unrelated venv (`eddyflow-venv`) read its numpy files fine at the same time `audio-comp-venv`'s numpy failed — ruled out a filesystem-wide outage.
  - `pip install --force-reinstall` on the affected package hit the identical class of error reading pip's *own* vendored files — ruled out "just a corrupted numpy install," since the reinstall tool itself couldn't complete.
  - Created a brand-new venv (`audio-comp-venv2`) directly in `$HOME`; `ensurepip` failed writing a *brand-new* file with the same `OSError: [Errno 5] Input/output error` — ruled out "old corrupted files specifically," since this was a first-ever write to a location that had never held data before. This was the conclusive test: proved the problem was active, ongoing, and general to `$HOME` writes at that moment, not isolated to any one file or package.
  - Confirmed `$SCRATCH` unaffected by a trivial write/read/delete — asked the user how to proceed given the scope of a rebuild; they chose to rebuild at a fresh path. Built the venv at `$SCRATCH/audio-comp-venv` instead — clean on the first attempt.
  - `pip install -e .` for `audio_comp` itself kept failing on one specific file (`audio_comp/models/__init__.py`) even after the venv rebuild, consistently across retries (no longer intermittent for this file specifically) — while every sibling file in the same directory read fine. `ls -la` on that one file showed literal `-?????????` (couldn't even `stat` it), confirming a single corrupted inode, not a directory- or filesystem-wide problem by that point. Deleted just that one file and recreated it from its known-good committed content (available from earlier in-session context) — fixed immediately, `git status` confirmed the recreated content matched the committed version exactly (no diff).
  - Net assessment: **not data loss, not caused by anything we did** — likely a transient storage-backend issue on the cluster that happened to leave one file's inode in a bad state by the time it (mostly) resolved. Everything of substance was already git-pushed throughout, so nothing was actually at risk regardless.
  - Also switched `audio_comp` off `pip install -e .` entirely (not just moved venvs) — its wheel-build step had already shown itself to be less robust to this kind of intermittent failure than plain file reads were (it kept failing on that exact file across several retries even before the fully-broken state was reached). Now `PYTHONPATH`-relative, same approach `xares_eval` already used out of necessity (its CLI loader needs CWD-relative paths, incompatible with a normal install). Updated both sbatch scripts and README accordingly.
- Practical impact: roughly the second half of this session's wall-clock time went into diagnosing and recovering from this rather than the analysis itself. Worth remembering as a pattern: this cluster's shared login node has now shown genuinely extreme load (~7000-7600 range, 100+ concurrent users) on at least two separate occasions this project (2026-08-09's hubert/mert deadlock, this incident) — plausibly correlated with, though not proven to cause, storage-layer flakiness. Not something to "fix," just something to expect and diagnose methodically (isolate scope first — other venvs? new files? other filesystem? — before assuming data loss or reaching for a full rebuild) rather than panic or guess.

## 2026-08-10 (cont'd) — DeepShip: metafile record_id turned out unreliable, blocking vessel-grouped split

- Started the underwater-acoustics extension. ShipsEar (full access requires emailing dsantos@gts.uvigo.es, no self-service) and DeepShip (full ~265-ship dataset needs emailing mirfan@mail.nwpu.edu.cn; GitHub hosts only a 63-clip subset across 4 classes: cargo 12, passengership 20, tanker 28, tug 3) are both access-gated for the full data. Confirmed our existing `ship_vessel` probe-set category (`peng7554/DS3500`) has zero labels/metadata (raw audio only) so it can't substitute. User chose to start with DeepShip's 63 GitHub-hosted clips now rather than wait on ShipsEar or skip vessel data.
- Downloaded DeepShip's GitHub subset via zip archive (`scripts/setup_deepship.sh`) rather than `git clone` — `git clone` hit the same `Input/output error`/`Cannot send after transport endpoint shutdown` signature as the earlier `$HOME` incident (this time on `git`'s `.git/logs/HEAD` ref-log append specifically; plain file writes to the same directory succeeded immediately after), and `df -h $HOME` showed a corrupted-looking Lustre report (`-11P` used, `471%`) at the same moment even though `diskusage_report` showed normal quota usage — consistent with "transient backend flakiness," not a real quota or data problem. A `Write` tool call also hit the identical `EIO` once during this session and succeeded on retry. Treating this as the same known, intermittent, not-our-fault cluster storage issue as the earlier incident, not investigating further each time it recurs — just retry, and prefer zip/plain-file operations over git operations on `$HOME` when the two are equally easy (zip has no ref-log write at all).
- Read the metafile format directly (all 4 classes): comma-separated, no header — `record_id, node_id, vessel_name, date(YYYYMMDD), time(HHMMSS), duration_s, range_field("A-B"), <empty>`. Confirmed `duration_s` is real (not estimated) by matching `soxi` output against 4 sample files across all 4 classes (Cargo/15.wav 187s, Passengership/1.wav 37s, Tanker/2.wav 150s, Tug/9.wav 203s — exact match on all four at the time).
- **Then built a systematic check (all 63 hosted files, not just the 4 samples) matching each hosted wav's actual measured duration against its metafile row(s) — and the record_id key turned out not to be reliable as a join key at the scale needed.** Metafiles have far more rows than hosted files (tanker-metafile has 244 rows for only 28 hosted tanker files) and **record_id repeats within a metafile across entirely different vessels/durations** (e.g. tanker record `47` appears twice: `CHERRY GALAXY` dur=246s and `SEAVOYAGER` dur=251s; the actual `Tanker/47.wav` file measures 21s — matching *neither* candidate). Systematic result: Cargo 10/12 mismatched, Passengership 2/20 mismatched, Tanker 15/28 mismatched (13 more only "resolved" by picking whichever duplicate happened to have the closest duration, not a confirmed real match), Tug 0/3 mismatched (but n=3, not informative). The 4-sample spot-check that seemed to confirm the record_id-as-key assumption earlier was, in hindsight, not a representative sample — none of those 4 happened to hit a duplicated ID.
- **Practical consequence: vessel identity, date, and session cannot be reliably recovered for most of this 63-clip GitHub subset.** The only metadata that's structurally guaranteed correct is the class label itself (from the folder name). This directly blocks the user's explicit requirement to split by recording session/vessel instance rather than by random clip — there is no trustworthy vessel-instance signal to group by here. Whoever assembled the GitHub subset most likely renamed files sequentially per class folder, independent of the original metafile's record_id numbering, rather than preserving it.
- Did not silently fall back to a random clip-level split (which would violate the leakage-control requirement without saying so) or silently proceed with the unreliable vessel-name join (which would produce a false sense of leakage control while possibly grouping unrelated vessels together or splitting one vessel's clips across folds). Stopped to get a decision instead: recommended fallback is grouping by *source recording file* (each of the 63 hosted wav files as its own group/session) rather than by claimed vessel identity — reliable (file identity is not in question, only the metafile join is), and still prevents the milder "clips from the same continuous recording pass leak across train/test" failure mode, even though it can't guarantee the same physical vessel never appears in both train and test across *different* files (since we can't confirm which files share a vessel without the broken join).
- **User decision: proceed with file-level grouping** (the recommended option). Rebuilt `xares_eval/deepship/build_manifest.py`: `n_clips`/`duration_s` now come from each file's own measured audio length (`soundfile`), never the metafile's duration field (same unreliability as record_id — using it had silently produced badly wrong clip counts, e.g. tanker's metafile-derived total was 534 10s-clips across 3 folds vs. 153 once measured directly, almost 3.5x inflated). Fold assignment is now file-level greedy-balancing (still deterministic, no seed needed), 3 folds (tug only has 3 hosted files, so more folds would guarantee empty tug folds). `vessel_name`/`date`/`range_field` are kept in the manifest only as a `metadata_confidence` -tagged best-effort field (`exact`/`ambiguous_resolved`/`mismatch`, computed by nearest-duration match) for possible future manual verification — explicitly never used for the split. Final fold balance (clips at 10s each): fold0 183, fold1 182, fold2 180, reasonably even; per-class breakdown also balanced (see `data/deepship_manifest.csv`).
- Packaged as an X-ARES **private** task (`xares_eval/tasks_scratch/deepship_task.py`, `xares_eval/deepship/make_audio_tar.py`) — `private=True` skips X-ARES's Zenodo download path entirely (confirmed via direct read of `xares/task.py`'s `download_audio_tar`/`make_encoded_tar`), so the tars just need to already exist under `env_root/deepship/` before a run. Modeled the TaskConfig on ESC-50's real k-fold pattern (`xares/tasks/esc50_task.py`) — the one difference: `write_audio_tar`'s `labels` argument must be a dict (`{"vessel_class": ...}`) with the class resolved to an int via `label_processor`, matching every other real X-ARES task's convention (UrbanSound8K, FMA, GTZAN) — not ESC-50's own script, which passes raw ints directly and would actually hit a `ValueError` in the copy of `xares==0.1.3` installed here (checked `write_audio_tar`'s source directly; not fixed upstream, just avoided by not copying that particular pattern).
- **Frequency-domain preservation audit** (per the user's explicit requirement — distinguish "poor score = bad representation" from "poor score = preprocessing threw away the relevant band," same principle as the music2vec collapse-vs-idiosyncrasy check earlier). Checked all 7 models' actual feature-extraction code/config directly (not assumed):
  - `hubert`, `wav2vec2`, `mert`, `music2vec` all use `Wav2Vec2FeatureExtractor` (confirmed for MERT via `AutoFeatureExtractor.from_pretrained`) — raw-waveform CNN feature encoders, no mel/fbank front-end at all, so no frequency cutoff is even possible at the feature-extraction stage; the model's learned conv kernels see the full resampled waveform.
  - `clap` (`ClapFeatureExtractor`): checked HF's `feature_extraction_clap.py` source directly — `frequency_min` defaults to **0** (not overridden anywhere in our wrapper), `frequency_max=14000`. No low-frequency cutoff.
  - `musicfm`: builds `torchaudio.transforms.MelSpectrogram(sample_rate=24000, n_fft=2048, ...)` in `modules/features.py` without passing `f_min` — torchaudio's own default is `f_min=0.0`. No low-frequency cutoff.
  - `audio_jepa`: **the one real exception** — `_compute_mel_spec()` calls `torchaudio.compliance.kaldi.fbank(..., low_freq=20, ...)`, an explicit 20Hz high-pass on the mel filterbank's lowest edge (ported directly from the upstream `ltuncay/Audio-JEPA` inference code, not introduced by our wrapper; matches Kaldi's own conventional default). 20Hz is low enough to preserve most vessel acoustic signatures (propeller cavitation broadband, most machinery tonals), but large/slow vessels can have blade-rate or shaft-rate tonals below 20Hz, so this is a real, specific, testable risk for `audio_jepa` specifically if it underperforms on DeepShip — not the other 6 models.
  - Resampling itself (`audio_comp/models/_util.py`'s `resample()`) uses `librosa.resample` (band-limited polyphase/soxr resampling, not naive decimation) — downsampling only low-pass-filters at the *new* Nyquist, which never touches low-frequency content regardless of target rate. Not a risk for any model.
  - Incidental finding: `audio_jepa`'s `SAMPLE_RATE = 32_000` exactly matches DeepShip's native sample rate, so `audio_jepa` is the only one of the 7 models that does **zero resampling** on this task's raw audio.
  - **Net conclusion**: only `audio_jepa` has an explicit, real (if small) low-frequency cutoff among the 7 models; if it underperforms specifically on DeepShip relative to its cross-model geometry/silhouette standing, this 20Hz cutoff is now a documented, checked-not-assumed candidate explanation, not a guess reached for after the fact.
- Validation run launched for `hubert` on the DeepShip task (Slurm job 53975603) before submitting the other 6 models, same "cheapest real validation first" approach used for the original 3-task X-ARES run. Completed cleanly: MLP 0.364, kNN 0.355 (3-way fold average, chance = 0.25 for 4 classes) — pipeline validated end-to-end (tar packaging, `private=True` task config, encode/train/eval all worked on the first real run), so submitted the remaining 6 models immediately after.
- **All 7 models complete on DeepShip** (all classes well above the 0.25 chance baseline for cargo/passengership/tanker/tug). Results (MLP / kNN, 3-fold average, appended to `results/xares_results.csv` via the existing `xares_eval/parse_results.py` — no changes needed, it already generalizes to any task):

  | Model | MLP | KNN |
  |---|---|---|
  | mert | 0.494 | 0.337 |
  | musicfm | 0.430 | **0.386** |
  | wav2vec2 | 0.415 | 0.310 |
  | music2vec | 0.408 | 0.294 |
  | audio_jepa | 0.402 | 0.288 |
  | clap | 0.369 | 0.325 |
  | hubert | 0.364 | 0.355 |

  - The two music-domain-trained models (mert, musicfm) take 1st and 2nd on MLP, and musicfm wins kNN outright — plausible reading: ship engine/propeller acoustic texture (broadband + harmonic tonals) shares more useful structure with music's training distribution than with speech's, but this is two models out of seven and one dataset, not a strong claim — flagging as a candidate pattern to watch when the roster expands (Stage 2), not a conclusion.
  - **audio_jepa is 5th/7 on MLP and dead last on kNN** — consistent with (not proof of) the frequency-domain audit's flagged risk earlier this session: it's the only one of the 7 models with an explicit low-frequency cutoff in its feature extraction (`low_freq=20` in its kaldi.fbank call). Framing this carefully: n=63 real clips, file-level-not-vessel-level split, one dataset — nowhere near enough to confirm the 20Hz cutoff is *why* it underperforms here specifically (could just as easily be the same general-cohesion-over-separability pattern already seen on X-ARES's FMA/UrbanSound8K tasks, where audio_jepa was also mid/lower-pack). Recording the observation, not the conclusion.
  - This is a genuinely small, partial dataset (63 clips, only the GitHub-hosted subset of DeepShip's 265 ships) with a weaker-than-ideal leakage control (file-level, not vessel-level, grouping — see above). Any DeepShip number in a write-up needs both caveats attached every time, not just the first time it's mentioned.
- **DeepShip stage of the underwater-acoustics extension is functionally done**: manifest + tar packaging + task config + full 7-model run + results logged. Remaining per the original sequencing: ShipsEar (blocked on author email, not pursued this session), then BirdCLEF (next).

## 2026-08-10 (cont'd) — BirdCLEF task, all 7 models

- Source: `mteb/birdclef25-mini` (HF dataset) — a curated, balanced 1000-clip subset of BirdCLEF 2025 (50 species x 20 recordings each, sourced 750/250 from Xeno-canto/iNaturalist). Chosen over the raw `sebasgar/birdclef-202{1,2,3}` mirrors (full untouched Kaggle competition zips, tens of GB) and over hand-rolling a subset from raw Xeno-canto. Unlike DeepShip, **no data-integrity problem here** — every row's `url` field is unique across all 1000 rows, confirmed directly, so the row->audio->label mapping needed no join at all (verified, not assumed, per the DeepShip lesson two entries up).
- Recordings are real, highly variable field recordings (1.0s to 491s, median 21s) — not pre-trimmed like ESC-50/UrbanSound8K. Used 5s clips (not DeepShip/FMA's 10s convention) — matches BirdCLEF's own official evaluation window and bird vocalizations' much shorter typical duration than ship engine tonals. Recordings under 5s (69/1000) are zero-padded up to 5s rather than dropped (unlike DeepShip's remainder-dropping) — dropping outright would shrink some species' already-small 20-recording pool. `xares_eval/birdclef/build_manifest.py` / `make_audio_tar.py`, `xares_eval/tasks_scratch/birdclef_task.py`, `scripts/slurm/run_xares_birdclef.sbatch` — same private-task pattern as DeepShip. 5-fold split (not DeepShip's 3), clean and balanced since every species has exactly 20 recordings (4/species/fold) — recording-level grouping is the *correct* leakage control here, not a fallback like DeepShip's file-level split, since every row already is one independent recording.
- Validated end-to-end with `hubert` first (job 53979431: MLP 0.260, kNN 0.068, chance=1/50=0.02), then all 6 remaining models. **All 7 complete**, all above chance:

  | Model | MLP | KNN |
  |---|---|---|
  | clap | **0.284** | **0.189** |
  | hubert | 0.260 | 0.068 |
  | mert | 0.250 | 0.068 |
  | musicfm | 0.247 | 0.113 |
  | music2vec | 0.121 | 0.049 |
  | audio_jepa | 0.089 | 0.043 |
  | wav2vec2 | 0.049 | 0.035 |

  - **CLAP wins decisively on both metrics** — this is now the *third* task (after FMA-genre and UrbanSound8K, 2026-08-10 earlier entries) where CLAP is the best fine-grained classifier despite not having the strongest coarse-domain cohesion. Strengthens the standing hypothesis (not yet formally tested — still the flagged Stage 6 candidate) that contrastive/discriminative training pressure (Wang & Isola alignment/uniformity) is a better predictor of fine-grained classification than raw cohesion/intrinsic-dimension measures.
  - **audio_jepa and wav2vec2 are the bottom two on both metrics** — worth being precise about what this does and doesn't support. audio_jepa's DeepShip weakness (previous entry) has a plausible frequency-domain explanation (its 20Hz feature-extraction cutoff). That explanation **cannot apply here** — bird vocalizations are predominantly well above 1kHz, nowhere near a 20Hz cutoff — yet audio_jepa is still near the bottom. Read together, this actually argues *against* the frequency cutoff being the primary or sole driver of audio_jepa's cross-task weakness, and *for* the already-established, more general "coarse cohesion over fine-grained separability" pattern (2026-08-10 X-ARES entries) being the dominant explanation across all four downstream tasks tried so far (FMA, UrbanSound8K, DeepShip, BirdCLEF). The frequency-cutoff hypothesis stays on record as a plausible *contributing* factor specifically for DeepShip, not a general explanation — don't over-extend it.
  - wav2vec2's weakness here is a third data point (after its unexplained LibriSpeech-ASR zero and its earlier weak FMA/UrbanSound8K showing) in the same direction — still no confirmed root cause, staying logged as an open, checked-but-unresolved pattern rather than reaching for a new explanation without evidence.
- Results appended to `results/xares_results.csv` via the existing `xares_eval/parse_results.py`, unchanged — it already generalized cleanly to a 4th task and a 2nd private task without modification.
- **BirdCLEF stage of the underwater/bio-acoustics extension is functionally done.** Remaining from the original sequencing: ShipsEar (still blocked on author email, not pursued this session).

## 2026-08-10 (cont'd) — checkpoint-provenance schema + Stage 2/5 roadmap formalization

- User provided a detailed spec (informed by external research into checkpoint availability across candidate models) to formalize checkpoint provenance tracking and expand the Stage 2/Stage 5 roadmap sections. Scoped explicitly to registry + CLAUDE.md updates only — no new adapter implementation this pass.
- Added `checkpoint_status` to `ModelInfo` (`audio_comp/models/base.py`): four-value enum (`official_open_weights`, `official_public_weights_license_unclear`, `community_conversion`, `code_only`), with `COMPARISON_ELIGIBLE_CHECKPOINT_STATUSES` restricting the main comparison to the first two. Enforced at two points, not just documented: `register_model()` validates the status is a *known* value at import time; `get_model_class()` — the single chokepoint every pipeline entry point (`extract_embeddings.py`, all 7 `xares_eval/encoders/*.py`) already resolves a model name through — additionally validates the status is *comparison-eligible*, raising a clear error otherwise. Sanity-checked after the change: all 8 currently-registered models still resolve cleanly.
- Retroactively labeled all 8 registered models `official_open_weights`. **Did not blindly apply this to `beats`** as literally instructed, since `beats.py`'s own docstring already recorded its license as "unverified for checkpoint weights" — accepting the instruction at face value would have contradicted an already-recorded, deliberately-flagged finding, exactly the kind of unverified-secondary-label mistake the music2vec correction exists to prevent. Spun up a research pass instead: unilm's root LICENSE is MIT; the BEATs subdirectory's own README defers to that root license with no separate statement or contrary terms for the checkpoint weights specifically. Read as MIT-covered by absence of any carve-out, not an explicit per-checkpoint citation — a real but non-airtight judgment call, documented as such in `beats.py`'s docstring rather than silently upgraded to a confident label. Landed on `official_open_weights` (matching the instruction) but with the reasoning and caveat fully on record, and confirmed this doesn't change beats' active-vs-deferred status (it's still in `deferred_models` — the underlying reason is now confirmed to be *purely* the missing loader, not a checkpoint/license blocker, worth revisiting as a fast follow but not done here).
- Expanded CLAUDE.md's Stage 2 bullet in place (not a new duplicate section — the roadmap already had a Stage 2 entry) with the full Tier 1 (AudioMAE, AST, PANNs CNN14, BirdMAE, Perch 2.0, EnCodecMAE, each tagged with the specific gap it fills) / Tier 2 (WavLM, BYOL-A, Whisper, NatureLM-audio, explicitly deferred) breakdown, the checkpoint_status gate, and NatureLM-audio's flagged pre-requirement (an explicit layer/pooling decision before it can enter any RDM comparison, since it's a multimodal audio-language model rather than a plain encoder). Noted BirdMAE/Perch 2.0's dual purpose explicitly (also serve `xares_eval/birdclef/`, which now exists as of this session — one integration effort, not two, once either is wired up).
- Expanded CLAUDE.md's Stage 5 bullet in place with the two-condition fine-tuning methodology: an "official fine-tuned" condition (released checkpoints, logged separately, never blended into the main comparison) and a "matched fine-tuning" condition (identical protocol across every encoder — same data/split/epochs/optimizer/trainable-layer policy/seeds) that is the only condition used for the actual scientific correlation against Stage 3's frozen-geometry metrics. Also linked DeepShip's 63-clip result forward as an early informal preview of the OOD axis Stage 5 will formalize, not the Stage 5 run itself.
- Per the user's explicit instruction, **no Tier 1 adapter work started this pass** — next step is user confirmation of the updated CLAUDE.md content before any new model integration begins.

## 2026-08-10 (cont'd) — Tier 1 integration begins: PANNs CNN14 + Bird-MAE wired up

- User confirmed the CLAUDE.md content and gave the go-ahead for Tier 1, with a sequencing suggestion: do PANNs CNN14 (only pure-CNN architecture in the roster) and one bioacoustic model first, since architecture-family is a completely untested axis so far (everything so far has varied training objective, not architecture), and doing it early is more likely to surface a genuinely new pattern than AudioMAE/AST (which mostly extend already-motivated axes). Went with PANNs CNN14 + Bird-MAE (over Perch 2.0) as the bioacoustic pick, since Bird-MAE's checkpoint was immediately verifiable and cleanly HF-native-loadable (`trust_remote_code=True`), while Perch 2.0's hosting (likely Kaggle Models/TF-Hub, not HF) would need a separate loader investigation — left for later.
- Also formalized the standing principle behind the `beats` deviation (previous entry) into CLAUDE.md's Working Conventions directly, since the user pointed out it's a general rule worth stating explicitly, not a one-off: verify any factual claim about a model against its primary source before it enters code or a comparison table, even when the claim comes from an instruction or your own earlier output.
- **PANNs CNN14** (`audio_comp/models/panns_cnn14.py`, `scripts/setup_panns.sh`): checkpoint provenance verified directly against `github.com/qiuqiangkong/audioset_tagging_cnn` (not a secondary mirror) via a WebFetch pass — MIT-licensed (`LICENSE.MIT`), official checkpoint on the same authors' Zenodo record (3987831), matching the exact URL the `panns_inference` pip package (same author) downloads from by default. Used `panns_inference`'s `Cnn14` architecture class directly (not its `AudioTagging` wrapper, which hardcodes a `$HOME` checkpoint path and forces `DataParallel` — neither fits this project's conventions) plus our own checkpoint download script, matching the `$SCRATCH`/`$AUDIO_COMP_EXTERNAL` convention. `Cnn14.forward()` takes raw waveform directly (spectrogram extraction happens inside the model) and returns a dict with a 2048-d `embedding` alongside the AudioSet tagging output. Smoke-tested clean on CPU (1s dummy + 60ms short clip, no NaN, both single and batch `embed()`).
- **Bird-MAE** (`audio_comp/models/bird_mae.py`): checkpoint provenance re-verified independently rather than taking the tentative Tier 1 table label at face value (same "verify, don't assume" discipline as `beats`) — DBD-research-group is confirmed to be the actual paper's ("Can Masked Autoencoders Also Listen to Birds?", arXiv 2504.12880) own research group (same group behind BirdSet), so this is a genuine official release. But: **no LICENSE file exists in the GitHub repo at all** (confirmed via GitHub's own license-detection API returning a flat 404, not just "couldn't classify it" — a stronger, more concrete absence than `beats`' "MIT-but-no-explicit-per-checkpoint-line" situation) and no license field is set on the HF model card either. `checkpoint_status="official_public_weights_license_unclear"` is the correct label here, not `official_open_weights` — still comparison-eligible per the schema, but a real flag, not a formality; worth an email to the authors before this checkpoint is used anywhere beyond this internal comparison.
  - Hit a genuine version-skew bug loading it: the checkpoint's own `config.json` declares `transformers_version: "4.38.0"`, but this project's venv has transformers 5.15.0 (a major-version gap). `modeling_bird_mae.py`'s `BirdMAEModel.__init__` never calls `self.post_init()` (confirmed: the string doesn't appear anywhere in the file), which newer transformers now requires to populate `self.all_tied_weights_keys` before `_finalize_model_loading()` — without it, `AutoModel.from_pretrained` crashes with `AttributeError: 'BirdMAEModel' object has no attribute 'all_tied_weights_keys'` before the model ever loads. This is a bug in the checkpoint's own vendored code relative to a newer transformers, not anything specific to this project. Fixed with a narrow, documented monkeypatch on `PreTrainedModel._move_missing_keys_from_meta_to_device` (defaults the missing attribute to `{}`, active only during `load()`) — same category of fix as `audio_jepa`'s flash_attn shim, not a transformers downgrade (which would risk breaking every already-working HF-native model in the roster).
  - Once loaded: uses the model's own bundled `BirdMAEFeatureExtractor` (fbank extraction, fixed 512-frame padding/truncation, dataset-specific mean/std normalization) rather than reimplementing that logic — same reasoning as the audio_jepa kaldi.fbank lesson, reimplementing risks silently diverging from what the checkpoint actually trained on. `BirdMAEModel.forward()` already mean-pools internally (`config.global_pool == "mean"`) and returns the pooled 768-d vector as `last_hidden_state` — despite the name, not a per-frame sequence, no extra pooling needed. Smoke-tested clean on CPU (1s dummy + 60ms short clip → correctly gets zero-padded internally to the model's fixed 512-frame/5s input shape, no NaN).
- Both added to `configs/models.yaml`'s `active_models` (now 9 total) only after passing the same smoke-test discipline as every prior model. Updated `audio_comp/models/__init__.py`, README's dependency list (`panns_inference`, `torchlibrosa` — neither declares a `torch` dependency, confirmed via `pip show`, so no repeat of the flash_attn/torch-downgrade incident) and model table (added a `checkpoint_status` column throughout, corrected `beats`' license cell to match the previous entry's finding), README's one-time-setup-script list, and CLAUDE.md's Tier 1 table (added a `Status` column marking these two `wired up 2026-08-10`).
- Full registry sanity check after wiring: all 9 active models resolve through `get_model_class()` cleanly (the actual comparison-eligibility gate), correct `checkpoint_status` per model. Not yet run through the actual probe-set embedding extraction pipeline (`extract_embeddings.py` on the full 10,000-clip probe set) or the RSA/CKA comparison (Stage 3) — that's the next step, not done in this pass.
- **Pre-registered predictions, written before running the 9-model geometry pass or the PANNs/BirdMAE BirdCLEF re-run** (user's explicit request, same discipline as the audio_jepa low-ID/high-cohesion prediction, 2026-08-09):
  1. **PANNs, architecture axis.** If RSA/CKA agreement between `panns_cnn14` and the transformer-based models is notably *lower* than agreement among the transformer-based models with each other (even ones that differ in training objective), that's evidence architecture family shapes representational geometry independently of training paradigm — every model in the roster until now has varied objective/data, never backbone, so this would be a genuinely new axis, not a replay of an existing one.
  2. **BirdMAE, the sharper test.** BirdMAE is domain-matched (bioacoustic-specific) but reconstruction-based (MAE-style), not contrastive — this directly stress-tests the discriminative-pressure/contrastive-training account of fine-grained classification that's been building since CLAP's wins on FMA/UrbanSound8K/BirdCLEF (2026-08-10 entries). If CLAP still beats BirdMAE on BirdCLEF specifically despite BirdMAE's domain match, that's strong, citable evidence the discriminative-pressure story dominates domain relevance — a genuinely striking result given how "obviously" a specialist should win on its own domain. If BirdMAE wins, that says domain-relevance can override the training-objective effect — the more expected outcome, but not yet confirmed, worth stating as the alternative rather than assumed.
  Sequence: full RSA/CKA/TwoNN pass at 9 models first, then BirdCLEF re-run with `panns_cnn14`/`bird_mae` added, before deciding whether AudioMAE/AST or a write-up pause makes more sense.
- Hit a real bug extracting `panns_cnn14`'s probe-set embeddings: `RuntimeError: Given input size: (256x1x16). Calculated output size: (256x0x8)` — Cnn14's 5 stages of (2,2) time-pooling collapse the logmel time axis to zero for short clips, and the 10,000-clip probe set includes real UrbanSound8K-derived `city_noise` clips as short as 60ms (same class of issue as the audio_jepa kaldi.fbank short-clip lesson, 2026-08-09, just a different model this time). My original smoke test missed this because it tested the 60ms clip only inside a padded batch alongside a 1s clip (which masked the problem), never as a standalone `embed()` call the way `extract_embeddings.py` actually calls it (one clip at a time, batch size 1). Empirically tested the real threshold: fails up to 300ms, first reliably safe at 400ms. Fixed with a 1.0s zero-pad floor in `embed_batch()` (a safety margin, not tied to any PANNs design constant, unlike audio_jepa's fixed-10s window) — re-extracted cleanly afterward, and applied the identical fix in the new `xares_eval/encoders/panns_cnn14_encoder.py` wrapper too, since X-ARES's own dataloader doesn't know about this model-specific minimum either.
- Built `xares_eval/encoders/panns_cnn14_encoder.py` and `bird_mae_encoder.py` for the eventual BirdCLEF re-run, following the same pooled-single-frame-sequence pattern CLAP's wrapper already established (both models pool out the time axis entirely inside `forward()`, so there's no real per-frame sequence to expose — see `clap_encoder.py`'s docstring for the original rationale). Both smoke-tested clean via `check_audio_encoder`.
- **Ran the full RSA/CKA/TwoNN comparison at 9 models** (`results/rsa_matrix.csv`, `cka_matrix.csv`, `intrinsic_dimension.csv`, heatmaps). Took considerably longer than the original 7-model run (~35+ min, backgrounded rather than foreground after a 2-minute timeout) — expected given `compute_rdm`'s O(n^2 * dim) cost on 10,000 clips, and PANNs' 2048-d embedding is the largest in the roster.
  - **Prediction 1 (PANNs, architecture axis): not supported.** Computed directly: mean RSA among the 8 transformer-based models (28 pairs) = 0.2826; mean RSA between `panns_cnn14` and each of those 8 = 0.3336 — PANNs' agreement with the transformer roster is *higher* than the transformers' agreement with each other, the opposite direction the prediction was looking for. No evidence architecture family (CNN vs. transformer) is an independent geometry-shaping axis, at least not in the direction of causing systematic *disagreement*. A clean negative result, stated as such, not reached-for.
  - **Candidate alternative pattern, worth flagging rather than following up on unprompted**: PANNs' two *highest* individual correlations are with `audio_jepa` (0.60) and `clap` (0.56) — both very different paradigms (JEPA-family, contrastive) but both trained on broad, general-audio-scale data (PANNs itself: AudioSet, a broad supervised-tagging corpus). This lines up with the still-not-started Stage 1(b) breadth-hypothesis reframing (narrow-speech / narrow-music / broad-mixed groupings) better than with either the paradigm or the architecture-family framing — PANNs, audio_jepa, and CLAP would all sit in the same "broad-mixed" bucket under that reframing. Not chased further now (Stage 1(b) is still explicitly out of scope for this session), but this is a second independent data point in its favor, worth remembering when that check finally happens.
  - `bird_mae`'s RSA pattern is comparatively flat/uniform across the roster (0.28–0.45, no sharp outlier pairing either way) — nothing sharp enough here to comment on yet; the real BirdMAE test (prediction 2) is functional (BirdCLEF classification), not geometric, and needs the Slurm re-run's actual numbers, not the RSA matrix.
  - TwoNN intrinsic dimension: `panns_cnn14` = 12.524 (unremarkable, sits between `clap` 12.314 and the rest of the pack — no architecture-driven outlier there either), `bird_mae` = 14.348 (also unremarkable). Neither model's ID stands out the way, say, `music2vec`'s 21.5 (highest of all 9) or `audio_jepa`'s 10.8 (lowest) already did.
- BirdCLEF re-run submitted for `panns_cnn14` (job 54058115) and `bird_mae` (job 54058116) — both completed cleanly. Full 9-model BirdCLEF table (MLP / KNN, chance = 1/50 = 0.02):

  | Model | MLP | KNN |
  |---|---|---|
  | bird_mae | **0.345** | 0.183 |
  | panns_cnn14 | 0.308 | 0.134 |
  | clap | 0.284 | **0.189** |
  | hubert | 0.260 | 0.068 |
  | mert | 0.250 | 0.068 |
  | musicfm | 0.247 | 0.113 |
  | music2vec | 0.121 | 0.049 |
  | audio_jepa | 0.089 | 0.043 |
  | wav2vec2 | 0.049 | 0.035 |

  - **Prediction 2 (BirdMAE, the sharper test): result is real but more nuanced than either clean alternative posed.** BirdMAE *does* beat CLAP on MLP (0.345 vs. 0.284) — domain-relevance winning out over CLAP's discriminative/contrastive-pressure edge, the "more expected" outcome the prediction named as the alternative. But CLAP is still (barely) ahead on KNN (0.189 vs. 0.183 — a ~3% relative gap on n=1000, arguably within noise, not a clean win either way). So it's not a flat "BirdMAE wins" — it wins the trained-head comparison and ties/slightly loses the untrained raw-geometry comparison.
  - **The complication worth stating plainly, not glossing over: PANNs shows the *exact same qualitative pattern* relative to CLAP, and PANNs is not domain-matched to birds at all** (general AudioSet supervised tagging) — MLP 0.308 > CLAP's 0.284, KNN 0.134 < CLAP's 0.189. If domain-relevance were the operative explanation for BirdMAE's MLP win, there's no reason a general-purpose, non-bioacoustic model should show the identical MLP-wins/KNN-loses relationship to CLAP. It does. This points to a cleaner, more general explanation than "domain match beats discriminative pretraining": **CLAP's contrastive embedding space is more linearly/metrically separable out of the box (higher KNN, no training needed), while both PANNs (supervised classification) and BirdMAE (reconstruction MAE) produce features that a trained MLP head can exploit *more* than CLAP's despite being less separable untrained.** This is a genuine extension of the project's standing cohesion-vs-separability headline finding (2026-08-10, Stage 1a): it's not just "coarse cohesion vs. fine-grained separability don't transfer" — training objective also seems to trade off *raw embedding-space separability* (what KNN measures) against *trainable-head exploitability* (what MLP measures), and this trade shows up whether or not the pretraining domain matches the downstream task. Domain-relevance (BirdMAE's actual bird training data) may still matter at the margin — it does have the single highest MLP score of all 9 — but the PANNs comparison shows domain match isn't *necessary* to get the same qualitative MLP/KNN split, so it can't be the primary explanation on its own.
  - Not a fully resolved account yet — this is now three separable axes in play (coarse cohesion, fine-grained separability, and this new trained-head-exploitability-vs-raw-separability split) with only 9 models and 4 downstream tasks to trace them through. Flagging as the sharpest current lead for the eventual Stage 6 diagnostic (already candidate-marked for Wang & Isola alignment/uniformity metrics) rather than claiming it's settled.
- **Process fix, logged directly into CLAUDE.md's Stage 2 gate**: "test the shortest edge-case clip standalone (batch size 1), not only inside a batch alongside a longer clip" is now a permanent, explicit smoke-test requirement — this exact blind spot (a short clip padded up to a longer batch-mate's length never exercises the genuinely-short code path) caused a full-scale-only crash twice now with the same root shape: audio_jepa's kaldi.fbank window (2026-08-09) and panns_cnn14's pooling stack (2026-08-10, this session). Not treating it as a one-off anymore.

## 2026-08-10 (cont'd) — Stage 1(b) breadth-hypothesis check, finally run

- User pointed out this had been deferred three separate times without ever being run, each time with a new independent signal pointing at it: the original 10k-scale pattern break, audio_jepa aligning with clap over same-paradigm models, and now (this session) PANNs' strongest correlations landing on audio_jepa/clap specifically. Cheap and already-collected — no new extraction, just characterizing each of the 9 models' training-corpus breadth and testing it against the RSA matrix that already exists. Ran it: `audio_comp/pipelines/breadth_hypothesis_check.py`.
- Method: partition the 9 models two ways — the implicit domain+paradigm framing used through Stage 0, and the breadth framing (narrow-speech / narrow-music / narrow-bioacoustic / broad-mixed / self-distillation-narrow-music, extending the original Stage 1(b) sketch with `panns_cnn14`→`broad_mixed` (AudioSet is genuinely broad, 527 diverse everyday-sound classes) and `bird_mae`→new `narrow_bioacoustic` bucket) — then compute mean-within-group RSA minus mean-between-group RSA for each, plus a permutation test (20,000 reshuffles of group labels, same group sizes held fixed) for a significance read given how few models/pairs there are to work with.
- **Results**:
  - domain+paradigm: gap = 0.385 (mean_within 0.658 over only 2 pairs — `hubert`-`wav2vec2` and `mert`-`musicfm`, the only two non-singleton groups this framing produces), permutation p = 0.0095.
  - breadth: gap = 0.360 (mean_within 0.604 over 5 pairs), permutation p = 0.0003.
- **Read carefully, not just "which gap is bigger" — the raw gap is nominally slightly larger under domain+paradigm, but that's a thin, potentially fragile result.** Domain+paradigm's entire gap rests on exactly 2 pairs, both of which breadth *also* captures identically (`narrow_speech` and `narrow_music` contain the same two pairs). The real difference between the framings is what each does with `audio_jepa`, `clap`, `panns_cnn14`, and `bird_mae`: domain+paradigm scatters all four into singleton groups with zero explanatory power (5 of its 7 groups are singletons — 5 of 9 models contribute no within-group pairs at all), while breadth groups `audio_jepa`+`clap`+`panns_cnn14` into `broad_mixed` and captures a real, significant 3-way mutual agreement there (pairwise RSA 0.54–0.60, all well above the roster's overall 0.29 average) that domain+paradigm has no account for whatsoever. Only `bird_mae` and `music2vec` remain ungrouped under breadth (7/9 models covered) vs. 5/9 under domain+paradigm.
- **Verdict, per the Stage 1(b) gate ("if it doesn't explain better, keep domain+paradigm"): breadth wins, adopted as the working explanation.** Not because its raw gap is bigger (it's marginally smaller) but because it explains meaningfully more of the roster's structure with a far stronger significance signal (p=0.0003 vs. 0.0095) — it's a strict extension of what domain+paradigm already captured (the two narrow-domain pairs) plus real structure domain+paradigm couldn't touch (the broad_mixed cluster). This closes out Stage 1(b), marked "not started" in CLAUDE.md until now — three independent signals across this project (the original 10k pattern-break, audio_jepa/clap alignment, PANNs' correlations) all turned out to point at something real once actually tested, not just plausible-sounding in hindsight.

## 2026-08-10 (cont'd) — Wang & Isola alignment/uniformity computed for real, replacing the silhouette proxy

- Implemented `audio_comp/geometry/alignment_uniformity.py` (both metrics, L2-normalized/unit-hypersphere per the original paper) and `audio_comp/pipelines/alignment_uniformity_check.py` (computes both for all 9 models on the existing probe-set embeddings, correlates against downstream KNN/MLP using only already-collected data — no new extraction). **Adapted, not literal, definition of alignment**: the original paper's "positive pairs" are augmented views of the same instance; this probe set has no such structure, so alignment here uses same-*category* clips (5 coarse categories) as the positive-pair proxy — stated plainly, not implied to be the textbook metric unmodified.
- Two correlation scopes, both free (no new Slurm jobs): BirdCLEF (all 9 models, the only task every current model shares) and FMA-genre+UrbanSound8K average (the original 7-model roster, pre-`panns_cnn14`/`bird_mae` — matches the exact task pairing the original silhouette-vs-MLP check used, so this is a direct upgrade of that check with the real metric).
- **Results, and an honest methodological caveat first**: alignment and uniformity turned out to be **perfectly rank-correlated with each other across all 9 models** (Spearman = -1.0, Pearson = -0.991, p<1e-6) — meaning the same-category-proxy adaptation of alignment isn't capturing anything independent of overall embedding-space dispersion here, unlike the original paper's design where true instance-level positive pairs make alignment and uniformity meaningfully complementary. In this project's data the two numbers are really one axis (call it "embedding-space dispersion"), not two — report them as such, not as independent cross-validating evidence.
  - **FMA+UrbanSound8K (7 models, original roster): uniformity predicts MLP very strongly** — spearman(uniformity, MLP) = -0.964, p<0.001 (more uniform/dispersed embedding space → higher MLP classification accuracy, the theoretically expected direction). KNN correlation is weaker and only marginal: -0.750, p=0.052.
  - **BirdCLEF (9 models, includes panns_cnn14/bird_mae): the pattern flips direction** — uniformity correlates *more* with KNN (-0.633, p=0.067) than with MLP (-0.400, p=0.286), neither reaching significance at n=9. This is the direction the PANNs/BirdMAE mechanistic hypothesis (previous entry) would predict ("uniformity should track KNN specifically, the untrained-separability side of the split") — but it directly contradicts what the FMA+UrbanSound8K scope showed.
  - **Net verdict: genuinely mixed, not a clean validation of the "uniformity predicts KNN better than MLP" mechanistic hypothesis.** True in one scope, false in the other, and both scopes are underpowered (n=7, n=9) to resolve the disagreement definitively — this isn't being explained away, it's an honest split result. What *is* solidly established: uniformity/dispersion is a real, strong, quantitative predictor of downstream MLP performance in the original 7-model roster (p<0.001, not marginal) — a substantially stronger and more specific result than the earlier silhouette proxy managed (rho=0.357, p=0.43, not significant, 2026-08-10 earlier entry). The real metric is a clear upgrade over the proxy even though it doesn't cleanly settle the sharper KNN-vs-MLP question yet.
  - Not chased further this session — resolving the scope disagreement would need either a larger/more powered model roster (more Tier 1 additions) or a genuinely independent (non-same-category) way to compute alignment, not more analysis of the same 9-model, 2-scope data. Logged as the honest current state of the Stage 6 diagnostic candidate, not a finished result.

## 2026-08-10 (cont'd) — true instance-level positive pairs for alignment (fixing the degeneracy) + AST/AudioMAE (Tier 1, continued)

- User's read on today's alignment/uniformity result: the -1.0 rank correlation is fixable, not just a limitation to note — same-category pairs conflate "close because genuinely similar" with "close because same broad label" (every "ship" clip paired with every other "ship" clip, regardless of whether they're actually acoustically similar). Recommended a true instance-level positive-pair proxy (augmented views of the same individual clip) instead, in parallel with continuing Tier 1 (AudioMAE + AST specifically — AST isolates "is it supervision specifically" from "is it PANNs specifically" by adding a second supervised model with a *different* architecture; AudioMAE adds a third non-contrastive candidate to check whether PANNs/BirdMAE's shared MLP-wins/KNN-loses pattern is a general non-contrastive-objective property or three isolated cases).
- **Built the true-pair fix**: `audio_comp/data/build_augmented_probe_subset.py` samples 100 clips/category (500 total, stratified, seeded) from the probe set and pitch-shifts each by +2 semitones (`librosa.effects.pitch_shift` — a standard augmented-positive-pair choice in the contrastive audio literature, raw-waveform-level so it needs zero model-specific code, unlike SpecAugment-style masking). Extracted embeddings for all 500 original + 500 augmented clips across all 9 models (`scripts/slurm/extract_embeddings_augmented.sbatch`, reusing `extract_embeddings.py` unmodified via a different manifest/data-root). Added `alignment_score_paired()` to `audio_comp/geometry/alignment_uniformity.py` — the literal Wang & Isola definition (mean squared distance between true (original, augmented) pairs), not an adaptation, kept alongside the old category-proxy version for the historical comparison rather than deleting it.
- **Result: real but partial decorrelation, and one new, actually-significant correlation.** Paired-alignment vs. uniformity: Spearman -0.917 (p=0.001) — down from the category-proxy's perfect -1.000, so the fix genuinely helped, but the two metrics remain strongly related, not fully independent (plausibly because pitch-shift is a fairly mild perturbation, so a positive pair's distance is still heavily influenced by the same overall embedding-space-dispersion factor driving uniformity — a true independent measurement might need a more aggressive/varied augmentation set). The BirdCLEF scope now shows a **genuinely significant** correlation for the first time: spearman(paired_alignment, KNN) = +0.717, **p=0.030** (vs. MLP's +0.583, p=0.099, not significant) — the direction the PANNs/BirdMAE mechanistic hypothesis predicts, and now with real significance at n=9, not just a suggestive direction. But the FMA+UrbanSound8K scope (7 models) still reverses it: paired_alignment vs. MLP = +0.821 (p=0.023, significant) vs. KNN = +0.536 (p=0.215, not significant) — the same scope-dependent flip persists even with the corrected metric. (Sign note: positive correlation here, not negative, because pitch-shift alignment measures distance directly rather than the log-negative-exponential uniformity does — a model less invariant to pitch-shift, i.e. worse/higher raw alignment, is also typically a more generally-dispersed embedding space, i.e. better/more-negative uniformity; the two track each other in the "performance" direction once the sign conventions are worked through, consistent with the strong residual anti-correlation between them.)
- **Honest verdict: this was worth doing — real progress, not a wasted detour — but doesn't resolve the core question.** The degeneracy is confirmed fixable (partial decorrelation achieved) and the corrected metric surfaces a new significant KNN correlation that the degenerate proxy couldn't show cleanly. But the scope-dependent reversal (BirdCLEF favors KNN, FMA+UrbanSound8K favors MLP) survives the fix, meaning it's more likely a genuine roster-composition effect (which models happen to be in each scope) than purely an artifact of the degenerate proxy, as originally hypothesized. Resolving it now most plausibly needs the larger, more powered roster the AudioMAE/AST work below is building toward, rather than a better positive-pair definition alone.
- **AST** (`audio_comp/models/ast.py`): checkpoint provenance verified directly against the primary source (`github.com/YuanGongND/ast`, the paper's own repo, Interspeech 2021) — BSD-3-Clause, matches `MIT/ast-finetuned-audioset-10-10-0.4593`'s HF card license exactly ("MIT" is the authors' institution, not the license name). Clean HF-native integration (`ASTModel`/`ASTFeatureExtractor`, real per-frame `last_hidden_state` unlike CLAP/PANNs/Bird-MAE, uses the model's own `pooler_output`). Uses the AudioSet-*trained* checkpoint, not a "base, non-finetuned" variant — documented explicitly why this isn't a violation of the project's usual "avoid -ft checkpoints" rule: AST has no two-stage self-supervised-then-finetuned structure the way wav2vec2/hubert do, its supervised AudioSet training *is* its designed pretraining, and CLAUDE.md's Tier 1 table specifically wants this checkpoint to test supervision's effect independent of architecture (now that architecture itself has been ruled out as an explanatory axis, 2026-08-10 earlier entry). Smoke-tested clean, including the shortest edge-case clip standalone (60ms, batch size 1) per the process fix logged earlier today.
- **AudioMAE** (`audio_comp/models/audiomae.py`, `scripts/setup_audiomae.sh`): checkpoint provenance verified directly against `github.com/facebookresearch/AudioMAE` — CC-BY-4.0, explicitly stated to cover the whole project including weights (no judgment call needed here, unlike `beats`). Checkpoint (ViT-B, AudioSet-2M pretrained-only, not finetuned) downloaded from the repo's own README-linked Google Drive file via `gdown`. **Exact model-construction kwargs read directly out of the checkpoint's own saved `args` Namespace** (pretraining scripts save the full argparse config into the checkpoint dict) and cross-checked against the state dict's own tensor shapes (`pos_embed` (1,513,768) => 512 patches + cls token, matching img_size=(1024,128) at patch_size=16) — not assumed from the README or guessed, the same discipline as every other checkpoint-provenance check this session.
  - **Four separate, genuine version-skew issues hit and fixed, all with narrow documented shims, none with a global downgrade**: (1) `util/misc.py` does `from torch._six import inf` — removed in PyTorch 2.0 (project runs 2.11.0); (2) the repo's `Block(..., qk_scale=...)` call needs an old timm API modern timm removed — pinned `timm==0.4.9` project-wide (safe: confirmed via `pip show timm`'s empty `Required-by` that nothing else in the roster depends on timm, so no shared-dependency risk like the flash_attn/torch incident); (3) that same old-timm-required `Block.qk_scale` conflicts with the checkpoint's `decoder_mode=1` Swin-based decoder needing a *newer* timm `SwinTransformerBlock` API (`feat_size`/`drop_attn` args) — no single timm version has both APIs (tested 0.3.2, 0.4.9, 0.4.12, 1.0.28); resolved by constructing with `decoder_mode=0` instead (the decoder is never used — `forward_encoder_no_mask()` only touches the encoder) and loading the state dict with `strict=False`, with an explicit runtime assertion that every unmatched key is decoder-prefixed, so the encoder's correctness is *verified*, not assumed; (4) `util/pos_embed.py`'s sin-cos position-embedding initializer uses `np.float`, removed in numpy >=1.24 — restored as a one-line alias, harmless since that initial value is immediately overwritten wholesale by `load_state_dict` anyway. Smoke-tested clean afterward, including the standalone 60ms edge case.
- Both added to `configs/models.yaml`'s `active_models` (now 11 total). Full-probe-set extraction submitted for both (jobs 54068294 `ast`, 54068295 `audiomae`) — results pending, 11-model RSA/CKA/TwoNN re-run and BirdCLEF re-run to follow once extraction completes.

## 2026-08-10 (cont'd) — 11-model results land: breadth confirmed stronger, and the KNN/MLP story gets a real correction

- Full-probe extraction succeeded for both new models (10,000 embeddings each, `ast` 768-d, `audiomae` 768-d). X-ARES encoders written (`xares_eval/encoders/ast_encoder.py`, `audiomae_encoder.py` — both use the real per-frame sequence, unlike CLAP/PANNs/Bird-MAE's pooled-single-frame workaround: AST has a genuine ViT patch-token sequence, AudioMAE's `forward_encoder_no_mask()` returns one before the adapter's own mean-pool), smoke-tested clean, BirdCLEF re-run submitted alongside the 11-model RSA/CKA/TwoNN re-run.

- **Breadth-hypothesis check, re-run at 11 models — even more decisive than at 9.** `ast` and `audiomae` both trained on AudioSet (broad, diverse general-audio data, same argument already applied to `panns_cnn14`), so both join `broad_mixed` (now 5 members: `audio_jepa`, `clap`, `panns_cnn14`, `ast`, `audiomae` — categorized by documented training-data breadth, not by how well each one happens to fit the group; `audiomae`'s actual RSA correlations with the rest of the group are noticeably weaker (0.27-0.52) than the other four's mutual correlations (0.55-0.84), and it's included anyway rather than moved to a category invented to fit better — that weaker fit is itself honest information, not smoothed over). Result: breadth's permutation p-value is now **0.0000** (0/20,000 reshuffles matched or exceeded the observed gap) vs. domain+paradigm's p=0.0082 — even stronger than the 9-model result's already-clear 0.0003 vs. 0.0095. Breadth's raw gap (0.330) is now a bit further below domain+paradigm's (0.370) than at 9 models (`audiomae`'s weaker fit pulls the group average down), but the significance gap widened anyway, because breadth is now explaining 12 real within-group pairs (the 5-way `broad_mixed` cluster alone contributes 10) against domain+paradigm's same thin 2 — a much harder pattern to produce by chance. Breadth now covers 9/11 models in real (non-singleton) groups vs. domain+paradigm's 4/11. **This is now the fourth independent thing in this project pointing at breadth** (the original 10k-scale pattern break, `audio_jepa`/`clap` alignment, PANNs' correlations, and now AST's correlations landing in the same cluster) — the working explanation keeps getting more entrenched every time a new model is added, not less.

- **BirdCLEF, full 11-model table (MLP / KNN, chance = 0.02):**

  | Model | MLP | KNN |
  |---|---|---|
  | **ast** | **0.394** | **0.239** |
  | bird_mae | 0.345 | 0.183 |
  | panns_cnn14 | 0.308 | 0.134 |
  | clap | 0.284 | 0.189 |
  | hubert | 0.260 | 0.068 |
  | mert | 0.250 | 0.068 |
  | musicfm | 0.247 | 0.113 |
  | audiomae | 0.167 | 0.039 |
  | music2vec | 0.121 | 0.049 |
  | audio_jepa | 0.089 | 0.043 |
  | wav2vec2 | 0.049 | 0.035 |

  - **AST wins outright on both metrics** — not a trade-off, a clean sweep, beating `bird_mae`'s previous-best MLP (0.345) and `clap`'s previous-best KNN (0.189). This directly answers the question AST was added to test ("is it supervision specifically, or is it PANNs specifically" driving PANNs'/Bird-MAE's MLP-wins/KNN-loses-to-CLAP pattern): **it's not supervision as a category.** AST is also supervised, also AudioSet-trained, and shows *none* of PANNs'/Bird-MAE's/AudioMAE's KNN weakness relative to CLAP — it beats CLAP's KNN outright. The clean "non-contrastive objectives trade raw separability for trainable-head-exploitability" story from the earlier PANNs/Bird-MAE entry does not survive AST's addition as originally stated.
  - **The AudioMAE-vs-Bird-MAE pair is the cleanest isolated domain-relevance test run in this project so far, and it says domain relevance is real.** Same training objective (reconstruction-MAE) on both, only the training domain differs (AudioMAE: general AudioSet; Bird-MAE: bird-only BirdSet) — and Bird-MAE wins by a wide margin on the bird-specific task (MLP 0.345 vs. 0.167, more than double; KNN 0.183 vs. 0.039, more than 4x). Unlike the earlier PANNs-vs-Bird-MAE comparison (different objectives, so domain-relevance and objective were confounded), this pair holds objective constant and isolates domain match cleanly. Worth restating precisely: this doesn't reinstate domain-relevance as *the* explanation for Bird-MAE's earlier win over CLAP (PANNs, non-domain-matched, still showed that pattern too) — it establishes that domain relevance is a real, additional, independently-acting effect on top of whatever the objective-driven pattern is.
  - **Revised account, replacing "non-contrastive trades KNN for MLP" with something that survives AST:** what actually separates the roster isn't contrastive-vs-not, it's closer to **discriminative training pressure (contrastive, like CLAP, *or* supervised classification with real labels, like AST) produces embeddings that are well-separated in raw distance (high KNN) *and* trainable-head-exploitable (high MLP) — these two properties aren't in tension for discriminatively-trained models.** Reconstruction-based training (Bird-MAE, AudioMAE) produces comparatively weaker raw separability (lower KNN) while still being reasonably MLP-trainable, i.e. *that* pairing does show the earlier-observed trade-off. PANNs (supervised but CNN, weaker on both metrics than AST despite the same AudioSet supervision) suggests architecture may modulate how well supervised training pressure gets converted into separable geometry, but isn't disqualifying on its own — PANNs still clearly outperforms every reconstruction-trained and self-supervised model except Bird-MAE and CLAP. Flagging this as the current best account, not a settled one — it now needs to survive whatever Perch 2.0 or EnCodecMAE show, the same way the original account didn't survive AST.
  - `audiomae`'s weakness here (worst of the AudioSet-trained models, barely above the weakest self-supervised models) is consistent with, not contradicted by, its notably weaker RSA fit within `broad_mixed` above — both point the same direction: general-domain reconstruction training produces a geometry that's the least "typical" member of the broad-training cluster, on both the RSA-agreement axis and the downstream-function axis.
- Results appended to `results/xares_results.csv` and `results/rsa_matrix.csv`/`cka_matrix.csv`/`intrinsic_dimension.csv` via the existing pipelines, unmodified.

## 2026-08-10 (cont'd) — Perch 2.0 checked and deferred; BirdNET added as an isolated-environment exception

- User's proposed next step: one more targeted addition to complete a clean 2x2 (objective type: discriminative vs. reconstruction) x (domain: bioacoustic vs. general) — cells already filled: AST=discriminative/general, PANNs=discriminative/general (redundant with AST on domain, useful for the architecture question), Bird-MAE=reconstruction/bioacoustic, AudioMAE=reconstruction/general. Missing cell: discriminative/bioacoustic. Perch 2.0 proposed as the candidate, explicitly gated on verifying its actual training objective first — "worth confirming from its paper before assuming."
- **Perch 2.0 checked against the primary paper (Chen et al., "Perch 2.0: The Bittern Lesson for Bioacoustics," arXiv:2508.04665, Aug 2025) — does not cleanly fill the cell.** Its objective is a hybrid: supervised multi-taxa classification as the backbone, plus self-distillation via a prototype-learning classifier, plus a "source-prediction" training criterion. Not a clean discriminative counterpart to AST/PANNs. Also confirmed TensorFlow-only (Kaggle Models / TF-Hub SavedModel, Apache-2.0, no official PyTorch/JAX/ONNX release).
- User's response to this: pursue both an isolated-environment path for Perch 2.0 specifically (separate venv/subprocess writing embeddings to disk, main pipeline reads them in, zero dependency-contamination risk) **and** check whether BirdNET has a more PyTorch-friendly path as an alternative.
- **BirdNET checked against the primary source (Kahl et al., github.com/kahst/BirdNET-Analyzer)** — confirmed clean supervised classification (sigmoid + BCE multi-label over species, no contrastive/self-distillation component), a better conceptual fit for the missing cell than Perch 2.0's hybrid. But also TensorFlow/TFLite-only, same framework problem as Perch 2.0 — doesn't avoid the isolation requirement, just has a cleaner objective. Given the isolation infrastructure was needed regardless of which model got picked, went with BirdNET (cleaner objective, simpler official checkpoint/embedding-extraction path) and left Perch 2.0 documented-and-deferred rather than building both — the isolation infrastructure built for BirdNET works unchanged for Perch 2.0 if a second bioacoustic-domain check is wanted later.
- **Built the isolation**: `scripts/setup_birdnet_env.sh` creates a fully separate, CPU-only venv (`$SCRATCH/birdnet-venv`) with `birdnet-analyzer` + TensorFlow — zero shared dependencies with the main torch-based `audio-comp-venv`, so zero contamination risk regardless of what TF/CUDA does internally. `birdnet_analyzer`'s own checkpoint downloader (`ensure_model_exists()`) hit a 404 on its default URL (`tuc.cloud`, apparently stale) — downloaded the official Zenodo v2.4 TFLite release (zenodo.org/records/15050749) directly instead, same "don't patch the broken thing, get it from a verified alternate official source" approach already used for FMA/UrbanSound8K's broken Zenodo bulk-download and DeepShip's git-clone-vs-zip issue.
  - Confirmed the package's pip-reported version (2.2.0, the latest resolvable given this cluster's TensorFlow wheelhouse tops out below the 2.4.0 package's `tensorflow>=2.20` requirement) still bundles the **model** checkpoint labeled `V2.4` — package version and model version are independent numbering schemes here, verified by reading `cfg.MODEL_VERSION` directly rather than assuming version 2.2.0 meant an older/different model.
  - Bypassed `birdnet_analyzer`'s higher-level `embeddings()` API (needs `perch_hoplite`, which failed to resolve — an unexpected but real dependency link between BirdNET-Analyzer's newer embeddings workflow and the Perch/Hoplite ecosystem) in favor of the lower-level `model.embeddings(sample)` function (direct TFLite interpreter call, feature layer instead of the classification layer, zero extra dependencies) — simpler and more robust than fighting a fragile extra dependency chain for functionality that boils down to one interpreter call.
  - `scripts/birdnet_extract_embeddings.py`: standalone script (NOT part of the `audio_comp` package, cannot be imported from the main venv) that reads the probe-set manifest, chunks each clip into BirdNET's expected 3s/48kHz segments (short clips zero-padded, long clips mean-pooled across chunks — same conventions as every other adapter in this project), and writes a `.npz` in the **exact schema** `extract_embeddings.py` produces (`clip_ids`, `embeddings`) — every downstream pipeline (RSA/CKA/TwoNN, breadth check, alignment/uniformity) picks it up transparently by globbing the embeddings directory, no changes needed on the PyTorch side. Smoke-tested standalone on the 60ms edge case and a real multi-chunk 30s clip before running on the full probe set (same discipline as every torch-based adapter, even though this one can't live in the same registry).
  - This is a genuine architectural exception, documented as such rather than forced into the existing pattern: no `audio_comp.models.birdnet` adapter exists or can exist (no `BaseAudioEncoder` subclass works across a process boundary), so it's not `@register_model`-registered and not covered by `registry.py`'s `checkpoint_status` enforcement. Provenance (code MIT, model weights **CC BY-NC-SA 4.0** — more restrictive than most of the roster, comparable to mert/music2vec's CC-BY-NC-4.0 already flagged there; non-commercial research use is unrestricted) documented directly in CLAUDE.md and the extraction script's docstring instead.
  - Full-probe-set extraction (10,000 clips) launched in the background on CPU — completed cleanly, ~0.4s/clip, ~67 min total, no errors on the full run after the standalone-edge-case smoke test passed.
- **Hit, then properly fixed, a real infrastructure bug running the 12-model RSA/CKA/TwoNN comparison**: the background `nohup` `compare_models.py` process (which had worked, if slowly, for the 9- and 11-model runs via the same pattern) got silently killed within ~2.5 minutes at 12 models, twice in a row, with no error in its log and no crash on a direct foreground re-run — almost certainly the login node's own reaper/cgroup limits catching a CPU/memory-heavy background process, exactly the kind of shared-resource use those limits exist to stop. This should have been a Slurm batch job from the start, same as every other compute-heavy step in this project — fixed properly with `scripts/slurm/compare_models.sbatch` (CPU job, `def-spadon_cpu`, 8 cores/32GB/1.5hr) rather than continuing to fight the login node with more background-process workarounds. Ran clean on the first Slurm submission.
- **BirdNET's RSA fit, and what it complicates.** BirdNET's correlations with every other model are weak — max 0.32 (`musicfm`), most in the 0.05–0.25 range, one **negative** (`audiomae`, -0.12). Notably, its correlation with `bird_mae` — the other `narrow_bioacoustic` member, same domain, opposite objective (discriminative vs. reconstruction) — is only 0.21, much weaker than the same-domain same-objective pairs elsewhere in the roster (`hubert`-`wav2vec2` 0.80, `mert`-`musicfm` 0.51). This is real, specific evidence — not just a plausible-sounding story — that training *objective* shapes representational geometry even *within* a fixed narrow domain, a refinement the breadth-only account doesn't capture on its own (flagged as a possibility in the script's own comment before this result came in, not fitted after the fact).
  - **Breadth-hypothesis check re-run at 12 models**: permutation p-value **stays at 0.0000** (BirdNET's weak fit doesn't break the overall result — the 5-member `broad_mixed` cluster alone still carries enough real structure), but the raw gap drops further (0.321, down from 11-model's 0.330) as `domain+paradigm`'s rises slightly (0.396, up from 0.370, essentially noise at n=2 pairs) — BirdNET is a genuine, acknowledged complication for the `narrow_bioacoustic` group specifically, not swept under the rug just because the overall significance held.
  - **CKA cross-check surfaces a real RSA/CKA divergence worth flagging on its own, not resolving in either direction.** BirdNET's CKA correlations with the rest of the roster (0.24–0.66, e.g. 0.60 with `ast`, 0.63 with `panns_cnn14`, 0.61 with `musicfm`) are substantially *higher* than its RSA correlations (0.01–0.32) — CKA reads BirdNET as reasonably well-integrated with the roster, RSA reads it as close to an outlier. Per this project's standing rule (Davari et al. 2023 — CKA is gameable, never load-bearing alone), RSA is treated as primary and this entry doesn't try to adjudicate the disagreement — but it's a clean, concrete illustration of exactly why that rule exists, not just an abstract caveat restated from the literature.
  - TwoNN intrinsic dimension: `birdnet` = 17.9, second-highest of all 12 (behind only `music2vec`'s 21.5) — consistent with, not contradicted by, its RSA-isolation: a comparatively high-dimensional, idiosyncratic representation space.
- Updated `configs/models.yaml`-equivalent tracking for `birdnet` is intentionally **not** added there — it's not resolvable through the normal `audio_comp.models` registry/config path at all (see above), so it's tracked only in CLAUDE.md's Tier 1 table and this journal, not in `configs/models.yaml`.
- Not yet integrated into BirdCLEF/X-ARES (would need a subprocess bridge from the PyTorch-based `xares_eval` venv into the isolated `birdnet-venv` — real but bounded additional infrastructure, not attempted this session) or into the alignment/uniformity check (straightforward to add, just not done yet — same `.npz`-globbing pattern would pick it up with a one-line addition once wanted).

## 2026-08-11 — Stage 5 v1: matched LoRA fine-tuning on DeepShip, a real (negative) result

- User's direction after the roster-expansion work closed out: draft a write-up structure now while the four-finding story is fresh (`writeup.md`, committed — methods/results skeleton with real numbers throughout, `[OPEN]` markers where synthesis or a Stage 5 result is still needed, not placeholder prose), and start Stage 5 (OOD vessel-domain fine-tuning) in parallel. Per the write-up's own §5, the cheapest real Stage 5 preview is a small matched LoRA run on DeepShip's already-embedded 63-clip subset, correlating adapted accuracy against Finding 1 (breadth-cluster membership) — started there rather than a from-scratch OOD dataset acquisition, since ShipsEar/full-DeepShip are still access-gated.
- **Scoped Stage 5 v1 to 5 models, architecture-driven not arbitrary**: `wav2vec2`, `hubert`, `mert`, `music2vec` share identical attention submodule names (`q_proj`/`k_proj`/`v_proj`/`out_proj`, confirmed by directly inspecting `named_modules()`, not assumed from architecture family) and `ast` uses the same `q_proj`/`v_proj` names — a genuinely matched LoRA target across all 5. Checked the rest of the roster too before excluding them: `panns_cnn14` is a pure CNN with no attention layer for a standard LoRA target to apply to at all (architecture-level exclusion, not a naming inconvenience); `clap` (`query`/`value`), `bird_mae`/`audiomae` (fused `qkv`), `musicfm` (`linear_q`/`linear_v`), `audio_jepa` (flash_attn-shimmed custom module) each use a different convention — individually addable later but each needs its own verified target and its own smoke test, not assumed to work by analogy to the 5 that matched exactly.
- **Protocol** (`audio_comp/pipelines/stage5_lora_finetune.py`): LoRA rank=8, alpha=16, dropout=0.05, `target_modules=["q_proj","v_proj"]`, identical across all 5 models. Trainable: LoRA adapters + a fresh linear head (embed_dim -> 4 classes) on top of the same pooling convention used everywhere else in this project (`mean_pool`/`pooler_output`); frozen: everything else. Data: DeepShip's already-segmented 10s clips (`$SCRATCH/audio_comp/deepship_clips/`), 3-fold CV using the existing file-level fold assignment (`data/deepship_manifest.csv`) — train on 2 folds, eval on the held-out fold, repeat for all 3 rotations, report mean.
- **Infrastructure note**: hit a GPU OOM on the first smoke test (10GB MIG slice; wav2vec2-large + LoRA + fp32 activations for a 10s-clip batch of 4 doesn't fit) — bumped to the 20GB slice, matching this project's own established precedent for this exact class of issue (LibriSpeech-ASR's Qwen2 decoder needed the same bump back in the original X-ARES work). Smoke test then passed cleanly: 1 epoch, wav2vec2, fold 0, 51.1% accuracy (well above the 25% chance baseline).
- **Full result, all 5 models, full 10-epoch x 3-fold protocol** (`results/stage5_lora_deepship.csv`):

  | Model | Fold 0 | Fold 1 | Fold 2 | Mean |
  |---|---|---|---|---|
  | music2vec | 0.258 | 0.350 | 0.576 | 0.395 |
  | ast | 0.249 | 0.283 | 0.611 | 0.381 |
  | mert | 0.204 | 0.272 | 0.653 | 0.376 |
  | hubert | 0.330 | 0.317 | 0.389 | 0.345 |
  | wav2vec2 | 0.367 | 0.161 | 0.389 | 0.306 |

  Every model except `hubert` shows the same lopsided pattern: fold 2 far outperforms folds 0-1 (likely reflecting DeepShip's severe class imbalance interacting with which vessels/classes land in which fold, not a real per-model effect — worth flagging, not smoothed over).
- **The real finding: matched LoRA fine-tuning does not beat frozen-embedding linear probing on this task, and actively underperforms it in every comparable case.**

  | Model | Frozen MLP (earlier DeepShip run) | LoRA-adapted | Delta |
  |---|---|---|---|
  | mert | 0.494 | 0.376 | **-0.118** |
  | wav2vec2 | 0.415 | 0.306 | **-0.109** |
  | music2vec | 0.408 | 0.395 | -0.013 |
  | hubert | 0.364 | 0.345 | -0.019 |

  All four deltas negative. Spearman(frozen, adapted) across the 4 comparable models = **0.000** (p=1.000, n=4 — genuinely uninformative on its own, but consistent with the deltas: whatever ranking exists among these models under fine-tuning bears no relationship to their frozen-probe ranking). `ast` has no frozen DeepShip baseline to compare against (wasn't in the original 7-model DeepShip run) — noted, not filled in retroactively.
- **Corroborating signal, not a separate finding**: `wav2vec2`'s 1-epoch smoke-test accuracy (0.511, fold 0) was *higher* than its full 10-epoch fold-0 result (0.367) — more training made it worse on this exact same fold. Taken together with the frozen-vs-adapted comparison above, the coherent explanation is **overfitting on an extremely small fine-tuning set** (63 total clips, ~40 available for training per fold after the 2-fold split, one class — tug — with only 3 vessels total in the whole dataset) rather than any genuine cross-model difference in OOD adaptability.
- **Did not force the planned breadth-cluster correlation on top of this.** With only 5 models split across 3 breadth groups (2/2/1) and the adaptation signal itself dominated by overfitting noise rather than real cross-model variation, a formal permutation-test correlation here would be noise regressed on noise, not a real test of Finding 1's OOD-adaptability prediction — computing one anyway and reporting a p-value would manufacture false precision. Recorded as the honest reason this specific analysis is deferred, not silently skipped.
- **This is a real, reportable Stage 5 v1 result on its own** — matched lightweight adaptation at this data scale doesn't help, likely because there isn't enough OOD data yet to adapt into rather than overfit into. It sets a concrete, evidence-based bar for what Stage 5 v2 needs before the breadth/objective/alignment-vs-adaptability questions can actually be tested: either (a) fewer epochs / early stopping against a held-out validation slice, (b) stronger regularization (lower LoRA rank, higher dropout, weight decay), or (c) more OOD data — which loops back to the still-open ShipsEar access question and DeepShip's still-blocked full 265-vessel set, not a fine-tuning-recipe fix alone.

## 2026-08-12

- **Stage 2 Tier 2: added WavLM** (`audio_comp/models/wavlm.py`, `microsoft/wavlm-base-plus`) — another speech masked-modeling variant alongside hubert/wav2vec2, doesn't fill a new representational gap (Tier 2 by CLAUDE.md's own tiering), picked as the lowest-engineering-risk Tier 2 model to start with (native `transformers.AutoModel` support, same adapter shape as hubert.py). License verified directly against `microsoft/UniSpeech`'s root LICENSE (the repo WavLM's own HF card links to for licensing — **not** `microsoft/unilm`, the repo BEATs came from; not assumed to share BEATs' MIT license just because both are Microsoft speech projects, per the standing verify-against-primary-source rule): **CC BY-SA 3.0 Unported**, a license type not otherwise in this roster (not MIT like BEATs, not CC-BY-NC like mert/music2vec). No separate weights-specific license statement exists beyond the HF card's explicit link to this file — read as covering the checkpoint by the same absence-of-carve-out reasoning already used for BEATs; `checkpoint_status=official_open_weights`. Smoke-tested clean: dummy 1s clip, the standalone 60ms edge-case clip at batch size 1 (not just padded inside a longer batch, per the audio_jepa/panns_cnn14 lesson), and a mixed-length batch — all produced correctly-shaped `(N, 768)` embeddings. Registered in `configs/models.yaml`'s `active_models`. Not yet re-run against the full probe set / RSA-CKA sweep (12→13 models) — that's the natural next step, not done in this pass.
- Context for why Tier 2 (not finishing Tier 1's remaining `EncodecMAE` first): Digital Alliance cluster GPU queue is currently badly congested (fairshare priority dropped hard after this week's heavy GPU usage — see the confidential Stage 5 v2 work, not detailed here per the standing disclosure limit) — Tier 2 model-onboarding work doesn't need GPU-queue time the way a real training run does, so it's a reasonable thing to parallelize against the queue wait rather than sit idle. `EncodecMAE` remains the last open Tier 1 item.
- **Pushed roster from 12→18 active models (19 registered) in one batch**, per explicit instruction to prioritize breadth/count now, accepting `official_public_weights_license_unclear`-tier ambiguity rather than skipping a model over it (though as it happened, everything added this batch resolved to a clear, verified license, not just "close enough"). Added, each license-verified against its own primary HF card/repo and smoke-tested (dummy 1s clip, standalone 60ms edge case at batch=1, mixed-length batch — all correctly shaped): **Whisper** (`openai/whisper-base`, Apache-2.0, encoder-only via `WhisperModel.encoder()`, ASR/transcript-supervised — a different supervision signal than AST's label supervision, not the same axis), **Data2Vec-audio** (`facebook/data2vec-audio-base`, Apache-2.0 — real gap-filler, gives data2vec-family a speech-domain point alongside music2vec's music-domain one; had to specifically avoid the `-960h` ASR-fine-tuned sibling checkpoint), **MMS** (`facebook/mms-300m`, CC-BY-NC-4.0 — most extreme training-breadth data point in the roster, 1400+ languages, directly relevant to the standing breadth-hypothesis finding), **UniSpeech-SAT** (`microsoft/unispeech-sat-base`, CC BY-SA 3.0 Unported — same repo as WavLM, license not re-verified separately since it's the identical file), **SEW** (`asapp/sew-tiny-100k`, Apache-2.0 — efficiency-focused architecture, distinct from the plain-transformer family), **wav2vec2-Conformer** (`facebook/wav2vec2-conformer-rel-pos-large`, Apache-2.0 — same objective/data as plain wav2vec2, Conformer encoder instead of plain transformer, a second and more tightly-controlled architecture-axis test than the earlier PANNs CNN comparison).
- **Checked and explicitly deferred, not silently skipped**: SpeechT5 (base checkpoint lives under a non-official `ajyy/` namespace, not `microsoft/`; official `microsoft/` checkpoints found were all ASR-fine-tuned, excluded by this project's base-checkpoint-only policy) and EnCodec (`facebook/encodec_24khz`, MIT-verified, but `transformers`' `EncodecModel` public API only exposes discrete post-quantization codes, no documented path to the continuous pre-quantization latent this project's RDM pipeline needs — would require undocumented internals, not worth the fragility for this batch).
- **Real infra bug hit and fixed mid-batch**: `$HOME` quota (48GiB, well-known cluster constraint) hit 98% (47G/48G, 1.3GB free) and MMS's download failed with `Disk quota exceeded` — the interactive smoke-test commands weren't setting `HF_HOME`, so HF's cache defaulted into `$HOME` instead of `$SCRATCH` (the existing `stage5_lora_finetune.sbatch` already sets this correctly for batch jobs; the ad hoc interactive smoke tests this session didn't). Fixed by exporting `HF_HOME=$SCRATCH/hf_cache` for the rest of the session and deleting the now-redundant `~/.cache/huggingface` (4.8GB, fully re-downloadable) — recovered to 3.7GB free. Worth carrying `HF_HOME` forward into any future interactive/smoke-test work on this cluster, not just batch jobs.
- **Full RSA/CKA/TwoNN sweep re-run at 18 active models** (`compare_models.sbatch`, job 54519498, 1h28m, `def-spadon_cpu`) — extraction jobs for the 6 new models submitted first (`def-spadon_gpu`, all completed clean, 10,000/10,000 embeddings each, only the standard benign UNEXPECTED-key warnings). This is the formal Stage 3 re-run target CLAUDE.md has been building toward. **Headline new finding**: `data2vec_audio` vs `music2vec` — the first same-paradigm (data2vec-family, self-distillation/EMA-teacher), cross-domain (speech vs. music) pair this roster has ever had — shows a strikingly *low* RSA correlation (**0.086**), much weaker than same-domain-same-objective pairs elsewhere (hubert-wav2vec2 0.80). Sharpens the standing "objective and domain both shape geometry, paradigm label alone doesn't guarantee similarity" pattern (previously only seen via CKA divergence and narrow-domain-different-objective pairs like bird_mae-birdnet) with a cleaner, more direct paradigm-held-fixed/domain-varied test. MMS shows moderate correlations with the other speech models (0.37-0.52, strongest with unispeech_sat at 0.52) — plausible given shared wav2vec2 architecture but much broader (1400+ language) training distribution; **not yet formally re-tested against the breadth-hypothesis permutation framework at 18 models** — that's the natural next analytical step, not done in this pass (the 12-model version is what's currently written up in CLAUDE.md's Stage 1(b)). TwoNN intrinsic dimension recorded for all 18 (`results/intrinsic_dimension.csv`) — new models range from `sew` (10.43, lowest in the whole roster) to `wav2vec2_conformer` (18.54, second-highest after birdnet's 17.87) — not yet interpreted, logged for the record.
- **Real bug caught and fixed in the confidential Stage 5 v2 local-run script mid-session** (`data/AIS/train_vessel_classifier_streaming.py`, no confidential detail here per the standing disclosure limit): a laptop GPU OOM'd on a run that a synthetic single-batch dry run had predicted would fit comfortably. Root cause, two compounding factors: (1) a laptop's OS/display reserves real VRAM a headless dry-run test never sees (this specific card reports 6144 MiB total but only ~5730 MiB was actually free at idle — matches the "5.60 GiB total capacity" PyTorch's own OOM message reported almost exactly); (2) real multi-batch training epochs ran ~19% higher peak memory than one isolated forward+backward+step suggested (allocator fragmentation accumulating over many batches). Fixed with periodic `torch.cuda.empty_cache()` calls during training (every 20 batches + end of epoch), a more conservative memory cap (75% instead of 80%), and roughly halved local batch-size recommendations. Generalizable lesson for any future local-GPU memory-budgeting work in this project: a clean synthetic dry run measures a *floor*, not a safe operating point — build in real margin, don't just use the raw number.

## 2026-08-13

- **Started a brain-RSA side detour** (`brain_rsa/`, kept separate from the Phase 1/Stage roadmap per CLAUDE.md's scope rules): comparing our foundation models against human auditory-cortex fMRI data via Tuckute, Feather, Boebinger & McDermott (2023, PLoS Biology), using their `gretatuckute/auditory_brain_dnn` repo directly rather than reimplementing their RSA math. Cloned the repo, downloaded their `data/` bundle (~180MB: the 165-sound stimulus set at 16kHz + NH2015/B2021/NH2015comp neural datasets) via their own `setup_utils/download_files.py`.
- Wrote `brain_rsa/extract_activations.py` (+ `.sbatch`, one Slurm job per model, same GPU/venv/env-var pattern as the main `extract_embeddings.sbatch`) to pull each model's pooled final-layer embedding on the 165 stimuli, and `brain_rsa/run_rsa.py` which imports `run_correlation_on_feature_matrix`/`correlate_two_matrices_rsa` directly from the paper's own `rsa_matrix_calculation_all_models.py` rather than reimplementing the correlation-matrix/RSA math, to avoid a subtle methodology mismatch. Also computes the leave-one-out participant noise ceiling on NH2015 and reports noise-corrected RSA per model.
- **v1 scope, explicitly stated**: final layer only (not per-layer — the paper's actual headline result is about layer-stage correspondence, so this can't reproduce that yet), NH2015 only (not B2021), whole-brain (no ROI split), no train/test CV (no "best layer" to choose with only one embedding per model). Per-layer extraction via `output_hidden_states=True` is the natural next step once this quick pass produces a sane number.
- Submitted 5 Slurm extraction jobs (`wav2vec2`, `hubert`, `wavlm`, `whisper`, `ast`) — `wav2vec` and `AST` are both in the original paper's own external-model roster, useful as a rough sanity check even though checkpoints/adapters aren't identical to theirs. Jobs queued, not yet complete as of this entry.
- **Infra gotcha worth flagging for any session working with the laptop tunnel**: this cluster has multiple login nodes (`login1`, `login2`, `login3` confirmed so far, `login4` doesn't resolve) and a fresh SSH/Bash session can land on any of them — there's no guarantee it's the same one as last time. The laptop's reverse SSH tunnel (`ssh -N -R 127.0.0.1:<port>:localhost:22 ...`, see the confidential-work session's setup) only listens on whichever specific login node the tunnel command's outbound connection actually reached, not on all of them. Symptom hit today: a session checked `ss -tlnp` on its own current node, saw nothing on the tunnel's port, and concluded the tunnel had dropped — it hadn't; the tunnel was alive and well on a *different* login node than the one that session happened to be connected to. Confirmed by checking all three nodes directly (`ssh -o BatchMode=yes <node> 'ss -tlnp | grep <port>'`, works node-to-node without re-auth since `$HOME`/`authorized_keys` is shared across login nodes) — found the live tunnel on `login3` while the checking session was on `login1`. **Before concluding the tunnel is down, check all known login nodes, not just the current one** — a false "tunnel is down" diagnosis wastes the user's time re-establishing a connection that was never actually broken. Port `2222` shows up as LISTEN on every login node too, consistently — confirmed to be some other user's unrelated process on this shared, multi-tenant system, not ours (this is why the original tunnel setup moved off port 2222 to a randomized, loopback-bound port in the first place).
- **Brain-RSA v1 result (final layer, NH2015, whole-brain, 5 models)** — extraction ran on the user's laptop over a reverse-SSH tunnel rather than the (congested) cluster GPU queue, since 5 models x 165 short clips on CPU is trivially light compute; see the login-node/tunnel-auth entries above for the connectivity saga, including a real laptop reboot (critical battery, not caused by the inference load) mid-run that required resuming after the tunnel came back on a different login node. `h5py` unavailable in the venv (Compute Canada wheelhouse gap, only reachable via a conflicting module-extension system) forced inlining the paper's two RSA/correlation-matrix functions directly in `run_rsa.py` instead of importing their analysis script wholesale (which pulls in an unneeded `h5py`/seaborn/matplotlib chain via `utils.py`'s in-house-model loading path) — verbatim copy, not a reimplementation, so the math still matches the paper's.
  | model | RSA (spearman, mean±sem, n=8 participants) | noise-corrected |
  |---|---|---|
  | wavlm | 0.399 ± 0.015 | 0.526 |
  | hubert | 0.361 ± 0.018 | 0.475 |
  | whisper | 0.298 ± 0.022 | 0.393 |
  | wav2vec2 | 0.252 ± 0.018 | 0.332 |
  | ast | 0.160 ± 0.018 | 0.211 |

  Noise ceiling (leave-one-out participant reliability) = 0.759 ± 0.012. All 5 models land well below it, as expected for a **final-layer-only** comparison — the paper's own headline finding is that the *best* predicting layer is usually a middle layer, not the last one, so this ranking is not expected to reproduce their per-model results and shouldn't be read as a full replication. Speech-masked-modeling models (wavlm, hubert) predict NH2015 better than wav2vec2 here, and AST (supervised, AudioSet-trained, not speech-specific) is clearly weakest — plausible given NH2015 is a general-natural-sound (not speech-only) probe set and AST's training/pooling may be less suited to it, but not yet disentangled from architecture/pooling-convention differences between our adapters and the paper's own. Per-layer extraction (`output_hidden_states=True`) is the natural next step to actually test the paper's layer-stage-correspondence claim, not done in this pass.
- **Brain-RSA v2: per-layer, per-ROI extraction** (`brain_rsa/extract_activations_per_layer.py`, `brain_rsa/run_rsa_per_layer.py`), actually testing the paper's headline layer-stage-correspondence claim (middle/early layers -> primary auditory cortex, deeper layers -> non-primary) rather than v1's final-layer-only pass. Extraction re-ran on the laptop over the tunnel (same setup as v1, no incidents this time — tunnel stayed up on `login2` throughout). Standalone from `audio_comp/models/`'s adapter interface on purpose (every adapter's `embed()` contract elsewhere in this project is "one pooled vector per clip" — used by the main pipeline, X-ARES, Stage 5 LoRA — not worth changing project-wide for a 5-model, one-off detour); mirrors each adapter's own HF classes/preprocessing exactly with `output_hidden_states=True` added, so the numbers are directly comparable to v1's.
- ROI split (Primary/Lateral/Anterior/Posterior) via `df_roi_meta.pkl`'s `roi_label_general`, following `rsa_matrix_calculation_all_models.py`'s own `get_neural_data_matrix` approach directly (separate small reimplementation, not imported, same h5py/seaborn-avoidance reason as v1). Noise ceilings by ROI: AllROI 0.759, Primary 0.669, Lateral 0.723, Anterior 0.486, Posterior 0.445 (Anterior/Posterior noisier — smaller, less reliable ROIs, consistent with the original paper).
- **Result: qualitatively reproduces the paper's core claim.** Peak-RSA layer depth (layer index / (n_layers-1), 0=shallowest, 1=deepest) for Primary auditory cortex vs. the non-primary ROIs, averaged across all 5 models: **Primary 0.167, non-primary 0.408** — non-primary cortex peaks meaningfully deeper than primary, matching the paper's stage-correspondence finding, on foundation models the paper never tested (wavlm, whisper weren't in their original roster; wav2vec2/hubert/ast used different specific checkpoints than theirs). Per-model breakdown: 4/5 models (hubert, wav2vec2, wavlm, whisper) show a clear primary-shallower/non-primary-deeper split (e.g. wav2vec2: layer 0/25 for Primary vs. layer ~12/25 average for non-primary; hubert: layer 2/25 vs. ~11/25). **AST is the one exception** — its peak layer is roughly the same mid-depth (fraction ~0.42-0.50) across every ROI including Primary, showing no differentiation. Plausible reading, not yet confirmed: AST's supervised AudioSet-classification training may organize its depth axis around task-relevant abstraction rather than anything resembling an auditory processing hierarchy, unlike the self-supervised/ASR-supervised models — consistent with, though not proof of, this project's broader standing finding (Stage 1/4) that objective/training signal shapes representational geometry independent of paradigm labels.
- **Best overall AllROI predictor across all layers: wavlm** (RSA 0.473, noise-corrected 0.624, at layer 3/13 — an early-ish layer, not the earlier final-layer-only run's implicit assumption that later=better), consistent with v1's final-layer finding that wavlm was the strongest final-layer predictor too, but v2 shows its actual peak is well before the final layer (layer 3 of 13, not layer 12).
- Full per-model/per-layer/per-ROI table in `brain_rsa/results_per_layer_nh2015.csv` (415 rows: 5 models x ~16.6 layers avg x 5 ROI conditions).
- **Brain-RSA v3: per-category plots, at the user's explicit correction** — averaging RSA across all 165 sounds (v1/v2) was flagged as not meaningful here, since the stimulus set spans genuinely different "tasks" (speech, music, baby crying, mechanical sounds, animal calls...) that a single pooled number blends together. Re-ran per-layer RSA separately for each of the 11 `cat_assignment` semantic categories in `neural_stim_meta.npy` (Mechanical 39, EnvSound 27, Music 24, HumNonVoc 15, HumVoc 13, Song 11, EngSpeech 10, AniVoc 10, ForSpeech 7, AniNonVoc 5, Nature 4 — counts sum to 165), whole-brain only (NH2015 n_reps==3, not the ROI split — crossing category x ROI would fragment already-small subsets further). `brain_rsa/run_rsa_per_layer_by_category.py`, plot at `brain_rsa/plots/rsa_by_layer_per_category.png` (small-multiples, one subplot per category, x=layer depth %, y=RSA, one line per model, dataviz skill's validated 5-slot categorical palette), full table `brain_rsa/results_per_layer_by_category.csv` (913 rows).
- **This directly confirms averaging was hiding real, sometimes opposite, per-category trends**: wavlm and ast climb steadily with depth on **Music** (peaking 60-100% depth), but wavlm *peaks early* (~10%) then declines on **Mechanical** (the largest category, n=39) — opposite-direction curves that a pooled-average number would blend into a misleadingly flat "peaks early" summary. **EngSpeech** shows ast spiking sharply at ~30-40% depth (RSA~0.57), a much sharper speech-specific signature than ast shows in any other category or in the pooled v1/v2 results, despite ast not being a speech-domain model. Smallest categories (ForSpeech n=7, AniNonVoc n=5, Nature n=4) are visibly noisy, sometimes RSA<0 — flagged in the plot and here as exploratory only, not a stable per-category result at that sample size, not hidden or averaged away.
- **Brain-RSA: added B2021 support** (`run_rsa_per_layer_by_category.py --target B2021`) alongside NH2015 — confirmed B2021's 192 stimuli match NH2015's 165 in identical order (checked directly against `stim_info_v4.mat`, same assertion `utils.py`'s own `get_target()` makes). B2021's `voxel_features_meta.npy` has no `subj_idx`/`n_reps` fields (unlike NH2015) — participant grouping comes from `df_roi_meta.pkl` instead (covers all 26,792 voxels, not just ROI-labeled ones; no reps-filtering needed since B2021 uses a fixed 3 reps per voxel). Per-category line plot written to `plots/rsa_by_layer_per_category_b2021.png`, table to `results_per_layer_by_category_b2021.csv` — not yet compared against the NH2015 version's per-category patterns, that comparison is a natural next step.
- **New plot: per-category model x model x brain intercorrelation heatmap** (`plot_rsa_intercorrelation_by_category.py`, NH2015 only so far), extending the earlier model-vs-brain-only heatmap to also show model-vs-model RSA, with each model shown at its own peak (brain-best) layer depth for that category (folds the previously-dropped depth dimension back in via axis tick labels rather than a separate encoding). Shared color scale across all 11 category panels (not per-panel-normalized) so panels are visually comparable. Headline pattern: **models agree with each other far more than any agrees with the brain** (model-vs-model cells mostly 0.6-0.96, brain row/column mostly 0.03-0.66) — expected given shared architecture/training data among the 5 models vs. actual biological cortex. Human NonVocal and Animal NonVocal show wav2vec2/hubert/wavlm nearly redundant with each other (0.90-0.96), all three peaking at layer depth 0% for those categories. Foreign Speech (n=7, already flagged as small/noisy) is the standout bad panel: wavlm-whisper intercorrelation is *negative* (-0.29), brain-wav2vec2/whisper near zero — no real signal, not hidden or smoothed over. AST's peak-matching depth swings much more across categories (8% to 100%) than the other four models, which stay comparatively low and stable (0-33% for most categories) -- worth a closer look if AST's per-category behavior becomes analytically important.
- **New plot: model-vs-model RSA at depth quartiles, per category** (`plot_model_intercorrelation_by_depth_quartile_per_category.py`), crossing the two previous depth-quartile (`plot_model_intercorrelation_by_depth_quartile.py`) and per-category (`plot_rsa_intercorrelation_by_category.py`) analyses. One PNG per category (`plots/depth_quartile_by_category/<cat>.png`, 11 files) rather than one 44-panel combined grid, which would have made every cell unreadable. Layer-to-quartile assignment (nearest layer to each 25%-quartile's midpoint) is fixed per model, independent of category — only the stimulus subset (and thus the RDMs/RSA) varies per category.
- **Finding: the whole-dataset depth-quartile result (AST decoupling from the other 4 models with depth, down to near-zero by 75-100%) is itself category-dependent, not a fixed model property.** Mechanical (n=39, the largest category) shows this decoupling sharply and monotonically (AST-hubert RSA: 0.57 at 0-25% depth -> -0.01 at 75-100%). Instr. Music (n=24) shows the same directional pattern but much weaker and non-monotonic (0.46 -> 0.29 -> 0.41). The other four models (wav2vec2/hubert/wavlm/whisper) stay tightly mutually correlated (0.4-0.93) across all depths and both categories — it's specifically AST's relationship to the others that's category-sensitive, not a general property of "deep layers agree less." Full per-category breakdown not yet summarized across all 11 files — worth a follow-up pass if this becomes analytically important.

---

*(Separate session, main audio_comp roster/Stage 5 work, not brain_rsa — logging below.)*

- **New plot: `results/rsa_by_category.png`** (`audio_comp/pipelines/rsa_by_category.py`, run via a one-off `sbatch --wrap` on `def-spadon_cpu`, ~7min) — model-vs-model RSA per probe-set category (bird_sounds/city_noise/music/ship_vessel/speech, 2000 clips each), 18×18 per category, final-layer embeddings only (no depth axis — this project doesn't extract per-layer hidden states the way brain_rsa does). v1 had a real layout bug (bottom-row subplot titles overlapping the row above's tick labels) and used the full -1..1 color scale despite actual data only spanning ~0.19-0.32 mean off-diagonal, making everything look uniformly teal — fixed both (shared color scale computed from actual observed range across all categories, more `hspace`/`wspace`). Visible patterns, not yet formally tested: `data2vec_audio` reads as an isolated dark row/column in nearly every category (consistent with its 0.086 RSA against music2vec, see the earlier 18-model entry above); `audiomae` shows the same isolation specifically in `speech`; a warm-colored cluster (hubert/mert/mms/musicfm/panns_cnn14/sew/unispeech_sat) shows up together in music/city_noise/ship_vessel.
- **User caught a real, previously-unverified data-quality gap in the `ship_vessel` probe-set category, prompted by comparing it against the confidential Stage 5 v2 data's richness (distance, etc.)** — asked directly whether all `ship_vessel` clips actually contain vessel noise, given DS3500's 5s clips seemed "nitpicked" next to the real data's variables. Checked `peng7554/DS3500`'s actual HF dataset card rather than answer from the existing `ds3500.py` docstring's summary (which just says "ShipsEar-derived," no composition detail) — turned up a real issue that had been sitting unverified since Phase 1 kickoff: **~9.6% of DS3500 is "Class E, environmental noise"** (no vessel at all), and the whole dataset traces back to only **1,948 real ShipsEar recordings** (Spain's Atlantic coast, 2012-2013) expanded via **synthetic ray-theory deep-sea channel simulation** — its distance-to-vessel metadata (1-11km) is a simulated parameter, not measured. Logged as a standing limitation in `CLAUDE.md`'s Known Risks section (own subsection now, not folded into the generic probe-set-sensitivity bullet) since it directly affects how `ship_vessel`-category RSA numbers should be read, and directly strengthens (rather than weakens) the rationale for the confidential Stage 5 data: DS3500 is a fixed, single-region, partly-simulated academic benchmark, not a substitute for real AIS-verified contacts with genuinely measured distance from the actual target deployment environment. Should have been checked at Phase 1 kickoff, not four days into Stage 5 — a reminder that "verify against primary source" (CLAUDE.md's standing rule, previously applied mainly to model checkpoint/license claims) applies to dataset composition claims just as much.
- **Built a bare-minimum, explicitly non-research-grade ShipsEar classification task** (`xares_eval/shipsear/`, `xares_eval/tasks_scratch/shipsear_task.py`), per the user's direct ask ("we cant blindly compare representations without understanding if the model is correct") — 3-way vessel-type classification (A/B/D, real recordings only) with proper leave-one-session-out folds (10 sessions across the 3 classes; classes C and E excluded, each being a single unbroken recording session with no valid train/test split possible, see the ds3500/ShipsEar entry above). Ran against the 11 model encoders already wired up for X-ARES (not yet extended to the 6 newest roster additions). Results in `results/xares_results.csv`. All models land comfortably above the ~33% chance floor (0.39-0.49 MLP) except **wav2vec2 at 0.127 — genuinely below chance**, investigated rather than reported at face value: directly tested the `Wav2Vec2XaresEncoder`/`HubertXaresEncoder` wrappers on identical synthetic input — both produce healthy, non-degenerate features (no NaN, no all-zero, similar mean/std), ruling out a code bug or broken checkpoint. The real difference: wav2vec2's raw feature dynamic range is notably wider (min=-3.12 vs hubert's -1.44 on the same input, despite near-identical std) — plausibly reflecting wav2vec2-large-lv60 being pushed further out-of-domain by real underwater ship acoustics than hubert. Per-fold accuracy confirms this isn't a generic small-sample-size problem (hubert, same architecture family, same folds, same sample counts, has *zero* exact-0.0 folds in its MLP results; wav2vec2 has four) — it's specific to wav2vec2's feature distribution destabilizing X-ARES's downstream MLP probe on this small, imbalanced dataset. **Read the 0.127 as "the probe failed to train stably for wav2vec2 here," not "wav2vec2's ship-sound representation is uniquely bad"** — a real distinction, not explained away. Not yet root-caused further (e.g. whether feature normalization before the probe would fix it) — flagged as a real open question, not resolved.
- Confidential Stage 5 v2: handed the user updated run commands for the remaining 4 models (hubert, mert, music2vec, wav2vec2) on the real AIS data, now with both the memory fix and the new thermal-safety pacing (see below) applied. Not yet run as of this entry.
- **Real hardware incident: laptop hard-shutdown during a local run** (fan spun up hard, then forced power-off, confirmed AC-powered so not a battery/power-delivery issue -- a genuine thermal shutdown). The earlier memory-only fixes (GPU_MEM_FRACTION cap, periodic empty_cache()) did nothing for this, since sustained compute load and thermal load are different problems -- a "Max-Q" mobile GPU has real, tight thermal headroom under sustained training that a desktop or full-power laptop GPU wouldn't hit the same way. Added active temperature monitoring (`get_gpu_temp_c()`/`thermal_check_and_throttle()` in `train_vessel_classifier_streaming.py`, checked every 5 batches): paces with short sleeps once the GPU crosses 78C, and fully pauses (polling until back under 75C) if it crosses 85C -- a controlled, gentle slowdown well before the hardware's own emergency cutoff, rather than relying on that repeatedly (bad for hardware longevity, not just an inconvenience). These thresholds are conservative consumer-GPU defaults, not measured against this specific machine -- override via `AUDIO_COMP_GPU_TEMP_WARN_C`/`_COOLDOWN_C`/`_RESUME_C` if they turn out wrong in practice. Also flagged to the user: software pacing alone can't fix genuinely inadequate physical cooling (blocked vents, no airflow) -- if the pacing/cooldown messages fire constantly, that's a signal to check the physical setup, not just retune constants.
- **Thermal thresholds tightened, then loosened again, same day, tracking real observed behavior.** First tightening (65/78/60C) still cycled continuously on a real run (climbed straight back to 86C within moments of every resume) -- added consecutive-cycle escalation (3 cooldowns in a row forces a 90s rest instead of repeating 5s ones). Even the lightest workload in the project (frozen-embedding extraction, no backward pass at all) still triggered continuous cycling at these tighter thresholds -- strong evidence the laptop's physical cooling, not workload intensity, is the real bottleneck right now. User explicitly asked to loosen the caps for speed after hearing that tradeoff; declined to remove the check entirely (repeats the exact failure mode already observed once, doesn't fix the underlying problem) but raised thresholds significantly (88/93/85C) at their informed request -- still a real stop before the ~100-105C range these GPUs typically hard-shut down at, just a much higher bar. User separately clarified "workers" meant `--batch-size`, not an actual DataLoader worker setting (neither script uses multiprocessing) -- no code change needed, just a terminology mismatch.
- **Attempted the confidential-data LoRA run again via a fresh cluster `salloc` (2g.20gb slice) instead of the laptop** -- hit a genuinely new problem: the job burned its entire 3-hour allocation and only reached 800/2463 clips during prefetch (never got to training at all), roughly 180x slower than prefetch has run before on this exact code path. Tested basic TCP connectivity to bigdata6 from a fresh compute node (`fc30669`, different from the slow run's `fc11020`) -- fast (~0.1s/connect), so not a categorical "compute nodes can't reach bigdata6" issue. Couldn't test actual SFTP throughput without the password, so root cause (bad specific node vs. a general SFTP/protocol-level overhead vs. transient congestion) is still unresolved -- recommended the user try a fresh `salloc` (likely different physical node) and abort quickly if still slow, rather than risk losing another 3-hour window to the same problem blind.
- **User shared a real, reusable password (their CSID) for a second, shared, multi-user lab GPU server (`tca-s01.research.cs.dal.ca`) in chat**, asking the assistant to use it directly. Flagged before acting: this is a different class of credential than the MyShipTracking API key (broader/reusable, and the target is shared infrastructure where the person who books a slot can kill other people's running jobs -- real consequences for other people, not just this user's own hardware) -- recommended the user drive booking/login themselves, same pattern as bigdata6. User confirmed the credential "can only use it for this thing" and explicitly asked the assistant to proceed anyway; complied (informed, explicit override, scoped credential) but the actual login attempt via paramiko failed with `AuthenticationException` (network reachable, password rejected) -- did not retry blindly to avoid risking an account lockout on repeated failed logins; asked the user to double check for a typo or a possible extra auth step. Not yet resolved, user is sorting it out separately.
- **Stage 4 X-ARES coverage gap-fill, all public data (no confidentiality boundary), run directly by the assistant via sbatch**: built the 7 missing X-ARES encoder wrappers (`wavlm`, `whisper`, `data2vec_audio`, `mms`, `unispeech_sat`, `sew`, `wav2vec2_conformer` -- same `unpooled last_hidden_state` pattern as the existing 11, except whisper which uses `WhisperFeatureExtractor`'s `input_features` key and calls `.encoder(...)` directly, matching `audio_comp/models/whisper.py`'s own never-touch-the-decoder policy). Confirmed real, uneven prior coverage before submitting anything: FMA-genre/UrbanSound8K/LibriSpeech-100h only had 7 of 18 models run (missing `ast`/`audiomae`/`bird_mae`/`panns_cnn14` in addition to all 7 new models), BirdCLEF had 11/18 (missing only the 7 new models). Submitted 18 jobs total (11 models x 3-task combined run, 7 models x BirdCLEF) -- results pending as of this entry.
- **Breadth-hypothesis re-run at 18 models -- a genuine reversal, reported honestly rather than smoothed into the prior trend.** Extended both `DOMAIN_PARADIGM_GROUPS` and `BREADTH_GROUPS` in `breadth_hypothesis_check.py` for the 7 new models (`data2vec_audio` gets its own `data2vec_speech` bucket, sibling to music2vec's `data2vec_music`; `wavlm`/`unispeech_sat`/`sew`/`wav2vec2_conformer` join the existing `masked_modeling_speech`/`narrow_speech` buckets since all four train on the same 960h LibriSpeech corpus as plain wav2vec2/hubert; `mms`/`whisper` get a new `broad_speech` breadth bucket -- domain-narrow but extremely distribution-broad within that domain, a real axis the existing narrow/broad-mixed split didn't capture). Result: **domain+paradigm's gap (0.2394) is now larger than breadth's (0.2043)** -- every prior model count (9/11/12) had breadth's gap leading or comparable; this is a real reversal, not noise (both remain highly significant: domain+paradigm p=0.0006, breadth p=0.0001). Breadth still covers more model-pairs in real within-group comparisons (28 vs 16). Most likely explanation: most of the 7 new additions are narrow-corpus speech models that land in essentially the same cluster under both framings, diluting the specific disagreement cases where breadth used to win. Per the project's own standing gate, **breadth can no longer be reported as the outright winner** -- both framings should be reported together going forward with this reversal stated explicitly. Full reasoning logged in `CLAUDE.md`'s Stage 1(b) entry, not just here.
- **Sync note**: found substantial brain_rsa work already done by a separate concurrent session beyond what this session had built — `extract_activations_per_layer.py`, per-ROI/per-layer RSA (`run_rsa_per_layer.py`, `results_per_layer_nh2015.csv`), per-category breakdown (`run_rsa_per_layer_by_category.py`, `results_per_layer_by_category.csv`, `results_per_layer_by_category_b2021.csv`), several plots (`plot_rsa_confusion_matrix.py`, `plot_rsa_intercorrelation_by_category.py`, `plot_model_intercorrelation_by_depth_quartile*.py`), and the v1 final-layer run actually completed (`results_final_layer_nh2015.csv`: wavlm 0.399, hubert 0.361, whisper 0.298, wav2vec2 0.252, ast 0.160, all noise-corrected against an 0.759 leave-one-out ceiling). Not logged by that session yet — recording here so it's on the record regardless of which session gets to it first.
- **Received and verified a methodology critique via cross-session message from a peer session** (relaying a critique attributed to the user): the 5-model roster (wav2vec2/hubert/wavlm masked+speech, whisper ASR-supervised+speech, ast supervised+general-domain) confounds training objective and training domain for `ast` specifically — same structural issue as the main pipeline's earlier PANNs/Bird-MAE confound before AST itself disambiguated it there. Rather than act on the claim blind, checked it against the already-computed `results_per_layer_nh2015.csv` directly: **confirmed real**. AST's peak layer_frac is nearly identical for Primary (0.50) and Lateral (0.42) cortex — it doesn't show the depth-differentiation pattern every other model shows (wavlm: 0.08 Primary vs. 0.25 Lateral; hubert: 0.08 vs. 0.88; wav2vec2: 0.00 vs. 0.33). Small-category counts cited in the critique (AniNonVoc n=5, Nature n=4, ForSpeech n=7) also checked out exactly against `results_per_layer_by_category.csv`. Treating the critique as legitimate and grounded, not fabricated — worth noting as a real instance of the standing "verify before propagating, even from a peer/past-session claim" rule extending to cross-session messages, not just files/instructions.
- **Extended `extract_activations_per_layer.py` with `mert` (masked-modeling, music domain) and `clap` (contrastive, domain-general)** to disambiguate: mert fills the previously-empty masked-modeling x non-speech-domain cell of the implied 2x2; clap adds a second discriminative-objective, non-speech-domain data point alongside ast. Both smoke-tested on a single clip before the full run (mert: 25 layers x 1024-d via `AutoModel(trust_remote_code=True)`, clean `output_hidden_states` support; clap: 5 Swin-stage feature maps, `(C,H,W)` not `(seq,D)` — needed spatial mean-pooling instead of sequence mean-pooling, and CLAP's `audio_model` submodule specifically, not the full CLAP model, to get hidden states at all). Both need resampling (16kHz stimuli -> mert's 24kHz, clap's 48kHz) that the original 5-model script didn't need (all 5 were natively 16kHz). `musicfm` (also masked+music, would replicate mert's cell rather than fill a new one) deferred — non-`transformers` architecture, needs a custom per-layer loop via its `get_latent(layer_ix=...)` API rather than a single `output_hidden_states=True` call, real but bounded extra cost, not done in this pass. Submitted as a Slurm GPU job (`brain_rsa_perlayer_mert_clap`, job 54694979), queued as of this entry.

---

*(Main audio_comp roster/Stage 5 session, not brain_rsa — continuing below.)*

- **EncodecMAE wired up + smoke-tested** (`audio_comp/models/encodecmae.py`, `scripts/setup_encodecmae.sh`) — the last unstarted Tier 1 roster item. `pip install -e` from `github.com/habla-liaa/encodecmae` (MIT), model name `ec-ec-large_st` (verified by deliberately catching `load_model()`'s own error message listing every valid name, since the HF repo's own usage snippet says `large-st`, which doesn't exist as a valid name — a real instance of not trusting a secondary usage example over the actual code). No dependency conflicts (`encodec==0.1.1` and `transformers.EncodecModel` coexist fine, different namespaces). Caught a real crash during the mandatory standalone-60ms-clip smoke test: `extract_features_from_array()`'s internal chunking loop only processes windows over `min_length=2048` samples, and 60ms at the model's native 24kHz is only 1,440 samples — zero chunks produced, `acts[0]` on an empty list. Fixed by zero-padding short clips before calling the model. Same category of bug as the audio_jepa/panns_cnn14 short-clip lessons, caught by the same checklist item those lessons put in place — the discipline is working as intended, not just a paper rule.
- **BEATs loader written** (`audio_comp/models/beats.py`, `scripts/setup_beats.sh`) — vendors 3 files from `microsoft/unilm/beats` (`BEATs.py`/`backbone.py`/`modules.py` only; confirmed `Tokenizers.py`/`quantizer.py` are tokenizer-training-only, not needed for inference, by reading `BEATs.py`'s own imports directly). No fairseq/hydra dependency, unlike audio_jepa's vendored code -- `modules.py` is fully self-contained (torch only). Checkpoint identity double-checked against the README's table structure (Tokenizer | Pre-Trained Model | AudioSet Fine-Tuned Model 1 | AudioSet Fine-Tuned Model 2 columns) specifically because "BEATs_iter3+ (AS2M)" *looked* like it could be an AudioSet-label-fine-tuned checkpoint from the name alone -- it isn't, it's that iteration row's own Pre-Trained Model column entry, confirmed by fetching the table structure a second time after the first fetch's summary was ambiguous on this exact point. **Blocked on one real, unavoidable manual step**: BEATs' checkpoint host is a OneDrive personal-share link, confirmed via direct `curl -IL` to return HTTP 403 after the redirect chain -- no programmatic download path exists (every other model in this roster has been fetchable by a script; this is the first exception). Adapter code and vendored source are both ready; waiting on the user to download the checkpoint via browser and drop it at the documented path.
- **New OOD domain selected and setup started: MIMII** (industrial machine condition sounds -- valve/pump/fan/slide-rail, normal vs. anomalous), per explicit user request for a 6th domain none of the 19 models have ever been trained on. Chosen over two other verified candidates (InsectSet459 -- 26,399 clips, CC-BY-4.0/CC0, but 8-500kHz sample-rate range makes it messier to standardize; ICBHI 2017 respiratory sounds -- 6,898 cycles, license is a vaguer "freely available for research" rather than a named license, would need the same care as bird_mae's unclear-license case) specifically because its format is already uniform (16kHz, 10s clips -- same shape as `DS3500`/`UrbanSound8K` already in the probe set) and its license is unambiguous (CC-BY-SA 4.0, confirmed directly via the Zenodo API, not just the record's landing page). 18,019 total clips across 3 SNR tiers x 4 machine types (100GB combined) -- downloading only the cleanest tier (6dB SNR, ~32GB across all 4 machine types) since the probe-set sample only needs ~2,000-3,000 clips, not the full noisy-tier sweep. Download+extraction submitted as a background Slurm job, in progress as of this entry -- source loader, probe-set sampling, and a ShipsEar-style classification task still to be built once the raw data lands.
- **Filled remaining X-ARES coverage gaps for `ast` and `whisper`** (both had timed out at 6h on their first attempt, mid-way through `urbansound8k`/`librispeech` respectively -- resubmitted at 4h, both completed this time). All 18 active models now have complete coverage across all 4 public tasks (FMA-genre, UrbanSound8K, LibriSpeech-100h, BirdCLEF) for the first time.
- **Re-ran the parameter-count/accuracy/RSA scaling analysis with clean, complete data** (previously `ast`/`whisper` only had partial BirdCLEF-only accuracy averaged in). Both checks stay non-significant: log(params) vs. accuracy rho=-0.100 (p=0.69, down from the earlier incomplete-data 0.184), discriminative-objective vs. accuracy residuals rho=0.187 (p=0.46). Excluding `wav2vec2`'s known probe-instability anomaly doesn't change either result meaningfully (rho=0.013, p=0.96 for the params-accuracy check). **Genuinely inconclusive at n=18** -- doesn't confirm the "discriminative training wins independent of scale" reading from Stage 4, doesn't refute it either. One striking visual worth a closer look later, though not moving the aggregate correlation: `whisper` (smallest model in the roster, 20.6M encoder-only params) scores among the highest accuracy (0.57), well above every model near its size. RSA-vs-params (direct test of Huh et al. 2024's scale-convergence claim) stays flat (rho=-0.100, p=0.70) -- a real, citable null result for a claim this project has cited throughout but never tested directly before this pass.
- **Alignment/uniformity re-run at n=17** (`alignment_uniformity_check.py`, unmodified -- just fed the now-complete X-ARES coverage), resolving the earlier "genuinely mixed" n=7/n=9 finding logged 2026-08-10. Both scopes now agree uniformity predicts KNN better than MLP: BirdCLEF -0.679/p=0.003 (KNN) vs. -0.583/p=0.014 (MLP); FMA+UrbanSound8K -0.679/p=0.003 (KNN) vs. -0.471/p=0.057 (MLP, no longer significant at the conventional threshold, unlike the earlier n=7 result). Read as the earlier mixed result being underpowered (n=7/n=9), not a real reversal -- more data resolved it in the mechanistically-predicted direction rather than complicating it further.
- **MIMII download hit the same login-node-vs-compute-node bandwidth throttle already seen with `bigdata6` streaming.** A Slurm CPU job downloading the 4 zip files (~32GB, 6dB SNR tier only) was still on file 1 of 4 after 2.5 hours (~400KB/s). Diagnosed with a 15-second `curl` speed test run directly from the login node (~2MB/s, ~5x faster) rather than guessing -- confirmed the same asymmetry pattern, not specific to bigdata6. Fixed by killing the Slurm job (`scancel 54850124`), deleting the partial download, and re-running the download directly on the login node via `nohup ... & disown`, explicitly watched for the first ~150 seconds to confirm it survived the known ~2.5-minute login-node reaper window (a plain I/O-bound `curl` is lower-risk than the CPU/memory-heavy processes that trigger the reaper -- reasoning held, it survived). Completed in well under an hour once off the compute node.
- **MIMII probe-set category (`machine_sounds`) and classification task built end-to-end**, all via incremental patterns to avoid re-touching the existing 10,000-clip/5-category probe set: new source loader (`audio_comp/data/sources/mimii.py`, walks `<machine>/id_XX/{normal,abnormal}/*.wav` for valve/pump/fan/slider, deterministic shuffle), one new `configs/categories.yaml` entry, 2,000 rows appended to `data/probe_set_manifest.csv` (now 12,000 rows / 6 categories) via a scoped temp-config build rather than a full manifest rebuild, and `extract_embeddings_incremental.py` (new pipeline script) to add `machine_sounds` embeddings to all 18 models' existing `.npz` files without re-extracting the other 5 categories. Classification task built ShipsEar-task-style: `xares_eval/mimii/build_manifest.py` (16 leave-one-physical-unit-out folds, one per machine-type x id combination, not clip-level -- 18,019 total clips, 14,719 normal / 3,300 abnormal, confirmed to match documented dataset totals exactly), `make_audio_tar.py`, `xares_eval/tasks_scratch/mimii_task.py` (`private=True`, binary anomaly label, `eval_weight=18019`). Class imbalance (~4.5:1 normal:abnormal) stated explicitly in the task docstring so accuracy numbers get read correctly.
- **A separate, unrelated gap found while extracting MIMII embeddings**: `wavlm.npz` was missing from `/scratch/pdoshi/audio_comp/embeddings/` despite `wavlm` being part of the already-documented 18/19-model RSA comparisons -- root cause not investigated (not clearly tied to anything done this session), fixed pragmatically by re-running full `wavlm` extraction (job 54869990, 20 min, succeeded). Worth a closer look later if it recurs for another model, since an unexplained missing artifact that happened to get caught here could just as easily have gone unnoticed for a model nobody re-touched recently.
- **Real infrastructure incident: `$HOME` filled to 100% of its 48GB quota**, discovered when `mert`'s MIMII classification job failed on `OSError: Disk quota exceeded` while re-downloading its checkpoint (not a code bug -- HF's own download path). Root cause, found by `du`-ing `$HOME`'s largest entries: `~/audio_comp_data` (24G, raw/probe_set) and `~/audio_comp_external` (4.1G, vendored model checkpoints for audiomae/musicfm/panns/DeepShip/etc.) were both silently living in `$HOME` this entire project, not `$SCRATCH` -- `AUDIO_COMP_DATA`/`AUDIO_COMP_EXTERNAL` default to `~/audio_comp_data`/`~/audio_comp_external` in code and were never overridden in `.bashrc`, directly contradicting this project's own stated convention (`$HOME` = code/venv only, `$SCRATCH` = data/checkpoints/embeddings, see the original kickoff plan) -- a pre-existing misconfiguration from Phase 1, not introduced this session, just never hit the quota ceiling until now. `~/.cache/huggingface` (4.8G) added to the same problem. **Fixed**: rsync'd both directories (verified byte-identical size match before deleting anything) plus the HF cache to `/scratch/pdoshi/audio_comp/home_migrated/` and `/scratch/pdoshi/audio_comp/hf_cache/`, set `AUDIO_COMP_DATA`/`AUDIO_COMP_EXTERNAL`/`HF_HOME` permanently in `~/.bashrc` pointing at the new `$SCRATCH` paths (so this can't recur), then deleted the original `$HOME` copies only after explicit user confirmation (28GB reclaimed, `$HOME` now 32% full). This was very likely the same root cause behind at least one of the earlier-attributed "transient GPU driver error" MIMII job failures this session, though not re-diagnosed retroactively -- worth keeping in mind if a similar-looking failure recurs.
- **MIMII X-ARES classification jobs hit a transient, cluster-side (not code) GPU driver failure on a meaningful fraction of nodes**: `"Failed to get device handle for GPU 0: Unknown Error"` -> `torch.cuda.device_count()` returns 0 -> `ZeroDivisionError: integer modulo by zero` in `xares/run.py`'s `i % num_gpus`. Confirmed non-systemic (not a bug in this project's code) by directly comparing failed job logs against the 2 jobs that landed on healthy nodes in the same submission batch and succeeded outright (`bird_mae`, `clap`). Round 1: 12 of 18 jobs failed this way, all resubmitted. Round 2: the resubmitted 12 succeeded, but 2 new failures appeared -- `ast`/`whisper` timed out at 3h (18,019 clips is a bigger task than any prior public dataset run against), and 3 hit the *identical* GPU driver error again on re-submission (`audio_jepa`, `mert`, `music2vec` -- bad luck of node assignment, not a per-model issue, since none of these three failed the same way in round 1). Round 3 (submitted this entry): `audio_jepa`/`mert`/`music2vec` at 3h, `ast`/`whisper` at 5h -- landed on healthy nodes and running/queued cleanly, not yet complete as of this entry. **13 of 18 models now have valid MIMII classification results**: bird_mae 0.785/0.684 (MLP/KNN), clap 0.730/0.731, data2vec_audio 0.826/0.825, hubert 0.836/0.620, musicfm 0.816/0.673, wav2vec2 0.824/0.725, audiomae 0.754/0.664, mms 0.846/0.792, panns_cnn14 0.812/0.672, sew 0.845/0.777, unispeech_sat 0.815/0.730, wav2vec2_conformer 0.788/0.703, wavlm 0.822/0.570.
- **Alignment/uniformity check re-run at n=17 (up from 7-9), and the earlier mixed result is resolved, not just re-confirmed with more data.** Ran `alignment_uniformity_check.py` unmodified -- it already filters to models with complete task coverage, so filling the X-ARES gaps above let it pick up 17 models automatically. Both scopes now agree that uniformity predicts KNN more strongly than MLP (BirdCLEF: -0.679/p=0.003 vs -0.583/p=0.014; FMA+UrbanSound8K: -0.679/p=0.003 vs -0.471/p=0.057) -- the earlier *backwards* FMA+UrbanSound8K result (n=7) is gone, reading as an underpowered fluke rather than a real scope disagreement. Logged in CLAUDE.md's Stage 6 section as the strongest, most consistent signal yet for the KNN-specific uniformity hypothesis.
- **New per-category plot** (`plot_accuracy_vs_rsa_size.py`) replacing a flawed v1: the first version pooled a single global mean-RSA number across all 6 probe-set categories and paired it with mean accuracy across all public X-ARES tasks -- correctly flagged by the user as wrong, since RSA is inherently computed on a specific clip subset and pooling across categories throws away exactly the structure worth looking at. v2 computes, per category with a matching downstream task (music/FMA-genre, city_noise/UrbanSound8K, bird_sounds/BirdCLEF, machine_sounds/MIMII, ship_vessel/ShipsEar -- speech excluded, LibriSpeech is ASR not classification, no comparable accuracy metric), each model's mean RSA computed *only on that category's own clips* against its accuracy on that category's own task, point size = log10(params). Wrote `results/accuracy_vs_rsa_vs_size_by_category.png`.
- **Real infrastructure incident #2: `/scratch/pdoshi/audio-comp-venv` (the venv this session's sbatch jobs actually use) turned out to be missing `datasets`/`pyarrow` entirely**, discovered while trying to build a subclass-label plot (deferred, see below) that needed `datasets.load_dataset()` to join FMA/UrbanSound8K/BirdCLEF/DS3500 clip IDs back to genre/class/species/vessel-type labels. A second, older venv (`~/audio-comp-venv` in `$HOME`) does have `datasets==5.0.1`, meaning this project has silently had two divergent venvs for a while -- probably an artifact of an earlier $HOME-quota-driven move to $SCRATCH that only partially migrated. `pyarrow` on this cluster is provided via an Arrow environment module (`module load arrow`), not a normal pip wheel (`pip install pyarrow` fails with a deliberate `pyarrow-noinstall` dummy-package error directing you to the module); tried the documented fix (load the module before creating/activating a `--system-site-packages` venv) and it still didn't expose `pyarrow` to the venv's Python -- genuine unresolved environment issue, not chased further given the actual per-category plot didn't end up needing it. **The subclass-level plot (music genre / city_noise class / bird species / ship vessel-type / machine-type breakdowns) is deferred, not built this session** -- clip_id -> label join is straightforward in principle (FMA needs `fma_metadata/tracks.csv` genre lookup by track ID; UrbanSound8K/BirdCLEF-source/DS3500 all use `clip_id = f"{source}/{i}"` where `i` is a deterministic seed=42-shuffled row index directly into the same HF dataset the probe set was built from, so re-deriving the label per clip is just re-running that shuffle and indexing -- MIMII's machine-type/condition are already embedded in its clip_id, no join needed there) but blocked purely on getting a working `datasets`/`pyarrow` install in the venv actually used for sbatch jobs.
- **Discovered and fixed a second env-var bug while debugging the above**: the fix for the earlier $HOME-quota incident set `AUDIO_COMP_DATA` in `.bashrc`, but the actual code (`audio_comp/data/sources/fma.py`) reads `AUDIO_COMP_DATA_ROOT` -- a different name, meaning the fix silently didn't apply to anything reading that variable. Also discovered `sbatch --wrap` jobs do NOT source `.bashrc` at all (confirmed empirically: a job explicitly needing `AUDIO_COMP_EXTERNAL` still resolved to the old, now-deleted `$HOME` path) -- every sbatch job that touches `AUDIO_COMP_DATA_ROOT`/`AUDIO_COMP_EXTERNAL`/`HF_HOME` needs to export them explicitly in the `--wrap` command itself, `.bashrc` only helps interactive/login shells. Fixed the variable name in `.bashrc` and started explicitly exporting all three in every relevant sbatch submission from this point on.
- **TCA (`tca-s01.research.cs.dal.ca`) is now live for the user to book** (shared 2x RTX PRO 6000 Blackwell Max-Q server, dashboard at `:8000/dashboard`, plain SSH -- no Slurm/scheduler). Per this project's standing confidentiality rule, the assistant does not run anything touching `bigdata6`/real vessel audio there, but the user explicitly authorized SSH access (credentials placed in a gitignored `.env`, verified) for environment *setup* only. Repo cloned via HTTPS (public GitHub), fresh venv built with `torch==2.11.0+cu128` (matching the fir cluster's version, verified working against the Blackwell GPU) plus `transformers==5.15.0`/`peft==0.20.0`/`huggingface_hub==1.27.0` (version-matched to fir) and the rest of the pipeline's deps. Smoke-tested a full LoRA forward+backward pass on the GPU (`wav2vec2`, both a normal batch and a standalone 60ms clip) -- works cleanly. `data/AIS/` is entirely gitignored (not just the CSVs) so git clone didn't bring over the vessel-classifier scripts -- an SFTP transfer of just the three `.py` files (verified no hardcoded credentials/labels first) was blocked by the safety classifier for reaching into that directory even though the specific files were clean; the user will need to `scp` those files over themselves.
- **Extended LoRA fine-tuning from 5 to 14 of the 19 active models** (`audio_comp/pipelines/lora_model_configs.py`, new shared module), per explicit user request to run "all pretrained models" through both the frozen-probe and LoRA-fine-tune conditions on the confidential vessel data. Directly inspected `named_modules()` for every active model (not assumed from architecture family, per this project's standing verification rule) and grouped by actual LoRA target-module naming: 11 models share wav2vec2-style `q_proj`/`v_proj` (the original 5 plus `data2vec_audio`/`mms`/`sew`/`unispeech_sat`/`wavlm`/`whisper`), `wav2vec2_conformer` uses distinct `linear_q`/`linear_v` naming (Conformer's relative-position attention) despite sharing wav2vec2's training data/objective, `bird_mae` uses fused ViT-style `qkv`, and `clap` uses `query`/`value` scoped via regex to its `audio_model` submodule only (CLAP also has an unused text tower that would otherwise pick up wasted LoRA params under a bare leaf-name match). All 14 smoke-tested end-to-end (forward+backward pass, plus the mandatory standalone 60ms-clip check) on the fir cluster's GPU -- caught one real bug this way: `bird_mae`'s `last_hidden_state` comes out already pooled to `(batch, hidden)`, unlike every other model's `(batch, seq, hidden)`, so applying the same `mean_pool()` silently averaged over the *hidden* dimension instead of a nonexistent time axis; fixed by skipping pooling for that one model, root-caused by directly inspecting the tensor shape rather than guessing. **Five models explicitly excluded, not silently dropped**: `panns_cnn14` (pure CNN, no attention layer at all -- would need LoRA-on-Conv2d, a fundamentally different technique) and `audio_jepa`/`audiomae`/`musicfm`/`encodecmae` (none use the HF-style `self._extractor(...)` pattern the shared wiring assumes -- each has bespoke preprocessing baked into `embed_batch()` that would need individual reimplementation for a differentiable LoRA path; real, bounded follow-up work, not attempted this pass). Built `data/AIS/run_all_vessel_experiments.py`, a single combined entry point running one `RemoteStream` session (one password prompt) through both the frozen condition (all 19 active models, reusing `frozen_baseline_streaming.py`'s `run_model()`) and the LoRA condition (14 models) into one results CSV -- 33 total runs, not the "38" originally estimated, with the 5-model gap stated explicitly in the script's own docstring and printed at runtime. Import-checked (not run -- needs the real manifest CSV and bigdata6 password, which the assistant never touches) on the fir cluster.
- **User started the real confidential-vessel run on TCA and it surfaced real gaps this session's environment setup hadn't covered**: (1) `training_manifest.csv` isn't transferred by the assistant (confidential, `data/AIS/`'s whole-directory gitignore boundary) -- user copied it themselves. (2) TCA's fresh `$AUDIO_COMP_EXTERNAL` was empty -- `audio_jepa`/`musicfm`/`panns_cnn14`/`audiomae` all need vendored source/checkpoints from `scripts/setup_*.sh`, none of which are confidential (public model code/weights), so the assistant ran all four directly on TCA -- 3 succeeded outright, `setup_audiomae.sh`'s checkpoint step failed because the script invokes plain `python3 -m gdown`, which resolved to the system Python (no `gdown` installed) rather than TCA's venv -- fixed by re-running that one step through the venv's own Python explicitly. (3) `encodecmae` and `panns_cnn14`'s Python deps (`panns_inference`/`torchlibrosa`/`timm`/`pytorch_lightning`/`gin-config`/`ginpipe`/`encodec`/`torchinfo`) were never installed on TCA either (only the LoRA-relevant deps were, in the original venv setup) -- installed proactively before the user's re-run hit them one by one. (4) Installing `timm` pulled in a CPU-only `torchvision` as a side effect, silently incompatible with the already-installed `torch==2.11.0+cu128` -- `encodecmae`'s import chain (via `torchmetrics`) hit `RuntimeError: operator torchvision::nms does not exist`; a plain `pip install torchvision` no-op'd ("already satisfied") since pip didn't recognize the ABI mismatch -- fixed with `--force-reinstall --no-deps` against the `cu128` wheel index to actually rebuild the native extension.
- **Extended LoRA-gap coverage with a second, explicitly separate fine-tuning condition: top-K-layer unfreezing** (`audio_comp/pipelines/topk_unfreeze_configs.py`), per direct user request after asking how the 5 LoRA-incompatible models should be handled. Flagged the real methodological risk first (mixing LoRA and top-K-unfreeze into one "fine-tuned" comparison would confound adaptation *method* with model identity -- exactly the class of issue CLAUDE.md's Stage 5 "matched protocol" language exists to prevent) -- user chose to report it as a clearly separate condition, never blended with the 14-model LoRA comparison, matching the project's existing official-vs-matched fine-tuning split. Inspected each of the 5 gap models' top-level architecture directly: `panns_cnn14` has `conv_block1..6` + `fc1`/`fc_audioset`; `audio_jepa`/`audiomae` both have a 12-block ViT-style `.blocks` ModuleList + `.norm`; `musicfm` has `.conformer.layers` (12, ModuleList); `encodecmae`'s `visible_encoder.encoder_layers` (20, ModuleList) exists too, but reading its `extract_features_from_array()` source directly showed it's genuinely non-differentiable as written (`torch.no_grad()` wrapping numpy round-trips mid-pipeline via `self.apply_processors()`) -- excluded from this condition too, not force-fit, real reimplementation work not attempted this pass. Built unfreeze-last-K wiring for the other 4 (`TOPK_BLOCKS=2`, matched across all 4), all smoke-tested end-to-end (forward+backward + the mandatory standalone-60ms-clip check) -- caught a real latent bug this way: `musicfm`'s internal preprocessing crashes on inputs under ~1024 samples ("Padding size should be less than the corresponding input dimension"), the same short-clip class of bug as the audio_jepa/panns_cnn14 lessons, except this one exists in `musicfm.py`'s own production `embed_batch()` too (never triggered so far since every clip used in practice has been long enough) -- fixed both the new top-K path and the production adapter with the same `MIN_SAFE_SAMPLES_S`-style safety pad panns_cnn14 already used. `run_all_vessel_experiments.py` extended to a third `--condition topk` (default `--condition all` now runs frozen+lora+topk = 19+14+4 = 37 total runs, up from 33). Noted explicitly in the module docstring: `musicfm`'s `get_latent(layer_ix=7)` reads an intermediate layer, so unfreezing only the *last* K=2 layers (10-11) doesn't actually touch the extracted layer's own weights -- a real, non-obvious limitation of the current `TOPK_BLOCKS` value for that specific model, stated rather than glossed over.
- **Second TCA environment-skew round, caught live during the user's frozen-probe run**: crashed on `audiomae` with `Block.__init__() got an unexpected keyword argument 'qk_scale'` -- a `timm` version skew (TCA had `pip install timm` pull the latest `1.0.28`, but `AudioMAE-main`'s vendored `models_mae.py` needs the old `0.4.9` API, matching what's actually installed on the fir cluster). Fixed by pinning `timm==0.4.9` on TCA. **Proactively load-tested all remaining 16 models on TCA afterward** (not just fixing the one that crashed) rather than let the user hit each version-skew issue serially -- all 16 loaded cleanly, so all 19 active models are now confirmed loadable in TCA's environment.
- **MIMII LoRA production run completed** (`stage5_lora_finetune_mimii.py`, 5 models x 4 leave-one-machine-type-out folds, ~200 balanced clips/class/machine, submitted on the fir cluster after being smoke-tested but never actually launched at full scale earlier in the session -- caught only when the user asked directly what happened to it). All 5 jobs completed well under the 2h budget (17-28 min each). **Result: every model lands at or near chance** (mean accuracy: ast 0.499, music2vec 0.489, mert 0.485, hubert 0.477, wav2vec2 0.458 -- binary balanced task, chance=0.50). Read as a genuine, reportable negative result, not a bug: leave-one-*machine-type*-out is a much harder generalization test than the 16-fold leave-one-*unit*-out split the frozen X-ARES MIMII probe uses (where the same 5 models score 0.75-0.85 MLP) -- the two numbers are **not directly comparable** (different fold scheme, different class balance, different clip counts), so this isn't "LoRA fails on MIMII," it's "none of these 5 models' representations, even after matched LoRA adaptation, generalize anomaly-detection features to a machine type never seen during training." Worth a closer look at whether the frozen condition holds up under the same harder leave-one-machine-type-out split before concluding this is LoRA-specific.
- **Major bug found and fixed while extending the MIMII LoRA run to all 14 models**: `stage5_lora_finetune_mimii.py`'s `run_fold()` was passing `build_lora_model_and_head()`'s returned `sample_rate` (the MODEL's *expected* rate, e.g. 24000 for `mert`) into `lora_prepare_inputs()` as if it were the waveform's true native rate, instead of MIMII's actual 16000Hz. `resample()` then silently no-op'd whenever expected != native, feeding the model speed/pitch-corrupted audio. Harmless by coincidence for models expecting 16000 (most of the original 5), but a real, confirmed bug for `mert` (24000) and any future model with a non-16000 expected rate. **The exact same bug, unfixed, exists in the original `stage5_lora_finetune.py` (DeepShip)** -- checked directly and confirmed DeepShip's clips are natively 32000Hz while every one of its 5 models expects 16000 or 24000Hz, meaning DeepShip's LoRA fine-tuning has been training on ~2x-speed (16kHz models) or ~1.33x-speed (`mert`) corrupted audio this entire time. This directly confounds Stage 5 v1's already-published "matched LoRA underperforms frozen probing, overfitting-diagnosed" finding -- the overfitting explanation may still hold but was never isolated from this audio-corruption confound. **Per explicit user direction: DeepShip is being deprioritized (small, public dataset) in favor of the confidential vessel data, so `stage5_lora_finetune.py` is left unfixed and not re-run** -- the v1 finding should be read with this caveat going forward, not silently trusted as clean. Verified the confidential-vessel-data scripts (`train_vessel_classifier_streaming.py`, `frozen_baseline_streaming.py`, both `run_lora_model`/`run_topk_model` in `run_all_vessel_experiments.py`) do NOT have this bug -- all of them correctly use `srs[0]`, the true native rate returned per-clip by `ClipCache.get()` (via direct `sf.SoundFile(...).samplerate` inspection), never the model's expected rate. Fixed MIMII's version with an explicit `MIMII_NATIVE_SAMPLE_RATE = 16000` constant (verified via `sf.info()` directly, not assumed) passed at both call sites. Smoke-tested `mert` specifically post-fix: training loss now drops properly (0.685->0.109 over 5 epochs, vs. barely moving before) confirming the model is genuinely learning from correctly-resampled audio -- but held-out test accuracy stays near chance (0.515), suggesting the near-chance MIMII result is a real machine-type-generalization difficulty, not primarily an artifact of this bug.
- **Extended MIMII to all 14 LoRA-compatible models (post-fix) plus a new frozen-probing counterpart under the identical leave-one-machine-type-out split, all 19 active models** (`stage5_frozen_finetune_mimii.py`, new script, built specifically to disambiguate "hard task" from "LoRA-specific failure"). One real infra hiccup: `wav2vec2_conformer`'s LoRA job OOM'd twice in a row on a shared `3g.40gb` MIG GPU slice (other processes eating most of the 40GB) -- resolved by requesting a full non-MIG `gpu:h100:1` instead, succeeded cleanly. **Final result, 14 models with both conditions**: frozen probing and LoRA both cluster near chance (frozen 0.44-0.57, LoRA 0.44-0.50) on this harder split -- neither condition solves unseen-machine-type generalization, a genuinely different, cleaner-than-DeepShip negative result now that the sample-rate confound is fixed. **11 of 14 models still show LoRA underperforming frozen probing even so** (mean delta -0.027; only `clap` +0.059, `hubert` +0.016, `wav2vec2_conformer` +0.016 go the other way) -- this independently replicates DeepShip v1's "matched LoRA underperforms frozen" direction on a dataset 30x larger and with the sample-rate bug fixed, suggesting the effect isn't purely a DeepShip-specific overfitting/corruption artifact, though the effect size here is much smaller (both conditions being near-chance leaves little room for a large gap either way). The 5 frozen-only models (no LoRA config) also cluster near chance: `audio_jepa` 0.532, `musicfm` 0.518, `panns_cnn14` 0.518, `encodecmae` 0.514, `audiomae` 0.499.
- **Paired statistical verification, at user's request, of the "LoRA underperforms frozen" pattern.** Fold-level (n=56) vs. model-mean-level (n=14) tests disagree in an important way: model-mean paired t-test/Wilcoxon both cross p<0.05 (0.036/0.042) but only barely; the fold-level **sign test is essentially a coin flip** (30/54 nonzero folds negative, p=0.497) -- the mean-level "significance" is driven by a few models with large deltas (`data2vec_audio` -0.098, `unispeech_sat` -0.081), not a broad consistent pattern. Logged as "statistically detectable but small and not deeply robust" rather than "verified," per direct user pushback on over-claiming from a single split.
- **Built an easier MIMII variant at user request** (`stage5_lora_finetune_mimii_unit.py` / `stage5_frozen_finetune_mimii_unit.py`): leave-one-**unit**-out *within* the same machine type (train on 3 units of a type, test on the unseen 4th unit of that same type), instead of leave-one-machine-**type**-out -- same 4-fold structure/cost, genuinely easier since the test unit's machine type was seen during training. **Result: both conditions now show real above-chance signal** (frozen 0.49-0.68, LoRA 0.44-0.70), unlike the harder split's floor effect -- confirms the near-chance MIMII result wasn't a code/setup problem, it was specifically the machine-type-generalization axis being hard. **The "LoRA underperforms frozen" pattern does NOT replicate here**: mean delta shrinks to -0.013 (was -0.027), only 9/14 models negative (was 11/14), Wilcoxon p=0.326 and paired t-test p=0.426 -- nowhere near significant. Some models now clearly favor LoRA (`wavlm` +0.114, `data2vec_audio` +0.078, `hubert` +0.032), others still favor frozen (`mert` -0.109, `bird_mae` -0.067, `whisper` -0.065). **Honest read: the borderline "LoRA underperforms frozen" signal from the harder split looks like it was at least partly a floor-effect artifact** (both conditions sitting near chance amplifies any small stochastic difference into a consistent-looking direction) rather than a robust, generalizable property -- the corrected, fuller picture is "genuinely mixed, model-dependent," not a directional finding. Two OOM retries needed again (`mms`, `wav2vec2_conformer`, same shared-MIG-slice contention pattern), same full-GPU fix.
- **Per explicit user direction, DeepShip/ShipsEar work is deprioritized entirely** ("throw out deepship/shipsear... only focus should be our real and confidential data") -- moved to a new Appendix A in `CLAUDE.md` (not deleted) with the v1 result and its sample-rate confound documented for anyone who revisits it later. All new work redirects to the confidential vessel data and other public-data sanity checks (MIMII, and now FMA-genre/BirdCLEF/UrbanSound8K per a follow-up scope check -- DeepShip specifically is out, non-vessel public validation stays in).
- **First RSA/CKA representational-geometry pass on the confidential vessel domain** (`data/AIS/rsa_cka_vessel.py`, new script -- Phase 1's core methodology had never been run on this domain before, only downstream accuracy). Frozen embeddings only (matches this project's standing convention -- geometry is always compared pretrained, never fine-tuned). Only the aggregate N-model RSA/CKA correlation matrices are ever written to disk -- embeddings held in memory for one process, discarded on exit, no per-clip data persisted anywhere. User ran it on TCA and copied results back (`results/vessel_rsa_matrix.csv`, `results/vessel_cka_matrix.csv` + heatmaps) -- copy included several files that were exact duplicates of already-tracked top-level `results/` files (verified byte-identical via sha256 before deleting, not assumed) -- cleaned up, keeping only the 4 genuinely new vessel-specific outputs. **Striking result, cross-checked by both metrics**: `music2vec` and `data2vec_audio` are dramatically isolated from the rest of the 19-model roster on this domain (RSA 0.08-0.23 and 0.11-0.33 respectively; CKA independently confirms the same isolation, 0.03-0.22 for both) -- far more isolated here than in the public 6-category probe set's own RSA numbers. Visible cluster structure resembling the project's established breadth-hypothesis pattern: a broad-training cluster (`ast`/`panns_cnn14`/`bird_mae`/`audiomae`/`clap`/`audio_jepa`/`whisper`/`musicfm`, mutual RSA 0.6-0.9) separates from the narrow-speech wav2vec2-family cluster (`hubert`/`mms`/`sew`/`unispeech_sat`/`wav2vec2`/`wav2vec2_conformer`, mutual RSA 0.5-0.8), with weaker cross-cluster agreement -- not yet formally tested against the breadth-hypothesis framework on this domain, worth a follow-up pass.
- **Extended `rsa_cka_vessel.py` to a second, separate condition -- post-LoRA-adaptation geometry** (`--condition {frozen,lora,both}`, `both` is now the default), per direct user ask: "is this also based on pretrained and finetuned distribution? i need to see both." Frozen (19 models, unchanged) and LoRA (14 LoRA-compatible models, trains each on the standard matched split then embeds the FULL clip set with the adapted encoder's pooled representation, not the classification head) are computed and written to separate, distinctly-named files (`vessel_{rsa,cka}_matrix_frozen.csv` / `_lora.csv`) -- never blended into one matrix, same reasoning as every other frozen-vs-adapted split in this project. Not yet run (needs the user's own execution, ~14 model fine-tuning runs inside one RemoteStream session -- meaningfully longer than the frozen-only version).
- **Built local-file manifests for FMA-genre and UrbanSound8K**, the two of the three remaining probe-set categories needing a matched LoRA-vs-frozen comparison (BirdCLEF already had one from earlier X-ARES work). FMA-genre (`xares_eval/fma_genre/build_manifest.py`) needed no new download -- joined the probe set's own local `fma_small` cache against `fma_metadata/tracks.csv`'s `genre_top` + `split` fields, reusing X-ARES's own 8-class taxonomy (Hip-Hop/Pop/Folk/Experimental/Rock/International/Electronic/Instrumental) and FMA-small's native 80/10/10 train/val/test split -- 8000 clips, exactly 1000/genre, confirmed against the dataset's known structure. UrbanSound8K (`xares_eval/urbansound8k/build_manifest.py`) needed a fresh HF download + local wav decode (8732 clips, `class`/`classID`/native `fold` 1-10 fields -- Salamon et al. 2014's own standard 10-fold CV split, not a custom one) -- hit the same `datasets`/`pyarrow` wall the fir cluster's venv has had all session (see the 2026-08-16 entry on the Arrow-module dead end), and this time confirmed it's not fir-specific: even TCA's normally-unrestricted `pip install pyarrow` succeeded but the newer `datasets==5.0.1`'s audio decoding now requires `torchcodec`, which itself failed to load (`Could not load library: libtorchcodec_image.so`, a missing system dependency) -- fixed by downgrading to `datasets==2.19.0` on TCA, which still uses the older dict-based `{"array", "sampling_rate"}` audio format this project's other HF-sourced loaders (`iter_hf_audio_clips`) already expect, avoiding torchcodec entirely. Both manifests built successfully; LoRA/frozen fine-tuning scripts for these two (mirroring `stage5_lora_finetune_mimii.py`'s pattern) not yet built.
- **Adopted ALLoRA (Huang & Balestriero, arXiv:2410.09692) as a matched-protocol alternative to LoRA**, per explicit user request to try 7 candidate LoRA-alternative methods and pick the best. A research agent verified each against its actual paper (not the user's one-line characterizations, per this project's standing verification rule): MoRA/RandLoRA/Delta-LoRA/KRAdapter/GraLoRA/"Why LoRA Fails to Forget" all diagnose LoRA as having *too little* effective capacity (memorization, large-adaptation-budget, or backdoor-removal problems) -- this project's own evidence points the opposite way (LoRA underperforming a frozen linear probe, 1-epoch beating 10-epoch on the same fold), reading as an overfitting/optimization failure, not a capacity ceiling. ALLoRA's own paper targets exactly this regime (limited data, short fine-tuning runs) and needs zero architectural change -- only a different gradient rule -- making it the lowest-risk option to implement across 14 heterogeneous encoders. **Read the actual paper directly** (fetched the PDF, not just the abstract) to get the exact formula rather than guessing from a research-agent summary: removes LoRA's dropout and alpha/r scaling entirely; rescales the gradient feeding A/B (not the gradient propagated to the input) per output-row i by 1/sqrt(||(BA)_i,:|| + 1/eta^2). Implemented as a custom `torch.autograd.Function` (`audio_comp/pipelines/allora.py`) since a plain gradient hook can't apply different scaling to grad_input vs grad_A/grad_B. **Validated numerically before trusting it anywhere**: grad_input checked via `torch.autograd.gradcheck` (must equal the true gradient, since ALLoRA explicitly doesn't touch it) -- passed; grad_A/grad_B checked against an independent, hand-written, loop-based reference implementation of the paper's own formula (deliberately NOT gradcheck, since ALLoRA's rescaled gradient is intentionally not the true gradient of the forward pass) -- passed. Smoke-tested across all 14 models (manual module-replacement wiring in `allora_model_configs.py`, mirroring `lora_model_configs.py`'s target_modules exactly since ALLoRA has no peft integration) -- caught and fixed 3 real bugs: the entire base model was left trainable (forgot the explicit freeze before wrapping target layers -- caught via absurd 72M-568M "trainable_params" counts vs. LoRA's ~300K-800K); `wavlm`'s own code inspects `.bias`/`.weight` directly on what it expects to be a plain `nn.Linear` (added passthrough properties on `ALLoRALinear`); `whisper`'s forward function used `hasattr(model, "base_model")` to detect peft-wrapping, which collides with a real HF-native `base_model` property every `PreTrainedModel` has (fixed to check `peft_config` instead, a peft-specific attribute).
- **Built a shared multi-category fine-tuning framework** (`generic_lora_trainer.py`, factored out after writing the same ~80-line training loop 4 times) and extended FMA-genre/UrbanSound8K/BirdCLEF plus both MIMII splits to support `--condition allora` alongside the existing frozen/lora, per the user's ask to "run all fine-tuning experiments" with the newly-adopted method. **Caught a real mp3-decoding bug during the FMA-genre smoke test**: `soundfile`/libsndfile can't decode FMA-small's `.mp3` files (a `LibsndfileError`); `librosa.load()`'s automatic `audioread` backend dispatch was tried next and ALSO failed (`NoBackendError`), despite `audioread.available_backends()` listing `FFmpegAudioFile` as available AND a direct, manual `FFmpegAudioFile(path)` call succeeding cleanly in isolation -- a real flakiness in audioread's own automatic-dispatch logic, confirmed via a dedicated debug job rather than assumed. Fixed by calling `audioread.ffdec.FFmpegAudioFile` directly, bypassing the dispatcher entirely -- verified byte-correct (not just crash-free) against an independent `ffmpeg`-CLI decode of the same file, correlation 1.0. Also needed `module load ffmpeg/7.1.1` in every sbatch job touching FMA (not loaded by default), and skip-on-decode-failure handling for FMA-small's small number of genuinely corrupted mp3s (the exact file CLAUDE.md's own `fma.py` docstring already names, `099134.mp3`, showed up again here -- consistent, not a new problem). **Submitted the full batch, 2026-08-17**: FMA-genre/UrbanSound8K/BirdCLEF at 14 lora + 14 allora + 19 frozen each (141 jobs total), plus MIMII's remaining ALLoRA gap (14 models x 2 splits = 28 jobs) -- 169 jobs submitted, results pending.
- **Two real bugs found after the batch above completed** (session had a gap/restart in between; picked back up by checking actual job outcomes rather than trusting the pending-work summary). (1) **OOM on a full 80GB GPU** for several large wav2vec2-family models (`hubert`/`mert`/`wav2vec2`/`wav2vec2_conformer`/`wavlm`/`mms`) on FMA-genre specifically -- root cause: `generic_lora_trainer.py`'s training loop never called `torch.cuda.empty_cache()` (unlike every other fine-tuning script in this project), compounded by FMA-genre's 30s clips being far longer than MIMII/UrbanSound8K/BirdCLEF's 4-10s (X-ARES's own upstream `fma_genre_config` already crops to `crop_length=10` for the same reason -- matched that value here rather than guessing). Fixed with periodic `empty_cache()` calls plus a `max_duration_s` truncation parameter threaded through the whole trainer. (2) **A second, more serious sample-rate bug, this time within a single dataset**: `finetune_urbansound8k.py` assumed a uniform `NATIVE_SAMPLE_RATE=44100` for all clips (the script's own docstring even flagged this as unverified -- "verify per-clip if this assumption ever breaks"). Checked it directly this time: a 200-clip random sample showed genuinely mixed native rates (44100/48000/96000/24000/16000/8000/192000), not a uniform value -- meaning every UrbanSound8K run so far (frozen AND lora AND allora, all of which "completed" without error) silently resampled-or-not incorrectly for the ~37% of clips not at 44.1kHz. Fixed properly: `read_mono()` now returns each file's TRUE native rate (not assumed), `read_batch_skip_bad()` resamples every clip to a common target explicitly using that real rate. **All UrbanSound8K results from before this fix are corrupted and need a full rerun, not just the failed jobs** -- FMA-genre and BirdCLEF are unaffected (independently verified both ARE genuinely uniform-rate: FMA-small at 44.1kHz per its own mp3 encoding, BirdCLEF at 32kHz per a 200-clip sample check -- checked rather than assumed this time, after getting UrbanSound8K's assumption wrong). Also caught the identical `expected_sample_rate`-instead-of-native-rate bug pattern (same class as the earlier MIMII fix) inside `train_and_eval_frozen()`'s `embed()` function -- fixed with an explicit `native_sample_rate` parameter now required from all 3 dataset scripts' frozen calls.

## 2026-08-18

- The UrbanSound8K sample-rate fix from 2026-08-17 existed only locally for a while and was never committed -- TCA's `git pull` had nothing new to fetch and kept running the stale pre-fix trainer, reproducing the identical corruption failure there. Committed and pushed (`e87a1db`). Standing lesson: a fix isn't real until it's committed -- verifying it works locally isn't the same as verifying it's reachable from a `git pull` elsewhere.
- Separately (and this was the actual cause of TCA's continued failures even after the git push landed): TCA's `data/urbansound8k_manifest.csv` had every path corrupted with a literal, unresolved placeholder string (`/users/grad/<TCA_UID>/urbansound8k_audio_fromfir/...` instead of `/users/grad/pdoshi/...`). Root cause: an earlier `sed` path-correction for TCA was run using the literal text of a redacted display placeholder instead of the real resolved username. Fixed in place on TCA only (`sed -i 's|<TCA_UID>|pdoshi|g'`, not committed to git -- TCA and fir need different path prefixes in this file by nature, so divergence here is expected). Verified all 8732 manifest paths resolve on TCA afterward.
- Per explicit user direction ("push the urban thing to the slurm backlog, leave tca just for vessels as of now"), UrbanSound8K work is moved off TCA entirely and onto fir's Slurm queue; TCA is reserved for vessel-data work only going forward. Checked fir's actual job history via `sacct` rather than assuming: the post-fix batch (job IDs 55241500-55241620) had already completed successfully for most of the 47-job roster with the corrected manifest/sample-rate handling (sane accuracies, e.g. `us8k_lora_ast` 0.876-0.890 across folds) -- only 6 jobs had hit their time limit (`us8k_lora_ast`, `us8k_allora_ast`, `us8k_allora_bird_mae`, `us8k_lora_sew` at the 3h GPU-job limit; `us8k_frozen_unispeech_sat`, `us8k_frozen_wav2vec2` at the 1.5h limit). Resubmitted just those 6 with more headroom (6h for lora/allora, 3h for frozen) via `sbatch --wrap`, matching `extract_embeddings.sbatch`'s module-load/venv/env-var conventions -- job IDs 55329232-55329237.

## 2026-08-18 (continued)

- **Added two new probe-set data sources per user request, directly addressing the clean-vs-noisy confound logged above.** Researched candidates rather than guessing (per standing verification rule) -- for noisy speech, `edinburghcstr/ami` (AMI Meeting Corpus, CC-BY-4.0, verified via HfApi) was the clear winner: real (not synthetic) meeting-room noise, 267K pre-segmented utterance clips, clean license. Used the `sdm` (single distant mic) config over `ihm` (headset) deliberately -- `sdm` captures real room noise/reverb, `ihm` is close-talk and would defeat the point. Wired up as `audio_comp/data/sources/ami.py` -> `speech_noisy` category. For noisy music, the space is genuinely thin (confirmed via research, not assumed) -- `amphion/SingVERSE` (CC-BY-4.0, real noisy/clean singing-voice pairs across 19 real acoustic scenarios) is the only real candidate, but it's singing-voice-only, a domain-narrower match to FMA's genre diversity than ideal -- flagged explicitly in the module docstring rather than overstated. A broader, real-world, clearly-licensed noisy-music dataset at FMA's scale was searched for and not found; `seungheondoh/audioset-music` was ruled out (metadata/YouTube-ID only, no bundled audio, copyright grey zone). Wired up as `audio_comp/data/sources/singverse.py` -> `music_noisy` category, using only the `noisy_audio` field (the paired `clean_audio` field exists but isn't loaded here).
- **Smoke-tested per this project's standing rule.** `ami_meetings` fully verified end-to-end through the actual loader class with real shuffling -- correct clip IDs, 16kHz, sane per-clip durations (2.7-7s), right category tag. `singverse_noisy`'s schema/field extraction verified directly (raw `noisy_audio` field, 44.1kHz, matches docstring) but the full shuffled loader-class run couldn't complete on fir's login node -- killed once by the node's resource governor (SIGKILL, large per-row audio x buffer_size=2000) and timed out once even at buffer_size=50, purely from SingVERSE's parquet shards being slow to fetch over the network (an unshuffled single-row fetch alone took ~207s). Not a code defect: the wrapping logic is the identical, already-proven pattern used successfully for `ami.py`, and the underlying field access was independently confirmed twice. Worth remembering if this source is used in `build_probe_set.py` later -- expect slow first-shard latency, budget accordingly.
- Both new sources registered in `audio_comp/data/sources/__init__.py` and added to `configs/categories.yaml` (`speech_noisy` -> `ami_meetings`, `music_noisy` -> `singverse_noisy`) -- not yet run through `build_probe_set.py` to actually populate the probe set, that's a separate follow-up step.
- **mert + clap disambiguation result (job 54694979 completed)**: computed Primary-vs-Lateral peak-layer differentiation (Δlayer_frac = Lateral peak - Primary peak) for all 7 models. Speech-domain models differentiate regardless of objective: wav2vec2 (masked) Δ=0.33, hubert (masked) Δ=0.80, wavlm (masked) Δ=0.17, whisper (discriminative/ASR) Δ=0.66 — all real, positive, whisper's is the *second largest* despite being discriminative, directly contradicting a pure-objective account. Non-speech models are weaker/mixed: mert (masked, music) Δ=0.08 ≈ flat, ast (discriminative, general) Δ=-0.08 flat, but clap (contrastive, general, audio-text) Δ=0.25 — a real partial exception. **Reading: domain-match (speech vs. non-speech) predicts Primary/Lateral depth-differentiation better than objective (masked vs. discriminative) in this small n=7 sample** — whisper is the clearest single counterexample to an objective-only account. clap's partial differentiation despite non-speech domain is the interesting loose thread, plausibly tied to its natural-language supervision (a variable neither the objective nor domain axis captures) rather than either factor cleanly. Genuinely small-n (7 models, exploratory) — not strong enough to claim resolved, but a real, honest, motivating result, not fabricated or forced into a clean story. Reported back to peer session and folded into the course-proposal draft (`brain_rsa/proposal/`) as preliminary evidence motivating a larger controlled follow-up.
- **Drafted the Neurocomputing course-paper proposal** (`brain_rsa/proposal/proposal.tex`, compiles clean to `proposal.pdf`, 5 pages of body + 1 page references, 11pt, satisfies the assignment's constraints). Built directly on this detour's real findings rather than a generic literature summary: problem specification centers on the objective-vs-domain confound in Tuckute et al. (2023)'s model roster, discovered concretely via AST's flat Primary/Lateral depth-differentiation in this project's own preliminary data; related-work section reviews Tuckute et al. in depth plus wider lineage (Yamins et al. 2014, Kell et al. 2018, Kriegeskorte et al. 2008 RSA, Kornblith et al. 2019 CKA, Davari et al. 2023 CKA gameability, Schrimpf et al. 2018 Brain-Score, Millet et al. 2022, Huh et al. 2024 Platonic Representation Hypothesis); research question section states two falsifiable, mutually-non-exclusive hypotheses (domain-match vs. objective) with the real 7-model preliminary $\Delta$ table (HuBERT 0.80, Whisper 0.66, wav2vec2 0.33, CLAP 0.25, WavLM 0.17, MERT 0.08, AST -0.08) as motivating evidence; methodology section specifies the balanced 2x2 model roster extension (MusicFM, a second speech-domain discriminative model), the extraction/RSA pipeline (reusing this session's actual code), and a permutation-test statistical plan matching the approach already used for the main pipeline's breadth-hypothesis check. Had to trim substantially (first draft compiled to 8 pages) via tighter prose and `titlesec` spacing rather than cutting content substance, to hit the 5-page body limit.

## 2026-08-18 (continued, 2)

- **Compiled the pending FMA-genre/UrbanSound8K/BirdCLEF frozen-vs-LoRA-vs-ALLoRA comparison now that all three are complete**, per explicit user instruction to apply the same "check real numbers, don't assume a prior finding generalizes" discipline used in the brain_rsa proposal work to the main model runs too. Computed directly from `results/finetune_{fma_genre,urbansound8k,birdclef}.csv` rather than assuming the MIMII "LoRA underperforms frozen" pattern would carry over. **It does not**: across all 42 LoRA-vs-frozen and 41 ALLoRA-vs-frozen model x category comparisons, zero show the adapted condition underperforming frozen (mean delta +0.153 both conditions) -- the literal opposite of MIMII's near-chance-task pattern. Re-verified MIMII's own already-documented numbers at the same time (harder split -0.027/11-of-14, easier split -0.013/9-of-14) -- both match exactly what's already in CLAUDE.md, so that record was accurate; the new finding is that it shouldn't have been read as evidence about LoRA/ALLoRA generally, just about near-chance tasks specifically. Written into CLAUDE.md's Stage 5 section, directly under the FMA-genre/UrbanSound8K manifest entry it extends.

## 2026-08-18 (continued, 3)

- **Ran the actual Stage 5/6 ceiling correlation** (frozen geometry vs. OOD adaptability) that the whole buildout existed to enable but had never been run, per explicit user direction to keep applying the same real-data-verification discipline. MIMII: clean null across TwoNN/uniformity/alignment (all |rho|<=0.30, p>=0.30) -- survives the sample-rate, floor-effect, and overfitting confounds already separately ruled out for MIMII elsewhere, so a defensible null, with the n=14 power caveat stated explicitly. FMA-genre/UrbanSound8K/BirdCLEF (real-signal tasks): uniformity significantly predicts LoRA adaptation gain, pooled rho=+0.736 p=0.003, strongest in UrbanSound8K (rho=+0.798, p=0.001), absent in BirdCLEF (rho=+0.116, ns). TwoNN shows no relationship anywhere -- uniformity/alignment-specific effect.
- **Four follow-up checks before trusting the positive result**: (1) uniformity vs. adapted accuracy directly -- null (pooled rho=-0.319, p=0.267), meaning uniformity predicts *headroom*, not final quality -- this is the precise, non-overclaiming version of the finding. (2) BirdCLEF null re-tested excluding bird_mae (domain-relevance-swamping hypothesis) -- did not hold, rho moves to -0.016, a genuine unexplained dead end. (3) parameter-count confound -- ruled out, partial correlation controlling for log(n_params) is rho=+0.754, p=0.002, essentially unchanged from raw. (4) leave-one-model-out predictive validation -- MAE=0.048 vs. 0.060 naive-mean baseline, ~20% real out-of-sample improvement; Whisper named as the worst-predicted case rather than smoothed away.
- **Objective-type (C) and breadth-cluster (D) tested directly as categorical predictors of adaptation gain** -- both null on MIMII and on the pooled FMA/UrbanSound8K/BirdCLEF gain (all p>=0.11). Neither the variable that explains RSA-matrix structure (breadth) nor the one that explains in-domain classification skill (objective-type, Finding 2) predicts OOD adaptation gain.
- **Checked whether uniformity is actually independent of these categories before writing a "three separate axes" claim** -- it isn't cleanly independent: AST/CLAP are simultaneously the most-uniform models and the primary drivers of any apparent categorical effect on uniformity itself (breadth ANOVA across non-singleton groups p=0.0087, but Kruskal-Wallis across all 6 groups p=0.173 and collapsed narrow-vs-broad p=0.347 -- the significance evaporates under more conservative tests). Landed on the precise, appropriately modest framing: uniformity preserves resolution among the remaining models that the coarse categorical buckets discard, rather than being either a fully independent axis or a relabeled category effect. Written into CLAUDE.md's Stage 5/6 sections with the full reasoning visible, not just the verdict.
- Vessel-domain version of this correlation not yet run -- blocked on the LoRA condition finishing on TCA and on diagnosing a user-flagged data-quality concern with no specifics yet available.

## 2026-08-18 (continued, 4)

- **Vessel run finished cleanly** (all 14 LoRA models completed on TCA, tmux session `vessel`) -- not stuck as earlier suspected. Both the RSA/CKA geometry pass (`rsa_cka_vessel.py`) and the accuracy pass (`run_all_vessel_experiments.py`, already complete from 2026-08-17) had their outputs available.
- **Ran the vessel-domain version of the geometry-vs-OOD-adaptability correlation (closing item B).** Null across all three metrics (uniformity rho=+0.187 p=0.523, alignment rho=-0.152 p=0.605, TwoNN rho=+0.451 p=0.106). Vessel gain is small and mixed (mean +0.028, 3/14 models negative) and accuracy sits in a narrow band (frozen 0.16-0.23, lora 0.15-0.32) -- the MIMII shape, not the FMA/UrbanSound8K shape. This matches the honest pre-registered prediction stated before the run finished: vessel would either show the headroom effect (if it behaved like a real-signal task) or another null (if it behaved like MIMII's near-chance pattern) -- it landed on the null side, completing Finding 5's OOD story across all three domains tested (MIMII null, vessel null, FMA/UrbanSound8K/BirdCLEF positive-in-2-of-3).
- **Found and fixed a real gap while checking on the vessel run**: neither `rsa_cka_vessel.py` nor `run_all_vessel_experiments.py` had an ALLoRA condition wired up -- both scripts predate the ALLoRA adoption decision, so vessel was the one domain (of MIMII/FMA-genre/UrbanSound8K/BirdCLEF/vessel) still missing the LoRA-vs-ALLoRA comparison every other domain already has. Added `run_allora_model()` to `run_all_vessel_experiments.py` (mirrors `run_lora_model()` exactly, swapping in `build_allora_model_and_head`/`allora_prepare_inputs`) and `train_allora_and_embed()`/`train_allora_and_embed_checkpointed()` to `rsa_cka_vessel.py` (same pattern, using `trainable["model"]` instead of LoRA's `trainable["peft_model"]` key for the final pooled-embedding extraction, since ALLoRA uses manual module replacement not peft). Both wired into their respective `main()`/CLI (`--condition allora`, plus `all`/`both` now include it). Syntax-checked locally and on TCA; pushed directly via SFTP (both files live under the gitignored `data/AIS/`, so a git-based sync wouldn't have reached them). Not yet run -- next vessel session needs `--condition allora` (or the default `all`/`both`) to actually produce the ALLoRA numbers.

## 2026-08-18 (continued, 5)

- **Pre-registered prediction, stated before any AMI/SingVERSE data enters the probe set or any embedding is extracted** (per explicit user instruction, to keep this a real prediction rather than a retrofit). Motivated by Tuckute, Feather, Boebinger & McDermott (PLoS Biology, 2023) -- their finding that models trained to recognize speech in background noise produced better brain predictions than models trained on clean speech only. The analogous, testable claim for this project (cross-model RSA/CKA agreement among the 19 audio-encoder models themselves -- NOT brain data, no conflation with the separate brain_rsa side project) is: **models whose own training data included substantial noise/diversity exposure (whisper, mms, audio_jepa -- broad/AudioSet-derived training per the established breadth-cluster grouping) should show a SMALLER representational shift between clean and noisy versions of the same content-domain than models trained on narrower, cleaner corpora (hubert, wav2vec2 -- LibriSpeech-960h masked modeling).** Operationalized once ami_meetings/singverse_noisy are in the probe set: compute RSA/CKA specifically between `speech` and `speech_noisy` (ami_meetings), and between `music` and `music_noisy` (singverse_noisy), per model -- the prediction is that broad/noise-exposed models show higher clean-noisy RSA (smaller shift) than narrow/clean-corpus models. Falsifiable outcomes, all reportable per this project's standing pattern (Finding 5's BirdCLEF exception was investigated and reported honestly, not hidden): confirmed (real, clean result -- candidate sixth finding), refuted, or mixed/category-dependent (still resolves the noise-confound caveat already in CLAUDE.md's Known Risks either way).

## 2026-08-18 (continued, 6)

- **Second pre-registered prediction, stated before `finetune_librispeech.py` is written or run** (speaker-ID confirmed as the classification target -- `openslr/librispeech_asr` has a genuine `speaker_id` int64 field, verified directly against the dataset's actual schema via the HF datasets-server API, not assumed from the docstring; `train.clean.100`, already the split `librispeech.py` uses, has 28,539 utterances across the publicly-documented 251 speakers). **Sharper framing than "add speech as a fourth real-signal domain to Finding 5"**: BirdCLEF (50-way classification) was the one real-signal task where the uniformity-headroom effect went null while the coarser FMA-genre (8-way) and UrbanSound8K (10-way) both showed it (Finding 5, 2026-08-18). LibriSpeech speaker-ID at 251-way is an even finer-grained task than BirdCLEF. **Prediction**: if task granularity (not something bioacoustic-specific to BirdCLEF) is what actually drives the null, LibriSpeech speaker-ID should also come back null or weak for the uniformity-vs-gain correlation, extending the pattern to a second fine-grained task rather than adding a fourth positive. Either outcome is a real, reportable extension of Finding 5 -- confirms/extends the granularity account, or breaks it and reopens why BirdCLEF specifically went null.

## 2026-08-19

- **Vessel-domain ALLoRA run completed** -- both `run_all_vessel_experiments.py --condition allora` (accuracy) and `rsa_cka_vessel.py --condition allora` (RSA/CKA geometry) finished on TCA, closing the last gap in vessel-domain coverage (frozen/LoRA/ALLoRA now all done, both accuracy and geometry). The geometry pass crashed once with a CUDA OOM on `wav2vec2_conformer` from running concurrently with the accuracy pass on the same physical GPU (`CUDA_VISIBLE_DEVICES=1` on both) -- the checkpoint/resume feature built 2026-08-18 worked as intended, only 3 of 14 models (`wav2vec2_conformer`/`wavlm`/`whisper`) needed rerunning after the concurrent job finished and freed the GPU, not the full 14.
- **ALLoRA results**: tracks LoRA closely on vessel data (mean d_allora=+0.0273 vs. LoRA's +0.0282, both 3/14 negative), consistent with the near-chance/floor-effect shape already established for MIMII-style tasks -- not a different story under the alternate adaptation method. The geometry-vs-adaptability null (Finding 5) also replicates under ALLoRA specifically (uniformity rho=+0.134 p=0.648, alignment rho=-0.169 p=0.563) -- confirms the vessel null isn't an artifact of LoRA specifically.

## 2026-08-19 (cont'd) -- provenance audit of Finding 5's UrbanSound8K number, before touching LibriSpeech/AMI-SingVERSE

- **User asked, before any further gap-filling: was the 2026-08-18 "continued 3" ceiling correlation (uniformity vs. UrbanSound8K LoRA gain, rho=+0.798, p=0.001) computed from corrected or pre-fix-corrupted data**, given the sample-rate fix (e87a1db) and the TCA manifest-placeholder fix landed the same day. Treated as blocking -- did not proceed to LibriSpeech/AMI-SingVERSE until resolved.
- **Traced `results/finetune_urbansound8k.csv`'s actual write history via `sacct` + git, not assumed.** `generic_lora_trainer.py`'s results writer opens the output CSV with `open(out_csv, "a", ...)` -- append-only, never overwrites a model's prior rows. Confirmed via job start/end times cross-referenced against file line position that job `55178041` (`us8k_frozen_panns_cnn14`, 2026-08-17 08:41-08:43, **before** the sample-rate fix was written) wrote the earlier-position duplicate row (accuracy 0.8213), and job `55241614` (same task, 18:43-18:45, the "full rerun" **after** the fix, ~10 min before the fix was committed as `e87a1db` at 18:54) wrote the later-position row (0.8465) -- confirms file row order == chronological write order == **last occurrence per (model,condition,fold) is the corrected, post-fix value**, first occurrence is stale/pre-fix.
- **Real, previously-unflagged finding: the CSV is contaminated with duplicates from this append-only write pattern, and always has been -- even the very first committed snapshot (`e87a1db`, 341 lines) already had 115 duplicate `(model,condition,fold)` keys with differing accuracy values**, not something introduced later. Current working-tree file (466 rows, 125 uncommitted rows from the 6 resubmitted TCA-blocked jobs completing 2026-08-18T06:06-09:09) has 223 duplicate keys with differing values (2 more with identical values). `results/finetune_fma_genre.csv` has the same pattern on a smaller scale (6 duplicate `(model,condition)` pairs); `results/finetune_birdclef.csv` and `results/stage5_lora_finetune_mimii.csv` are clean (0 duplicates) -- this is specific to models/datasets that got resubmitted after a bugfix, not universal.
- **Recomputed the correlation two ways to test whether this contamination actually reached the logged number**: (a) naive -- average every row for a `(model,condition)` including all duplicates (what a plain `groupby().mean()` would do, blending pre-fix and post-fix accuracy together), vs (b) correct -- deduplicate to the last occurrence per `(model,condition,fold)` before averaging (the chronologically-correct, post-fix-only value). Recomputed uniformity directly from `$SCRATCH/audio_comp/embeddings/*.npz` for the 14 LoRA-covered models (`audio_comp.geometry.alignment_uniformity.uniformity_score`, unmodified) since no uniformity CSV was ever persisted.
  - **Naive (contaminated): rho=0.710, p=0.0045. Correct (deduplicated): rho=0.798, p=0.0006.** The deduplicated number matches the logged Finding 5 number (+0.798, p=0.001) to 3 decimal places -- the naive/contaminated version does not.
  - **Verdict: case (i) applies.** The 2026-08-18 "continued 3" correlation was computed from the corrected, post-fix, deduplicated UrbanSound8K data, not the corrupted blend -- nothing to redo, Finding 5's headline UrbanSound8K number stands as logged. This wasn't verifiable from any saved script (the original analysis was ad hoc, per its own 2026-08-18 entry, and no script/notebook survived it) -- inferred by matching the recomputed number against the logged one, which lines up too precisely (3 decimal places on both rho and p) to be coincidental.
  - FMA-genre spot-checked the same way: naive and dedup give **identical** rho=0.534, p=0.049 (its duplicate pairs happen to be close enough that Spearman's rank order doesn't move) -- also unaffected, also confirmed rather than assumed.
- **Standing risk, not yet fixed, flagged for whoever next touches `generic_lora_trainer.py`'s results writer**: the append-only write pattern means *any* future rerun of a model already in one of these result CSVs will silently reintroduce this exact contamination, and the fact that it happened to wash out here (dedup matched what was actually used) was verified after the fact, not designed in. A `pd.read_csv(...).groupby([...]).mean()` -- a completely natural thing to write in a fresh session with no memory of this quirk -- would silently blend stale and corrected rows again. Worth a real fix (overwrite-by-model-before-append, or a documented required dedup-to-last-occurrence step before any aggregation) before this bites a future analysis that doesn't get audited as carefully as this one was.

## 2026-08-19 (cont'd) -- fixed the append-only duplicate-row risk before starting LibriSpeech/AMI-SingVERSE

- **Correction to the entry above**: the append-only write lives in each `finetune_*.py` script's own `main()` (`open(out_csv, "a", ...)`), not in `generic_lora_trainer.py` itself -- `generic_lora_trainer.py` only owns the training loop, each dataset wrapper does its own results-file I/O. Noted since the prior entry mischaracterized where the fix needed to go.
- **Per direct user instruction, fixed this now rather than deferring it** -- LibriSpeech and AMI/SingVERSE are about to generate new results with no prior logged number to cross-check a recompute against (unlike UrbanSound8K, which only got caught because there was something to compare against). Added `write_results_idempotent(out_csv, header, key_cols, new_rows)` to `generic_lora_trainer.py`: reads the existing file (if any), rewrites it with any row matching `key_cols` replaced rather than duplicated, appends genuinely new keys. Applied to all 4 scripts that import `generic_lora_trainer` (the only ones that could plausibly run again soon): `finetune_urbansound8k.py`, `finetune_fma_genre.py`, `finetune_birdclef.py`, `finetune_librispeech.py` -- each now calls the shared helper instead of its own `open(..., "a")` block. Syntax- and import-checked all 5 touched files.
- **Deliberately did not touch** `stage5_lora_finetune*.py` (MIMII, DeepShip), `run_all_vessel_experiments.py`, or `rsa_cka_vessel.py` -- none of them import `generic_lora_trainer.py` (they predate it and have their own independent write blocks with the identical `"a"`-mode pattern), and `generic_lora_trainer.py`'s own docstring already states a deliberate prior decision not to retroactively refactor those scripts ("they work, touching them isn't worth the risk/time right now"). They carry the same latent risk -- explicitly flagged here as unresolved, not silently left out, and MIMII/DeepShip are largely closed-out domains rather than ones about to generate fresh reruns.
- **Cleaned the two currently-contaminated files in place** using the same helper (last-occurrence-wins, the semantics already verified as correct against real `sacct` job timestamps in the entry above): `results/finetune_urbansound8k.csv` 465 -> 235 rows (0 duplicate keys remaining, was 223 differing + 2 identical), `results/finetune_fma_genre.csv` 52 -> 46 rows (0 duplicate keys, was 6). Re-ran the uniformity-vs-gain correlation against the cleaned file: **rho=0.7978, p=0.000628** -- matches the pre-cleanup deduplicated computation and the originally logged number exactly, confirming the cleanup didn't change anything the analysis already accounted for, it just removed the latent trap for whoever reads the file next without knowing to dedup first.
