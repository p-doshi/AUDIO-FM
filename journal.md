## 2026-08-08

- Repo scaffolding started. `git init` run in `/home/pdoshi/audio_comp` (was not a repo before).
- Found empty `JORUNAL.md` (misspelled, empty) already present — replaced with this correctly-named `journal.md` per CLAUDE.md's mandatory logging convention.
- Confirmed environment: running on the `fir` cluster (Digital Research Alliance of Canada / Compute Canada). Slurm accounts available: `def-spadon_cpu`, `def-spadon_gpu`. GPU partitions available (`gpubase_bygpu_*`, `gpupreempt`, etc.), so Phase 1 embedding extraction will run as Slurm jobs distributed across GPUs, not interactively.
- `$HOME` quota is small (48GiB, ~10GiB already used) — code/venv only. `$SCRATCH` (/scratch/pdoshi) has 19TiB — checkpoints, downloaded audio, and embeddings/RDMs go there, in a project-specific subdir kept separate from the existing `/scratch/pdoshi/my_project/eddyflow` (unrelated project, out of scope per CLAUDE.md).
- Decision: probe-set audio will be sourced from public datasets (general-audio + vessel/underwater) rather than assumed to exist locally — nothing downloaded yet, composition to be proposed and confirmed before pulling data.
- No Phase 1 code written yet. Next steps: propose concrete probe-set composition (datasets, size, licensing) and set up the Python env (venv + module loads) before any checkpoint downloads or Slurm submissions.
- User clarified goal: this repo should be a reusable "go-to" framework for cross-model audio FM comparison, not a one-off script collection. Confirmed rollout: start with music + speech (in-distribution for most candidate teachers), then add bird sounds, ship/vessel sounds, city/urban noise (cars, construction). Pilot size: 20 clips/category, scaling to thousands/category later.
- Ran a research pass (web search + direct fetch of two user-supplied URLs) to verify current real availability of CLAUDE.md's candidate teacher models before committing to any of them, per CLAUDE.md's explicit instruction. Findings that update the original table:
  - **A-JEPA (original paper, Fei/Fan/Huang, arXiv 2311.15830) has no public checkpoint anywhere** — paper-only. User confirmed (after reviewing huggingface.co/ltuncay/Audio-JEPA and github.com/LudovicTuncay/Audio-JEPA) to substitute `ltuncay/Audio-JEPA` (Tuncay et al., ICME 2025, MIT license) — an independently-built, similarly-named JEPA-style audio model, NOT the original A-JEPA. Labeled as a substitute everywhere it's referenced.
  - BEATs has no native `transformers.from_pretrained` path (manual checkpoint download + microsoft/unilm loader code).
  - MusicFM likewise needs the upstream repo's own loader code, not a plain `AutoModel` call — but its loading API (`MusicFM25Hz(stat_path=..., model_path=...)`, `.get_latent(wav, layer_ix=7)`) is a plain, self-contained `nn.Module`, so a real working adapter was written for it (see below), unlike Audio-JEPA/BEATs.
  - Microsoft CLAP needs the separate `msclap` pip package, not `transformers` — dropped in favor of LAION CLAP (`laion/larger_clap_general`), which is fully HF-native and covers the same contrastive-paradigm slot.
- Inspected the actual upstream repos (cloned to scratch, read source) for MusicFM and Audio-JEPA before writing adapters, rather than guessing at loader APIs:
  - MusicFM (`github.com/minzwon/musicfm`): straightforward — wrote a real, working adapter (`audio_comp/models/musicfm.py`) plus `scripts/setup_musicfm.sh` to clone the repo and wget the MSD-trained checkpoint.
  - Audio-JEPA (`github.com/LudovicTuncay/Audio-JEPA`): its checkpoint is a PyTorch Lightning `JEPAModule` whose `encoder`/`predictor`/`criterion` constructor args are Hydra-instantiated objects explicitly excluded from saved hyperparameters (`save_hyperparameters(..., ignore=[...])`), so `load_from_checkpoint(ckpt_path)` alone won't work — the ViT-Base encoder needs to be reconstructed via the repo's Hydra configs first. Decided NOT to write untested loader code for this without a venv to verify against — left as a documented `NotImplementedError` stub (`audio_comp/models/audio_jepa.py`) with the exact integration path spelled out, same treatment as `beats.py`.
- Built out the full repo scaffold as a registry/adapter-pattern framework (not per-model scripts): `audio_comp/models/` (BaseAudioEncoder + registry + one adapter per model), `audio_comp/data/` (BaseDatasetSource + registry + one source per dataset + `build_probe_set.py`), `audio_comp/geometry/` (rdm.py, rsa.py, cka.py, intrinsic_dim.py via `skdim`'s TwoNN), `audio_comp/pipelines/` (`extract_embeddings.py` — one model at a time, `compare_models.py` — RSA/CKA matrices + heatmaps), `configs/{models,categories}.yaml`, `scripts/slurm/` (one sbatch job per model, submitted in parallel — this is the "distributed across GPUs" piece), `tests/`, `README.md`.
- Probe-set category sources picked for license cleanliness + HF-native availability where possible: music=FMA-small (official release, not the questionable HF mirrors), speech=Multilingual LibriSpeech English (Common Voice was ruled out — pulled from HF in Oct 2025, now Mozilla-Data-Collective-only), bird_sounds=BirdSet HSN eval config (avoiding its CC-BY-NC training subset), ship_vessel=DS3500 (ShipsEar-derived), city_noise=UrbanSound8K HF mirror.
- Security note: user pasted their HF API token directly into chat despite an earlier warning not to. Wrote it straight to `~/.cache/huggingface/token` (mode 600, standard `huggingface-cli login` cache location) without echoing it back, and told the user to rotate/revoke it since it's now in plaintext conversation history.
- Set up dedicated `~/audio-comp-venv` (python/3.11 module + cuda/12.6), deliberately NOT reusing the existing `eddyflow-venv` (oceanography deps, no transformers/librosa/soundfile) per CLAUDE.md's own instruction not to conflate the two projects. `pyproject.toml` added so `pip install -e .` handles all deps except torch/torchaudio/torchvision (installed separately via `pip install --no-index` from the Compute Canada wheelhouse, matching the convention already used by `eddyflow-venv`/`glorys-dl-venv`).
- Two env-setup snags, both fixed:
  - `skdim` isn't the PyPI package name — the actual package is `scikit-dimension` (still imports as `skdim`). `pyproject.toml` fixed.
  - `pip install -e .` failed building `pyarrow` (a `datasets` dependency) with Compute Canada's dummy `pyarrow-noinstall` wheel, which deliberately errors and tells you to `module load gcc arrow/x.y.z` *before* activating the venv rather than pip-installing pyarrow. Loaded `gcc arrow/25.0.0` and retried — installed clean. Confirmed by testing `import datasets` with and without the arrow module loaded that this isn't just an install-time requirement — the arrow module must be loaded at runtime too (pyarrow comes from the module's PYTHONPATH injection, not site-packages). Updated `extract_embeddings.sbatch` and README accordingly so this doesn't bite again in a Slurm job.
- Full venv install succeeded (torch 2.11.0+computecanada, transformers 5.14.1, datasets 5.0.1, etc.). All 17 non-skipped tests in `tests/` pass (`test_rsa_cka.py` fully; `test_model_adapters.py`'s registry/contract/deferred-model checks — the 6 real load+embed smoke tests are opt-in via `RUN_MODEL_SMOKE_TESTS=1` since they'd download real HF checkpoints). Verified all pipeline modules import cleanly and both registries populate (5 dataset sources, 8 models incl. the 2 deferred stubs).
- Not done yet, deliberately: no probe-set data actually downloaded/built, no embeddings extracted, no Slurm jobs submitted. Per the approved kickoff plan, next actual run is: `python -m audio_comp.data.build_probe_set` (pulls the 100-clip pilot, requires `scripts/download_fma_small.sh` and `scripts/setup_musicfm.sh` run first), then `scripts/slurm/submit_all.sh`, then `compare_models.py` — this is the real Phase 1 kickoff deliverable and hasn't happened yet.
- Pushed the repo to `github.com:p-doshi/AUDIO-FM` (user-provided remote, pushed over SSH — an existing key on this machine was already registered with their GitHub account). Before staging anything, found `.claude/settings.local.json` contained the (now-rotated) HF token in plaintext via an auto-approved Bash permission rule — added it to `.gitignore` before the first commit so it was never staged. User also moved their new HF token into `.env`, which was already gitignored.
- Ran the pilot for real and fixed everything that broke — none of this was visible from reading the code, only from executing it:
  - `facebook/multilingual_librispeech` has no "english" config → swapped to `openslr/librispeech_asr`.
  - `DBD-research-group/BirdSet` only loads via a legacy Python loading script; `datasets>=5` dropped script-based loading entirely (no version-pinning fix exists) → swapped to `ashraq/esc50` filtered to `chirping_birds`/`crow` classes.
  - `peng7554/DS3500`'s embedded split metadata doesn't match its actual data → load with `verification_mode="no_checks"`.
  - `datasets>=5` needs `torchcodec` to decode streamed Audio columns → added to `pyproject.toml`.
  - `m-a-p/music2vec-v1`'s published `config.json` has `vocab_size` set to a filesystem path (an artifact of the original author's machine) instead of an int; newer `transformers` validates config field types strictly and rejects it (older versions silently ignored the mismatch) → copy the snapshot to a mutable local dir and patch just that field before loading.
  - CLAP's processor API changed in the installed `transformers` (5.14.1): `audios=` kwarg renamed to `audio=`, and `get_audio_features()` now returns a `BaseModelOutputWithPooling` instead of a raw tensor (embedding is in `.pooler_output`) → fixed.
  - fir's Slurm requires an explicit GPU type (`--gres=gpu:1` alone is rejected) and the type:count gres syntax needs an explicit trailing count → `--gres=gpu:nvidia_h100_80gb_hbm3_1g.10gb:1` (smallest H100 MIG slice — plenty for single-clip inference, not training).
  - `$HOME`'s 48GB quota was nearly exhausted (18GB of it was the HF cache) → moved the cache to `$SCRATCH`, set `HF_HOME` there in the sbatch script.
  - **hubert/mert intermittently deadlocked** during `from_pretrained()` — confirmed via `/proc/<pid>/status` on the actual compute node that the process was alive but blocked on `futex_do_wait` with near-zero CPU (one run completed in 3.6s, others burned the full 1hr time limit with zero progress). This is the known tokenizers/hf_xet Rust-thread-pool-after-fork deadlock. Fixed with `TOKENIZERS_PARALLELISM=false`, `HF_HUB_DISABLE_XET=1`, `RAYON_NUM_THREADS=1`. Note: initially misattributed this to generic cluster load (login node load average was genuinely ~7000 with 131+ users, which was real but was a red herring for this specific bug) — user correctly pushed back ("are all the gpus busy? seems unlikely"), which prompted actually inspecting the live process instead of continuing to guess.
  - Even after that fix, mert hit a *different*, genuine issue: a flaky network transfer of its 1.26GB checkpoint (connection dropped at ~10%, resume got stuck for the rest of the hour). Decoupled the download from the GPU job's time budget by pre-fetching the checkpoint on the login node with a retry loop first (succeeded in 5 min), then resubmitted the extraction job against the warm cache.
- **Phase 1 pilot result** (100 clips, 20/category, 6 models — clap, hubert, mert, music2vec, musicfm, wav2vec2; audio_jepa and beats still deferred): RSA matrix in `results/rsa_matrix.csv`, CKA in `results/cka_matrix.csv`, TwoNN intrinsic dimension in `results/intrinsic_dimension.csv`, heatmaps as PNGs. Notable pattern: same-paradigm-and-domain pairs show strong agreement (hubert↔wav2vec2, both speech masked-modeling: RSA 0.80, CKA 0.83; mert↔musicfm, both music masked-modeling: RSA 0.49, CKA 0.52), while cross-domain/cross-paradigm pairs are weak (mostly RSA 0.1–0.3). music2vec (the sole active JEPA-family model) doesn't correlate strongly with anything (max RSA 0.25, with musicfm) — consistent with CLAUDE.md's decision-rule outcome 2 (partial/cluster agreement) rather than outcome 1 or 3, but **this is a 20-clip/category pilot, not a real Phase 1 read** — CLAUDE.md is explicit that small-probe-set results are preliminary. The actual decision-rule call needs the full-scale run (thousands/category) before Phase 2 is even discussed.

## 2026-08-08 (cont'd) — scaling to 2000/category

- User asked to scale the probe set to 2000 clips/category (10,000 total, 100x the pilot). Checked feasibility per source before running anything: `esc50_birds` (the pilot's bird source) only had 80 bird-related clips total across its two closest ESC-50 classes — a hard blocker, not a "might be tight" case. Also flagged LibriSpeech `test` split (2,620 utterances) as uncomfortably close to the 2000 target once short utterances get filtered.
- Replaced `esc50_birds` with `tglcourse/5s_birdcall_samples_top20` (~9,595 pre-segmented 5s clips, script-less/Parquet-native, 20 species) — found via a research pass that verified candidates against the live HF datasets-server API rather than going from memory (dataset hosting formats change; a dataset being script-less today doesn't mean it stays that way). License is unverified/mixed (Xeno-Canto-derived, "Unknown" on the card) — documented as such rather than assumed.
- Switched LibriSpeech split from `test` to `train.100` (28,539 utterances) for headroom — a probe set isn't a train/test split of anything, so this doesn't matter beyond volume.
- Bumped Slurm job time limit 1hr → 3hr for the 100x larger per-model workload.
- Hit three more real bugs only visible at this scale, none visible at 20 clips:
  - FMA-small has a handful of corrupted mp3s (documented upstream) — pilot got lucky, 2000-scale didn't. Fixed by skipping `librosa.load` failures instead of crashing.
  - DS3500's *prepared* Arrow cache (materialized before the earlier `$HOME`→`$SCRATCH` HF cache migration) had absolute paths baked in pointing at the old, now-empty `$HOME` location — the raw hub blobs moved correctly but the derived cache didn't get its internal references updated. Cleared the stale prepared caches for DS3500 and UrbanSound8K (both touched pre-migration; the new bird source was unaffected since it was first loaded post-migration).
  - DS3500 also had at least one genuinely corrupted/undecodable audio entry (torchcodec decode failure) — same class of issue as the FMA one. Factored the skip-on-decode-failure pattern into a shared `iter_hf_audio_clips()` helper (`audio_comp/data/sources/_util.py`) used by ds3500/urbansound8k/xeno_canto_birds, rather than tripling the same fix by hand.
- Built the full 10,000-clip manifest (2000/category × 5) cleanly on the first fully-fixed attempt. All 6 extraction jobs completed on their **first submission** this time — no repeat of the earlier hubert/mert deadlock (the env-var fix held) or network flakiness.
- **Phase 1 result at 2000/category** (10,000 clips total): pattern from the pilot holds and tightens with 10x less sampling noise:
  - RSA: hubert↔wav2vec2 (speech, masked-modeling) = 0.80; mert↔musicfm (music, masked-modeling) = 0.51. Cross-domain/cross-paradigm pairs stay weak, mostly 0.02–0.36.
  - music2vec (sole active JEPA-family model) still doesn't correlate strongly with anything — max RSA 0.25 (with musicfm), same as pilot.
  - Notable RSA/CKA divergence for clap↔musicfm: RSA=0.36 but CKA=0.67 — a concrete illustration of CLAUDE.md's warning that CKA is gameable and shouldn't be sole evidence; the two metrics disagree substantially for this pair specifically.
  - TwoNN intrinsic dimensions all scaled up vs. the pilot (expected — sparse pilot data underestimates true manifold dimension), relative ordering roughly preserved: wav2vec2/mert lowest (~11.5), music2vec highest (~21.5).
  - Reading against CLAUDE.md's decision rule: still outcome 2 (partial/cluster agreement — paradigm+domain-matched pairs agree, others don't), same as the pilot suggested, now on a much more credible sample size. **This is still not a final call** — CLAUDE.md requires a written summary of which outcome was reached before any Phase 2 code, and that's a decision for the project owner, not something to charge into unprompted.
- User pushed on music2vec specifically: two runs (20/category and 2000/category), same result — it doesn't correlate with any other teacher (max RSA 0.25). That's ambiguous by itself: could be idiosyncratic-but-real structure, or a collapsed embedding space that trivially fails to correlate with anything because it doesn't encode anything. Wrote `audio_comp/pipelines/inspect_geometry.py` to check directly — does music2vec's own RDM show sensible within vs. between-category structure, independent of cross-model agreement.
  - Result: NOT collapsed. Within-category distances are lower than between-category for all 5 categories (separation index 0.54; hubert as a reference point scores 1.02, ~2x higher, but hubert's number is driven almost entirely by a huge speech-vs-everything-else gap — more "speech detector" than fine-grained structure — so the raw magnitude gap isn't as damning as it first looks). Pairwise-distance CV=0.43 is nowhere near the ~0 a collapse signature would show. Combined with music2vec already having the *highest* TwoNN intrinsic dimension of all 6 models (21.5, vs. ~11–17 for the rest) — collapse shows up as *low* dimension, not high — this rules out collapse. music2vec organizes the probe set sensibly, just differently from the hubert/wav2vec2 and mert/musicfm clusters. Worth stating explicitly in any write-up: "doesn't cluster with the consensus" and "doesn't work" are different claims, and this data supports the former.

## 2026-08-09 — paradigm correction: music2vec is data2vec-family, not JEPA

- **Decision reversed.** music2vec has been labeled "JEPA-family" since the very first CLAUDE.md draft and carried that framing through every turn of Phase 1 analysis so far, including treating its cross-model RSA isolation as informative about H1 ("JEPA-family models agree with each other more than with others"). User caught this and walked through the actual architectural distinction: data2vec-style models (music2vec) have a student encoder that operates directly on masked input and predicts the EMA teacher's averaged top-K layer representations — no separate predictor network. JEPA as specified (and as A-JEPA/Audio-JEPA's own methods sections describe it) has three components: context encoder, EMA-updated target encoder, and a *separate predictor network* conditioned on the context representation. Both lineages descend from BYOL and are self-distillation-with-EMA-teacher, but the decoupled predictor is the real dividing line, and music2vec doesn't have one.
- **Practical consequence**: as of this correction there are zero working JEPA-family models in the active comparison. `audio_jepa` (the `ltuncay/Audio-JEPA` substitute, still deferred pending its Hydra-config loader work) will be the *first* JEPA-family data point once wired up, not a second one to compare against music2vec — H1 as originally phrased (mutual agreement *among* JEPA-family models) needs two working checkpoints and isn't testable yet.
- **Corrected everywhere the mislabel appeared**: `CLAUDE.md`'s candidate-model table and H1 statement (both annotated in place with correction notes, not silently rewritten), `audio_comp/models/music2vec.py`'s `ModelInfo.paradigm` field and docstring, `README.md`'s model table. Did NOT rewrite earlier journal entries above that say "JEPA-family" for music2vec — journal is append-only per CLAUDE.md's own convention; this entry is the correction of record.
- **New confound documented for future work**: `ltuncay/Audio-JEPA` is trained on meaningfully less compute than the other active teachers (100k steps, ~14h on 4 V100s, 5,338h AudioSet vs. wav2vec2/data2vec's 400k steps on larger batches), and its own paper reports it substantially underperforming both baselines on several linear-probe tasks (e.g. Speech Commands V1: 0.152 vs. data2vec's 0.927). If it comes back RSA-isolated once wired up, that's confounded between "JEPA-paradigm geometry is genuinely different" and "this checkpoint is comparatively undertrained" — RSA alone can't tell them apart. Documented directly in `audio_comp/models/audio_jepa.py`'s docstring so it's visible right where the loader gets implemented.
- **A specific, testable prediction for later**: the Audio-JEPA paper states its objective favors embedding cohesion over linear separability (strong kNN, weak linear probe on the same tasks) — predicting a *low* TwoNN intrinsic dimension and/or unusually tight within-category clustering once run through `inspect_geometry.py`. This is the opposite direction from music2vec, which had the *highest* ID of the six active models. A low-ID/high-cohesion result for `audio_jepa` would be real, paper-grounded evidence for paradigm-driven geometry differences — meaningfully stronger than an unexplained isolated RSA number. Not run yet — `audio_jepa` still needs its loader wired up first.

## 2026-08-09 (cont'd) — audio_jepa wired up, cohesion prediction confirmed

- User: "we will still treat the second version of audio-jepa as a JEPA model, lets see how it turns out. proceed." Wired up the real loader rather than the originally-assumed Hydra/Lightning reconstruction path — the HF repo (`ltuncay/Audio-JEPA`) ships an author-provided `inference_example.py` alongside `config.json`, both first-party and mutually corroborating every architecture constant. Loads `VisionTransformer` standalone, restores only the `encoder.*` keys from `JEPA.ckpt`'s state dict (predictor + target_encoder discarded per the model's own config.json: "Only the encoder is used downstream"), `load_state_dict(strict=True)` — 0 missing, 0 unexpected keys.
- `vision_transformer.py`'s `Block` unconditionally imports `flash_attn.modules.mha.MHA` (compiled CUDA extension) regardless of `use_flash_attn`. Ported the HF repo's own portable shim: a `torch.nn.functional.scaled_dot_product_attention`-based module matching the checkpoint's `qkv`/`proj` parameter names — no flash-attn build needed, and SDPA gets CUDA-optimized kernels automatically on GPU (not just a CPU fallback).
- **Self-inflicted regression caught and fixed**: before finding the shim, tried installing the real `flash_attn` package to see if that path was simpler. `pip install flash_attn` silently downgraded `torch` 2.11.0 → 2.9.1 as a dependency-resolution side effect (flash_attn's wheel required `torch~=2.9.0`), breaking ABI compatibility with `torchaudio`/`torchcodec`/`torchvision` — would have quietly broken every other model's pipeline. Caught via the next adapter test failing on an unrelated `torchaudio` import error, traced to the version bump, fixed by uninstalling `flash_attn` (unneeded — the shim replaces it) and reinstalling `torch==2.11.0` exactly. Full test suite green afterward.
- **Second real bug, only visible at full scale**: `kaldi.fbank`'s fixed ~98ms analysis window needs the input waveform at least that long; some real probe-set clips (brief UrbanSound8K events, one as short as ~60ms) are shorter, crashing the first full Slurm run. The CPU smoke test before that used a 5s dummy waveform and didn't catch it. Fixed by padding short waveforms to the model's designed 10s input before spectrogram computation.
- Moved `audio_jepa` from `deferred_models` to `active_models`; updated `test_model_adapters.py`'s `DEFERRED_MODELS` (now just `beats`) accordingly. All 7 models now extracted on the full 2000/category (10,000-clip) probe set.
- **Result — the cohesion prediction held, on both fronts specified in advance**:
  - TwoNN intrinsic dimension: **10.82, the lowest of all 7 models** (range 11.5–21.5 for the rest; music2vec highest at 21.5 — opposite end from audio_jepa).
  - `inspect_geometry.py`: within-category mean distance **0.062**, far tighter than hubert (0.256) or music2vec (0.161) — driven by bird/city/ship/speech clustering extremely tightly (diagonal 0.02–0.06) while music sits as a stark outlier (0.42–0.50 from every other category, including its own category average of 0.18). Separation index 1.36, highest of the three models checked this way.
  - This is a real, paper-grounded signature (low ID + tight within-category clustering matching the paper's own stated kNN-vs-linear-probe cohesion claim), not an unexplained number — the disambiguation this project's own tooling was built to enable.
- **Unlike music2vec, audio_jepa is NOT RSA-isolated from the other models.** RSA: audio_jepa↔clap = 0.54 (the single strongest pair in the entire 7-model matrix, edging out mert↔musicfm's 0.51), ↔musicfm = 0.42, ↔mert = 0.24, ↔music2vec = 0.20, ↔hubert = 0.02, ↔wav2vec2 = **-0.08** (essentially zero/slightly negative). CKA shows the same pattern (clap 0.50 highest, wav2vec2 0.13 lowest). So: audio_jepa aligns moderately with the music/general-audio cluster (clap, musicfm, mert) while being essentially decorrelated from the two speech-specialized masked-modeling models — a genuinely different relationship to the rest of the roster than either music2vec's flat isolation or hubert/wav2vec2's tight mutual pairing.
- H1 as originally phrased (mutual agreement *among* JEPA-family models) is still not testable — there is exactly one working JEPA-family model (`audio_jepa`) in the comparison, not two. But that one data point now has a distinctive, paper-predicted, non-confounded-looking signature (the compute/undertraining confound doesn't obviously explain a *low*-ID, high-cohesion result the way it might explain a merely-isolated one) worth carrying into any write-up as a real finding, not just a caveat.

## 2026-08-09 (cont'd) — extended roadmap adopted (Stage 0–6)

- User provided a full staged roadmap taking the project from the current 7-model Phase 1 result through to a capstone predictive diagnostic, restructuring what "Phase 2" means for this project (the original CLAUDE.md sketch — consensus-distilled JEPA vs. single-teacher-distilled vs. from-scratch — is superseded by a more granular staged plan). Folded into `CLAUDE.md` as a new "Extended roadmap" section rather than silently overwriting the original Phase 1/Phase 2 text, per the project's own convention of discussing scope changes and stating assumptions explicitly.
- Stage summary (full detail in CLAUDE.md): Stage 1 consolidates current findings cheaply (downstream linear-probe/kNN validation on existing embeddings, a training-distribution-breadth reframing of the RSA matrix, a written decision-rule outcome). Stage 2 expands the model roster to fill specific missing axes (reconstruction-target, supervised-vs-self-supervised, domain-specific bioacoustic, discrete-token) rather than just adding count. Stage 3 re-runs the full toolkit at ~11 models — the point CLAUDE.md now marks as the Datasets & Benchmarks-track publishable floor. Stage 4 repeats the downstream validation at that scale. Stage 5 is the harder empirical claim: OOD fine-tuning on real vessel/underwater-acoustic data (the project's actual eventual application domain), testing whether frozen-representation geometry predicts adaptability — explicitly flagged as a different object from Aghajanyan/Gupta/Zettlemoyer's fine-tuning parameter-update intrinsic dimension, same name, not to be conflated. Stage 6 (only if Stage 5 finds a real correlation) is a lightweight reusable diagnostic tool — the actual novelty ceiling beyond a benchmark report.
- Not started yet. Asked the user how they want to kick off Stage 1 given the size of the implementation (downstream probe protocol needs concrete decisions — exact X-ARES-matching methodology, train/test split, etc. — that are worth confirming before building rather than guessing and redoing).

## 2026-08-10 — Stage 1 X-ARES run complete: geometry doesn't predict downstream accuracy

- User chose real X-ARES toolkit integration (not a self-built lightweight probe) for genuine comparability to published numbers. Built `xares_eval/`: one encoder wrapper per model (reusing each audio_comp adapter's loading logic but returning unpooled frame-level embeddings, since X-ARES and the RDM pipeline want different things from the same model), validated all 7 via `check_audio_encoder()`.
- User chose the full LibriSpeech-ASR task (WER, needs a Qwen2-0.5B decoder) over the simpler LibriSpeech-Male-Female classification task when the two options were surfaced — heavier engineering, more informative about actual speech capability.
- Several real infrastructure bugs found and fixed, none visible until actually running the toolkit:
  - `xares==0.1.3`'s `download_zenodo_record` always uses Zenodo's bulk "files-archive" endpoint, which Zenodo hard-rejects for any record over 300MB. All three overlapping-task datasets (FMA ~8GB, UrbanSound8K ~7.1GB, LibriSpeech ~7.5GB) hit this immediately — not ours-specific, likely breaks for most real users of this toolkit. Wrote `xares_eval/download_zenodo_task.py`: fetches files individually via Zenodo's per-file API (no size cap there), then plants the same marker files the real downloader checks for.
  - `attr_from_py_path` (X-ARES's encoder/task loader) does a real `import_module()` on the path with `/`→`.` substitution — absolute paths become invalid leading-dot relative imports. `xares_eval` had to become a real importable package, and task/encoder CLI args have to stay relative to CWD.
  - `TaskConfig.env_root` defaults to `"./env"` (CWD-relative, no env-var override) — first attempt put ~15GB combined under `$HOME` and blew the 48GB quota outright (same class of issue as the earlier HF-cache incident, still hasn't fully stopped recurring). Wrote thin `xares_eval/tasks_scratch/` wrapper task files that call the real (external, unmodified) config functions and override `env_root` to an absolute `$SCRATCH` path before returning.
  - `download_hf_model_to_local`'s Qwen2 dependency downloads to CWD with no override — landed inside the repo when run from the repo root (required for the point above), got swept into a `git add -A` and rejected by GitHub outright (942MB safetensors file over the 100MB limit). Caught before it reached the remote (push failed, so only local); `git reset --soft` + gitignored `/Qwen/` and recommitted clean.
  - LibriSpeech-ASR's Qwen2 decoder OOM'd on the same 10GB MIG slice the other 6 extraction jobs used fine — bumped 10GB→20GB→40GB (20 was still short by ~3GB during backward pass; genuinely memory-hungry combination, not a bug).
  - mert and wav2vec2 both hit the initial 4hr time limit mid-training (mert at epoch 6/10, wav2vec2 at epoch 10/10 — both just needed more time, not stuck); bumped default to 6hr and both finished cleanly on resubmission.
- **Validation against published numbers**: hubert's MLP/kNN scores on FMA-genre and UrbanSound8K land within a few points of X-ARES's own published wav2vec2 baseline (FMA MLP 0.482 vs. published 0.469; UrbanSound8K MLP 0.682 vs. published 0.659) — real evidence the encoder-wrapper interface and frame-level extraction are correct, not just "it ran without crashing."
- **Full results** (MLP scores, `results/xares_results.csv`):

  | model | FMA (music) | UrbanSound8K | LibriSpeech-ASR |
  |---|---|---|---|
  | clap | 0.687 | 0.888 | 0.000 |
  | musicfm | 0.615 | 0.799 | 0.000 |
  | mert | 0.570 | 0.774 | 0.000 |
  | audio_jepa | 0.552 | 0.556 | 0.000 |
  | music2vec | 0.521 | 0.601 | 0.020 |
  | hubert | 0.482 | 0.682 | 0.855 |
  | wav2vec2 | 0.185 | 0.226 | 0.000 |

- **The Stage 1 question this was actually for — does geometry predict downstream function — has a clear answer: no, not straightforwardly.** audio_jepa (lowest TwoNN intrinsic dimension, tightest within-category clustering, per the 2026-08-09 cohesion finding) is only mid-pack on both classification tasks, not the best. CLAP — mid-range intrinsic dimension (12.3), not the tightest cluster — is the *best* classifier on both FMA and UrbanSound8K by a clear margin. music2vec has the *highest* intrinsic dimension of all 7 (least "cohesive" by that measure) but isn't the worst classifier either. Cohesion/low-ID is a real, paper-predicted geometric signature (confirmed 2026-08-09) — it just doesn't translate into downstream classification advantage in this data. This is a legitimate, reportable null result for the "geometry predicts function" hypothesis at this stage, exactly the kind of falsifiable check the roadmap called for — not something to force a positive spin onto.
- **Open item, not chased further tonight**: LibriSpeech-ASR returns exactly 0.0 for every model except hubert (music2vec: 0.02, barely above). Per-model this is mostly explicable (clap has no frame sequence to give the ASR decoder; musicfm/mert/music2vec are music-only trained, no speech exposure; audio_jepa's wrapper hard-truncates to its fixed 10s training window, losing the tail of longer LibriSpeech utterances). wav2vec2 is the one case without a clean explanation — same Libri-light-family training domain as hubert, finite/reasonable training loss throughout (no NaN), yet 0.0 vs. hubert's 0.855, a much starker gap than its milder-but-real underperformance on the two classification tasks (0.18–0.23 vs. hubert's 0.48–0.68). Checked and ruled out a normalization-config mismatch (`do_normalize=True` correctly paired with `feat_extract_norm=layer`). Most likely explanation on the table: wav2vec2's final transformer layer is known in the literature (SUPERB-style per-layer probing studies) to be less linearly-probe-friendly than HuBERT's — but that would predict *degraded*, not *exactly-zero*, performance, so this is flagged as genuinely unresolved rather than confidently explained. Worth a targeted look if the LibriSpeech-ASR number specifically becomes analytically important later; doesn't affect the two classification-task results, which are the ones validated against published baselines.

## 2026-08-10 (cont'd) — refining the null result before treating it as final

- User pushed back on the "geometry doesn't predict function" framing before letting it stand: (1) check whether X-ARES's kNN read (not just MLP) tells a different story for audio_jepa, since the source paper's own claim is specifically a kNN-vs-linear-probe split, not a flat "worse at everything"; (2) CLAP winning deserves a real hypothesis (discriminative/contrastive pretraining pressure, à la Wang & Isola alignment/uniformity), not just a note, and a silhouette-score proxy on the RDMs already computed would test it cheaply; (3) wav2vec2's LibriSpeech-ASR zero, given how architecturally close it is to hubert (their tightest RSA pair, 0.80), smells more like a wiring bug than a real result, worth an actual diff before shelving. Right call on all three — the flat null was underspecified.
- **(1) kNN vs MLP rank check**: audio_jepa's *relative rank* improves under kNN vs. MLP on both X-ARES tasks — FMA 4th→3rd (passes mert), UrbanSound8K 6th→5th (passes hubert). Real and direction-consistent with the paper's cohesion/separability split, though modest — not a reversal, audio_jepa doesn't become a top performer under kNN either. The kNN/MLP *ratio* tells a more complicated story: CLAP has the highest ratio of all 7 (0.94 avg — its kNN score is almost as good as its trained MLP score), not audio_jepa (0.69, 3rd). A high ratio means kNN captures most of what training can achieve — CLAP isn't trading cohesion for separability, it's winning at both simultaneously, which argues against reading the ratio itself as a clean cohesion indicator.
- **(2) Silhouette score on our own probe set** (not X-ARES's — the 5-category, 10,000-clip RDM data from Stage 0): **audio_jepa has by far the highest silhouette of all 7 models (0.602 — nearly double CLAP's 0.313, which comes 2nd)**. This is a much sharper version of the 2026-08-09 `inspect_geometry.py` finding (separation index 1.36, highest of the three checked then) now confirmed against all 7 models with a standard metric. So on *coarse* 5-domain discrimination (music/speech/bird/ship/city), audio_jepa's cohesion advantage is real and dominant, not marginal.
  - Correlated this silhouette ranking against X-ARES's average MLP score (FMA+UrbanSound8K) across all 7 models: **Spearman rho = 0.357, p = 0.432 — weak and not statistically significant** (n=7, so likely underpowered, but the direction and magnitude are informative regardless). audio_jepa is the clearest violator: #1 on coarse silhouette, only 6th of 7 on average X-ARES MLP.
  - **Refined finding, replacing the flat null from the earlier entry**: audio_jepa's cohesion advantage is real, large, and specific to *coarse* domain-level discrimination — it does not transfer to the *fine-grained*, within-domain classification X-ARES actually tests (8 FMA genres, 10 UrbanSound8K event types). "Geometry doesn't predict function" was imprecise; the more accurate statement is "coarse-grained cluster cohesion doesn't predict fine-grained linear classification within a domain, and these are measuring different things."
  - CLAP's fine-grained dominance (best MLP on both X-ARES tasks) despite only 2nd-place *coarse* silhouette supports the user's alignment/uniformity hypothesis better than a raw ID/cohesion story does: contrastive training seems to produce representations specifically suited to fine-grained discriminability in a way TwoNN dimension and coarse silhouette don't capture. Not confirmed with a real alignment/uniformity computation yet (Wang & Isola metrics, or a per-class silhouette breakdown) — flagged as the actual next step if this becomes a formal Stage 6 diagnostic candidate, more promising than raw ID alone.
- **(3) wav2vec2 wiring diff**: instantiated both `HubertXaresEncoder` and `Wav2Vec2XaresEncoder` and compared their *measured* (not assumed) properties directly — **identical**: `sampling_rate=16000, output_dim=1024, hop_size_in_ms=20.101` for both. Wrapper code was already confirmed byte-identical apart from the model name (2026-08-10 earlier entry). This rules out a dimension/frame-rate wiring bug definitively — the two encoders feed the Qwen2 ASR decoder structurally identical input. wav2vec2's `Wav2Vec2Model` load report shows several `UNEXPECTED` keys (`quantizer.*`, `project_q.*`, `project_hid.*`) — normal and expected, those are the contrastive-pretraining head's parameters, correctly dropped when loading the plain encoder-only class, not a sign of a bad checkpoint load. **Downgraded from "possible bug, unresolved" to "confirmed not a wiring/config bug, cause still unknown"** — most likely a genuine property of wav2vec2-large-lv60's specific embedding content interacting badly with X-ARES's ASR training for this exact checkpoint, not something in our integration. Not chased further; would need to inspect actual Qwen2 decoder outputs/attention during training to go deeper, which is a bigger undertaking than this check.
  - **Follow-up check, same session**: user specifically flagged `do_normalize`/checkpoint-variant matching (a known HF wav2vec2 gotcha — layer-norm "large/robust" checkpoints need `attention_mask` passed for padded batches, or CTC-style decoding degrades badly while pooled classification barely notices) as worth checking harder. `do_normalize=True` + `feat_extract_norm=layer` was already confirmed correctly paired for wav2vec2-large-lv60 (2026-08-09). Checked the more specific angle: `do_stable_layer_norm` — **identical between hubert (`True`) and wav2vec2 (`True`)**, so it doesn't differentiate them, but checking it surfaced something more concrete: read X-ARES's own `audiowebdataset.py`/`task.py` source directly — its collate function *does* zero-pad variable-length clips into batches, then the encoding loop unpacks the result as `(audio, _)`, **explicitly discarding the lengths tensor** before ever calling any encoder. No model — ours or any other X-ARES-evaluated encoder — ever receives an attention mask from this framework. Real, confirmed, framework-level mechanism, architecturally exactly the risk the user described — but since hubert shares the identical `do_stable_layer_norm=True` flag and scored 0.855 anyway, it doesn't cleanly explain a hubert-vs-wav2vec2 *difference* on its own. Every configuration axis now checked (wrapper code, measured output_dim/hop_size, do_normalize, feat_extract_norm, do_stable_layer_norm) comes back identical between the two models. Genuinely closing this out at "confirmed not a config/wiring mismatch, real but non-differentiating framework-level padding risk identified" — resolving further would need actually implementing attention-mask support (X-ARES doesn't give us the lengths to do this properly without a heuristic) and re-running both models to compare, which is a bounded but separate piece of work, not pursued now.
- **Unrelated but worth recording**: mid-way through this analysis, `$HOME` had a real storage incident (see the dedicated entry below) that stalled everything for a while — the venv had to be rebuilt on `$SCRATCH` and one corrupted repo file recovered before any of the above analysis could actually run.

## 2026-08-10 (cont'd) — $HOME storage incident, diagnosed and recovered

- Mid-way through the analysis above, basic file reads in `$HOME` started failing with `Input/output error`; `diskusage_report` got stuck reporting 97MiB used (down from ~40GB) across many repeated checks over an extended period; `git status` surfaced `"Cannot send after transport endpoint shutdown"` — a network-mount-drop signature. Initially assumed cluster-wide (login node load average was ~7600 again, matching the earlier hubert/mert deadlock investigation's signature), but methodically narrowed it down:
  - An untouched, unrelated venv (`eddyflow-venv`) read its numpy files fine at the same time `audio-comp-venv`'s numpy failed — ruled out a filesystem-wide outage.
  - `pip install --force-reinstall` on the affected package hit the identical class of error reading pip's *own* vendored files — ruled out "just a corrupted numpy install," since the reinstall tool itself couldn't complete.
  - Created a brand-new venv (`audio-comp-venv2`) directly in `$HOME`; `ensurepip` failed writing a *brand-new* file with the same `OSError: [Errno 5] Input/output error` — ruled out "old corrupted files specifically," since this was a first-ever write to a location that had never held data before. This was the conclusive test: proved the problem was active, ongoing, and general to `$HOME` writes at that moment, not isolated to any one file or package.
  - Confirmed `$SCRATCH` unaffected by a trivial write/read/delete — asked the user how to proceed given the scope of a rebuild; they chose to rebuild at a fresh path. Built the venv at `$SCRATCH/audio-comp-venv` instead — clean on the first attempt.
  - `pip install -e .` for `audio_comp` itself kept failing on one specific file (`audio_comp/models/__init__.py`) even after the venv rebuild, consistently across retries (no longer intermittent for this file specifically) — while every sibling file in the same directory read fine. `ls -la` on that one file showed literal `-?????????` (couldn't even `stat` it), confirming a single corrupted inode, not a directory- or filesystem-wide problem by that point. Deleted just that one file and recreated it from its known-good committed content (available from earlier in-session context) — fixed immediately, `git status` confirmed the recreated content matched the committed version exactly (no diff).
  - Net assessment: **not data loss, not caused by anything we did** — likely a transient storage-backend issue on the cluster that happened to leave one file's inode in a bad state by the time it (mostly) resolved. Everything of substance was already git-pushed throughout, so nothing was actually at risk regardless.
  - Also switched `audio_comp` off `pip install -e .` entirely (not just moved venvs) — its wheel-build step had already shown itself to be less robust to this kind of intermittent failure than plain file reads were (it kept failing on that exact file across several retries even before the fully-broken state was reached). Now `PYTHONPATH`-relative, same approach `xares_eval` already used out of necessity (its CLI loader needs CWD-relative paths, incompatible with a normal install). Updated both sbatch scripts and README accordingly.
- Practical impact: roughly the second half of this session's wall-clock time went into diagnosing and recovering from this rather than the analysis itself. Worth remembering as a pattern: this cluster's shared login node has now shown genuinely extreme load (~7000-7600 range, 100+ concurrent users) on at least two separate occasions this project (2026-08-09's hubert/mert deadlock, this incident) — plausibly correlated with, though not proven to cause, storage-layer flakiness. Not something to "fix," just something to expect and diagnose methodically (isolate scope first — other venvs? new files? other filesystem? — before assuming data loss or reaching for a full rebuild) rather than panic or guess.

## 2026-08-10 (cont'd) — DeepShip: metafile record_id turned out unreliable, blocking vessel-grouped split

- Started the underwater-acoustics extension. ShipsEar (full access requires emailing dsantos@gts.uvigo.es, no self-service) and DeepShip (full ~265-ship dataset needs emailing mirfan@mail.nwpu.edu.cn; GitHub hosts only a 63-clip subset across 4 classes: cargo 12, passengership 20, tanker 28, tug 3) are both access-gated for the full data. Confirmed our existing `ship_vessel` probe-set category (`peng7554/DS3500`) has zero labels/metadata (raw audio only) so it can't substitute. User chose to start with DeepShip's 63 GitHub-hosted clips now rather than wait on ShipsEar or skip vessel data.
- Downloaded DeepShip's GitHub subset via zip archive (`scripts/setup_deepship.sh`) rather than `git clone` — `git clone` hit the same `Input/output error`/`Cannot send after transport endpoint shutdown` signature as the earlier `$HOME` incident (this time on `git`'s `.git/logs/HEAD` ref-log append specifically; plain file writes to the same directory succeeded immediately after), and `df -h $HOME` showed a corrupted-looking Lustre report (`-11P` used, `471%`) at the same moment even though `diskusage_report` showed normal quota usage — consistent with "transient backend flakiness," not a real quota or data problem. A `Write` tool call also hit the identical `EIO` once during this session and succeeded on retry. Treating this as the same known, intermittent, not-our-fault cluster storage issue as the earlier incident, not investigating further each time it recurs — just retry, and prefer zip/plain-file operations over git operations on `$HOME` when the two are equally easy (zip has no ref-log write at all).
- Read the metafile format directly (all 4 classes): comma-separated, no header — `record_id, node_id, vessel_name, date(YYYYMMDD), time(HHMMSS), duration_s, range_field("A-B"), <empty>`. Confirmed `duration_s` is real (not estimated) by matching `soxi` output against 4 sample files across all 4 classes (Cargo/15.wav 187s, Passengership/1.wav 37s, Tanker/2.wav 150s, Tug/9.wav 203s — exact match on all four at the time).
- **Then built a systematic check (all 63 hosted files, not just the 4 samples) matching each hosted wav's actual measured duration against its metafile row(s) — and the record_id key turned out not to be reliable as a join key at the scale needed.** Metafiles have far more rows than hosted files (tanker-metafile has 244 rows for only 28 hosted tanker files) and **record_id repeats within a metafile across entirely different vessels/durations** (e.g. tanker record `47` appears twice: `CHERRY GALAXY` dur=246s and `SEAVOYAGER` dur=251s; the actual `Tanker/47.wav` file measures 21s — matching *neither* candidate). Systematic result: Cargo 10/12 mismatched, Passengership 2/20 mismatched, Tanker 15/28 mismatched (13 more only "resolved" by picking whichever duplicate happened to have the closest duration, not a confirmed real match), Tug 0/3 mismatched (but n=3, not informative). The 4-sample spot-check that seemed to confirm the record_id-as-key assumption earlier was, in hindsight, not a representative sample — none of those 4 happened to hit a duplicated ID.
- **Practical consequence: vessel identity, date, and session cannot be reliably recovered for most of this 63-clip GitHub subset.** The only metadata that's structurally guaranteed correct is the class label itself (from the folder name). This directly blocks the user's explicit requirement to split by recording session/vessel instance rather than by random clip — there is no trustworthy vessel-instance signal to group by here. Whoever assembled the GitHub subset most likely renamed files sequentially per class folder, independent of the original metafile's record_id numbering, rather than preserving it.
- Did not silently fall back to a random clip-level split (which would violate the leakage-control requirement without saying so) or silently proceed with the unreliable vessel-name join (which would produce a false sense of leakage control while possibly grouping unrelated vessels together or splitting one vessel's clips across folds). Stopped to get a decision instead: recommended fallback is grouping by *source recording file* (each of the 63 hosted wav files as its own group/session) rather than by claimed vessel identity — reliable (file identity is not in question, only the metafile join is), and still prevents the milder "clips from the same continuous recording pass leak across train/test" failure mode, even though it can't guarantee the same physical vessel never appears in both train and test across *different* files (since we can't confirm which files share a vessel without the broken join).
- **User decision: proceed with file-level grouping** (the recommended option). Rebuilt `xares_eval/deepship/build_manifest.py`: `n_clips`/`duration_s` now come from each file's own measured audio length (`soundfile`), never the metafile's duration field (same unreliability as record_id — using it had silently produced badly wrong clip counts, e.g. tanker's metafile-derived total was 534 10s-clips across 3 folds vs. 153 once measured directly, almost 3.5x inflated). Fold assignment is now file-level greedy-balancing (still deterministic, no seed needed), 3 folds (tug only has 3 hosted files, so more folds would guarantee empty tug folds). `vessel_name`/`date`/`range_field` are kept in the manifest only as a `metadata_confidence` -tagged best-effort field (`exact`/`ambiguous_resolved`/`mismatch`, computed by nearest-duration match) for possible future manual verification — explicitly never used for the split. Final fold balance (clips at 10s each): fold0 183, fold1 182, fold2 180, reasonably even; per-class breakdown also balanced (see `data/deepship_manifest.csv`).
- Packaged as an X-ARES **private** task (`xares_eval/tasks_scratch/deepship_task.py`, `xares_eval/deepship/make_audio_tar.py`) — `private=True` skips X-ARES's Zenodo download path entirely (confirmed via direct read of `xares/task.py`'s `download_audio_tar`/`make_encoded_tar`), so the tars just need to already exist under `env_root/deepship/` before a run. Modeled the TaskConfig on ESC-50's real k-fold pattern (`xares/tasks/esc50_task.py`) — the one difference: `write_audio_tar`'s `labels` argument must be a dict (`{"vessel_class": ...}`) with the class resolved to an int via `label_processor`, matching every other real X-ARES task's convention (UrbanSound8K, FMA, GTZAN) — not ESC-50's own script, which passes raw ints directly and would actually hit a `ValueError` in the copy of `xares==0.1.3` installed here (checked `write_audio_tar`'s source directly; not fixed upstream, just avoided by not copying that particular pattern).
- **Frequency-domain preservation audit** (per the user's explicit requirement — distinguish "poor score = bad representation" from "poor score = preprocessing threw away the relevant band," same principle as the music2vec collapse-vs-idiosyncrasy check earlier). Checked all 7 models' actual feature-extraction code/config directly (not assumed):
  - `hubert`, `wav2vec2`, `mert`, `music2vec` all use `Wav2Vec2FeatureExtractor` (confirmed for MERT via `AutoFeatureExtractor.from_pretrained`) — raw-waveform CNN feature encoders, no mel/fbank front-end at all, so no frequency cutoff is even possible at the feature-extraction stage; the model's learned conv kernels see the full resampled waveform.
  - `clap` (`ClapFeatureExtractor`): checked HF's `feature_extraction_clap.py` source directly — `frequency_min` defaults to **0** (not overridden anywhere in our wrapper), `frequency_max=14000`. No low-frequency cutoff.
  - `musicfm`: builds `torchaudio.transforms.MelSpectrogram(sample_rate=24000, n_fft=2048, ...)` in `modules/features.py` without passing `f_min` — torchaudio's own default is `f_min=0.0`. No low-frequency cutoff.
  - `audio_jepa`: **the one real exception** — `_compute_mel_spec()` calls `torchaudio.compliance.kaldi.fbank(..., low_freq=20, ...)`, an explicit 20Hz high-pass on the mel filterbank's lowest edge (ported directly from the upstream `ltuncay/Audio-JEPA` inference code, not introduced by our wrapper; matches Kaldi's own conventional default). 20Hz is low enough to preserve most vessel acoustic signatures (propeller cavitation broadband, most machinery tonals), but large/slow vessels can have blade-rate or shaft-rate tonals below 20Hz, so this is a real, specific, testable risk for `audio_jepa` specifically if it underperforms on DeepShip — not the other 6 models.
  - Resampling itself (`audio_comp/models/_util.py`'s `resample()`) uses `librosa.resample` (band-limited polyphase/soxr resampling, not naive decimation) — downsampling only low-pass-filters at the *new* Nyquist, which never touches low-frequency content regardless of target rate. Not a risk for any model.
  - Incidental finding: `audio_jepa`'s `SAMPLE_RATE = 32_000` exactly matches DeepShip's native sample rate, so `audio_jepa` is the only one of the 7 models that does **zero resampling** on this task's raw audio.
  - **Net conclusion**: only `audio_jepa` has an explicit, real (if small) low-frequency cutoff among the 7 models; if it underperforms specifically on DeepShip relative to its cross-model geometry/silhouette standing, this 20Hz cutoff is now a documented, checked-not-assumed candidate explanation, not a guess reached for after the fact.
- Validation run launched for `hubert` on the DeepShip task (Slurm job 53975603) before submitting the other 6 models, same "cheapest real validation first" approach used for the original 3-task X-ARES run. Completed cleanly: MLP 0.364, kNN 0.355 (3-way fold average, chance = 0.25 for 4 classes) — pipeline validated end-to-end (tar packaging, `private=True` task config, encode/train/eval all worked on the first real run), so submitted the remaining 6 models immediately after.
- **All 7 models complete on DeepShip** (all classes well above the 0.25 chance baseline for cargo/passengership/tanker/tug). Results (MLP / kNN, 3-fold average, appended to `results/xares_results.csv` via the existing `xares_eval/parse_results.py` — no changes needed, it already generalizes to any task):

  | Model | MLP | KNN |
  |---|---|---|
  | mert | 0.494 | 0.337 |
  | musicfm | 0.430 | **0.386** |
  | wav2vec2 | 0.415 | 0.310 |
  | music2vec | 0.408 | 0.294 |
  | audio_jepa | 0.402 | 0.288 |
  | clap | 0.369 | 0.325 |
  | hubert | 0.364 | 0.355 |

  - The two music-domain-trained models (mert, musicfm) take 1st and 2nd on MLP, and musicfm wins kNN outright — plausible reading: ship engine/propeller acoustic texture (broadband + harmonic tonals) shares more useful structure with music's training distribution than with speech's, but this is two models out of seven and one dataset, not a strong claim — flagging as a candidate pattern to watch when the roster expands (Stage 2), not a conclusion.
  - **audio_jepa is 5th/7 on MLP and dead last on kNN** — consistent with (not proof of) the frequency-domain audit's flagged risk earlier this session: it's the only one of the 7 models with an explicit low-frequency cutoff in its feature extraction (`low_freq=20` in its kaldi.fbank call). Framing this carefully: n=63 real clips, file-level-not-vessel-level split, one dataset — nowhere near enough to confirm the 20Hz cutoff is *why* it underperforms here specifically (could just as easily be the same general-cohesion-over-separability pattern already seen on X-ARES's FMA/UrbanSound8K tasks, where audio_jepa was also mid/lower-pack). Recording the observation, not the conclusion.
  - This is a genuinely small, partial dataset (63 clips, only the GitHub-hosted subset of DeepShip's 265 ships) with a weaker-than-ideal leakage control (file-level, not vessel-level, grouping — see above). Any DeepShip number in a write-up needs both caveats attached every time, not just the first time it's mentioned.
- **DeepShip stage of the underwater-acoustics extension is functionally done**: manifest + tar packaging + task config + full 7-model run + results logged. Remaining per the original sequencing: ShipsEar (blocked on author email, not pursued this session), then BirdCLEF (next).

## 2026-08-10 (cont'd) — BirdCLEF task, all 7 models

- Source: `mteb/birdclef25-mini` (HF dataset) — a curated, balanced 1000-clip subset of BirdCLEF 2025 (50 species x 20 recordings each, sourced 750/250 from Xeno-canto/iNaturalist). Chosen over the raw `sebasgar/birdclef-202{1,2,3}` mirrors (full untouched Kaggle competition zips, tens of GB) and over hand-rolling a subset from raw Xeno-canto. Unlike DeepShip, **no data-integrity problem here** — every row's `url` field is unique across all 1000 rows, confirmed directly, so the row->audio->label mapping needed no join at all (verified, not assumed, per the DeepShip lesson two entries up).
- Recordings are real, highly variable field recordings (1.0s to 491s, median 21s) — not pre-trimmed like ESC-50/UrbanSound8K. Used 5s clips (not DeepShip/FMA's 10s convention) — matches BirdCLEF's own official evaluation window and bird vocalizations' much shorter typical duration than ship engine tonals. Recordings under 5s (69/1000) are zero-padded up to 5s rather than dropped (unlike DeepShip's remainder-dropping) — dropping outright would shrink some species' already-small 20-recording pool. `xares_eval/birdclef/build_manifest.py` / `make_audio_tar.py`, `xares_eval/tasks_scratch/birdclef_task.py`, `scripts/slurm/run_xares_birdclef.sbatch` — same private-task pattern as DeepShip. 5-fold split (not DeepShip's 3), clean and balanced since every species has exactly 20 recordings (4/species/fold) — recording-level grouping is the *correct* leakage control here, not a fallback like DeepShip's file-level split, since every row already is one independent recording.
- Validated end-to-end with `hubert` first (job 53979431: MLP 0.260, kNN 0.068, chance=1/50=0.02), then all 6 remaining models. **All 7 complete**, all above chance:

  | Model | MLP | KNN |
  |---|---|---|
  | clap | **0.284** | **0.189** |
  | hubert | 0.260 | 0.068 |
  | mert | 0.250 | 0.068 |
  | musicfm | 0.247 | 0.113 |
  | music2vec | 0.121 | 0.049 |
  | audio_jepa | 0.089 | 0.043 |
  | wav2vec2 | 0.049 | 0.035 |

  - **CLAP wins decisively on both metrics** — this is now the *third* task (after FMA-genre and UrbanSound8K, 2026-08-10 earlier entries) where CLAP is the best fine-grained classifier despite not having the strongest coarse-domain cohesion. Strengthens the standing hypothesis (not yet formally tested — still the flagged Stage 6 candidate) that contrastive/discriminative training pressure (Wang & Isola alignment/uniformity) is a better predictor of fine-grained classification than raw cohesion/intrinsic-dimension measures.
  - **audio_jepa and wav2vec2 are the bottom two on both metrics** — worth being precise about what this does and doesn't support. audio_jepa's DeepShip weakness (previous entry) has a plausible frequency-domain explanation (its 20Hz feature-extraction cutoff). That explanation **cannot apply here** — bird vocalizations are predominantly well above 1kHz, nowhere near a 20Hz cutoff — yet audio_jepa is still near the bottom. Read together, this actually argues *against* the frequency cutoff being the primary or sole driver of audio_jepa's cross-task weakness, and *for* the already-established, more general "coarse cohesion over fine-grained separability" pattern (2026-08-10 X-ARES entries) being the dominant explanation across all four downstream tasks tried so far (FMA, UrbanSound8K, DeepShip, BirdCLEF). The frequency-cutoff hypothesis stays on record as a plausible *contributing* factor specifically for DeepShip, not a general explanation — don't over-extend it.
  - wav2vec2's weakness here is a third data point (after its unexplained LibriSpeech-ASR zero and its earlier weak FMA/UrbanSound8K showing) in the same direction — still no confirmed root cause, staying logged as an open, checked-but-unresolved pattern rather than reaching for a new explanation without evidence.
- Results appended to `results/xares_results.csv` via the existing `xares_eval/parse_results.py`, unchanged — it already generalized cleanly to a 4th task and a 2nd private task without modification.
- **BirdCLEF stage of the underwater/bio-acoustics extension is functionally done.** Remaining from the original sequencing: ShipsEar (still blocked on author email, not pursued this session).

## 2026-08-10 (cont'd) — checkpoint-provenance schema + Stage 2/5 roadmap formalization

- User provided a detailed spec (informed by external research into checkpoint availability across candidate models) to formalize checkpoint provenance tracking and expand the Stage 2/Stage 5 roadmap sections. Scoped explicitly to registry + CLAUDE.md updates only — no new adapter implementation this pass.
- Added `checkpoint_status` to `ModelInfo` (`audio_comp/models/base.py`): four-value enum (`official_open_weights`, `official_public_weights_license_unclear`, `community_conversion`, `code_only`), with `COMPARISON_ELIGIBLE_CHECKPOINT_STATUSES` restricting the main comparison to the first two. Enforced at two points, not just documented: `register_model()` validates the status is a *known* value at import time; `get_model_class()` — the single chokepoint every pipeline entry point (`extract_embeddings.py`, all 7 `xares_eval/encoders/*.py`) already resolves a model name through — additionally validates the status is *comparison-eligible*, raising a clear error otherwise. Sanity-checked after the change: all 8 currently-registered models still resolve cleanly.
- Retroactively labeled all 8 registered models `official_open_weights`. **Did not blindly apply this to `beats`** as literally instructed, since `beats.py`'s own docstring already recorded its license as "unverified for checkpoint weights" — accepting the instruction at face value would have contradicted an already-recorded, deliberately-flagged finding, exactly the kind of unverified-secondary-label mistake the music2vec correction exists to prevent. Spun up a research pass instead: unilm's root LICENSE is MIT; the BEATs subdirectory's own README defers to that root license with no separate statement or contrary terms for the checkpoint weights specifically. Read as MIT-covered by absence of any carve-out, not an explicit per-checkpoint citation — a real but non-airtight judgment call, documented as such in `beats.py`'s docstring rather than silently upgraded to a confident label. Landed on `official_open_weights` (matching the instruction) but with the reasoning and caveat fully on record, and confirmed this doesn't change beats' active-vs-deferred status (it's still in `deferred_models` — the underlying reason is now confirmed to be *purely* the missing loader, not a checkpoint/license blocker, worth revisiting as a fast follow but not done here).
- Expanded CLAUDE.md's Stage 2 bullet in place (not a new duplicate section — the roadmap already had a Stage 2 entry) with the full Tier 1 (AudioMAE, AST, PANNs CNN14, BirdMAE, Perch 2.0, EnCodecMAE, each tagged with the specific gap it fills) / Tier 2 (WavLM, BYOL-A, Whisper, NatureLM-audio, explicitly deferred) breakdown, the checkpoint_status gate, and NatureLM-audio's flagged pre-requirement (an explicit layer/pooling decision before it can enter any RDM comparison, since it's a multimodal audio-language model rather than a plain encoder). Noted BirdMAE/Perch 2.0's dual purpose explicitly (also serve `xares_eval/birdclef/`, which now exists as of this session — one integration effort, not two, once either is wired up).
- Expanded CLAUDE.md's Stage 5 bullet in place with the two-condition fine-tuning methodology: an "official fine-tuned" condition (released checkpoints, logged separately, never blended into the main comparison) and a "matched fine-tuning" condition (identical protocol across every encoder — same data/split/epochs/optimizer/trainable-layer policy/seeds) that is the only condition used for the actual scientific correlation against Stage 3's frozen-geometry metrics. Also linked DeepShip's 63-clip result forward as an early informal preview of the OOD axis Stage 5 will formalize, not the Stage 5 run itself.
- Per the user's explicit instruction, **no Tier 1 adapter work started this pass** — next step is user confirmation of the updated CLAUDE.md content before any new model integration begins.

## 2026-08-10 (cont'd) — Tier 1 integration begins: PANNs CNN14 + Bird-MAE wired up

- User confirmed the CLAUDE.md content and gave the go-ahead for Tier 1, with a sequencing suggestion: do PANNs CNN14 (only pure-CNN architecture in the roster) and one bioacoustic model first, since architecture-family is a completely untested axis so far (everything so far has varied training objective, not architecture), and doing it early is more likely to surface a genuinely new pattern than AudioMAE/AST (which mostly extend already-motivated axes). Went with PANNs CNN14 + Bird-MAE (over Perch 2.0) as the bioacoustic pick, since Bird-MAE's checkpoint was immediately verifiable and cleanly HF-native-loadable (`trust_remote_code=True`), while Perch 2.0's hosting (likely Kaggle Models/TF-Hub, not HF) would need a separate loader investigation — left for later.
- Also formalized the standing principle behind the `beats` deviation (previous entry) into CLAUDE.md's Working Conventions directly, since the user pointed out it's a general rule worth stating explicitly, not a one-off: verify any factual claim about a model against its primary source before it enters code or a comparison table, even when the claim comes from an instruction or your own earlier output.
- **PANNs CNN14** (`audio_comp/models/panns_cnn14.py`, `scripts/setup_panns.sh`): checkpoint provenance verified directly against `github.com/qiuqiangkong/audioset_tagging_cnn` (not a secondary mirror) via a WebFetch pass — MIT-licensed (`LICENSE.MIT`), official checkpoint on the same authors' Zenodo record (3987831), matching the exact URL the `panns_inference` pip package (same author) downloads from by default. Used `panns_inference`'s `Cnn14` architecture class directly (not its `AudioTagging` wrapper, which hardcodes a `$HOME` checkpoint path and forces `DataParallel` — neither fits this project's conventions) plus our own checkpoint download script, matching the `$SCRATCH`/`$AUDIO_COMP_EXTERNAL` convention. `Cnn14.forward()` takes raw waveform directly (spectrogram extraction happens inside the model) and returns a dict with a 2048-d `embedding` alongside the AudioSet tagging output. Smoke-tested clean on CPU (1s dummy + 60ms short clip, no NaN, both single and batch `embed()`).
- **Bird-MAE** (`audio_comp/models/bird_mae.py`): checkpoint provenance re-verified independently rather than taking the tentative Tier 1 table label at face value (same "verify, don't assume" discipline as `beats`) — DBD-research-group is confirmed to be the actual paper's ("Can Masked Autoencoders Also Listen to Birds?", arXiv 2504.12880) own research group (same group behind BirdSet), so this is a genuine official release. But: **no LICENSE file exists in the GitHub repo at all** (confirmed via GitHub's own license-detection API returning a flat 404, not just "couldn't classify it" — a stronger, more concrete absence than `beats`' "MIT-but-no-explicit-per-checkpoint-line" situation) and no license field is set on the HF model card either. `checkpoint_status="official_public_weights_license_unclear"` is the correct label here, not `official_open_weights` — still comparison-eligible per the schema, but a real flag, not a formality; worth an email to the authors before this checkpoint is used anywhere beyond this internal comparison.
  - Hit a genuine version-skew bug loading it: the checkpoint's own `config.json` declares `transformers_version: "4.38.0"`, but this project's venv has transformers 5.15.0 (a major-version gap). `modeling_bird_mae.py`'s `BirdMAEModel.__init__` never calls `self.post_init()` (confirmed: the string doesn't appear anywhere in the file), which newer transformers now requires to populate `self.all_tied_weights_keys` before `_finalize_model_loading()` — without it, `AutoModel.from_pretrained` crashes with `AttributeError: 'BirdMAEModel' object has no attribute 'all_tied_weights_keys'` before the model ever loads. This is a bug in the checkpoint's own vendored code relative to a newer transformers, not anything specific to this project. Fixed with a narrow, documented monkeypatch on `PreTrainedModel._move_missing_keys_from_meta_to_device` (defaults the missing attribute to `{}`, active only during `load()`) — same category of fix as `audio_jepa`'s flash_attn shim, not a transformers downgrade (which would risk breaking every already-working HF-native model in the roster).
  - Once loaded: uses the model's own bundled `BirdMAEFeatureExtractor` (fbank extraction, fixed 512-frame padding/truncation, dataset-specific mean/std normalization) rather than reimplementing that logic — same reasoning as the audio_jepa kaldi.fbank lesson, reimplementing risks silently diverging from what the checkpoint actually trained on. `BirdMAEModel.forward()` already mean-pools internally (`config.global_pool == "mean"`) and returns the pooled 768-d vector as `last_hidden_state` — despite the name, not a per-frame sequence, no extra pooling needed. Smoke-tested clean on CPU (1s dummy + 60ms short clip → correctly gets zero-padded internally to the model's fixed 512-frame/5s input shape, no NaN).
- Both added to `configs/models.yaml`'s `active_models` (now 9 total) only after passing the same smoke-test discipline as every prior model. Updated `audio_comp/models/__init__.py`, README's dependency list (`panns_inference`, `torchlibrosa` — neither declares a `torch` dependency, confirmed via `pip show`, so no repeat of the flash_attn/torch-downgrade incident) and model table (added a `checkpoint_status` column throughout, corrected `beats`' license cell to match the previous entry's finding), README's one-time-setup-script list, and CLAUDE.md's Tier 1 table (added a `Status` column marking these two `wired up 2026-08-10`).
- Full registry sanity check after wiring: all 9 active models resolve through `get_model_class()` cleanly (the actual comparison-eligibility gate), correct `checkpoint_status` per model. Not yet run through the actual probe-set embedding extraction pipeline (`extract_embeddings.py` on the full 10,000-clip probe set) or the RSA/CKA comparison (Stage 3) — that's the next step, not done in this pass.
- **Pre-registered predictions, written before running the 9-model geometry pass or the PANNs/BirdMAE BirdCLEF re-run** (user's explicit request, same discipline as the audio_jepa low-ID/high-cohesion prediction, 2026-08-09):
  1. **PANNs, architecture axis.** If RSA/CKA agreement between `panns_cnn14` and the transformer-based models is notably *lower* than agreement among the transformer-based models with each other (even ones that differ in training objective), that's evidence architecture family shapes representational geometry independently of training paradigm — every model in the roster until now has varied objective/data, never backbone, so this would be a genuinely new axis, not a replay of an existing one.
  2. **BirdMAE, the sharper test.** BirdMAE is domain-matched (bioacoustic-specific) but reconstruction-based (MAE-style), not contrastive — this directly stress-tests the discriminative-pressure/contrastive-training account of fine-grained classification that's been building since CLAP's wins on FMA/UrbanSound8K/BirdCLEF (2026-08-10 entries). If CLAP still beats BirdMAE on BirdCLEF specifically despite BirdMAE's domain match, that's strong, citable evidence the discriminative-pressure story dominates domain relevance — a genuinely striking result given how "obviously" a specialist should win on its own domain. If BirdMAE wins, that says domain-relevance can override the training-objective effect — the more expected outcome, but not yet confirmed, worth stating as the alternative rather than assumed.
  Sequence: full RSA/CKA/TwoNN pass at 9 models first, then BirdCLEF re-run with `panns_cnn14`/`bird_mae` added, before deciding whether AudioMAE/AST or a write-up pause makes more sense.
- Hit a real bug extracting `panns_cnn14`'s probe-set embeddings: `RuntimeError: Given input size: (256x1x16). Calculated output size: (256x0x8)` — Cnn14's 5 stages of (2,2) time-pooling collapse the logmel time axis to zero for short clips, and the 10,000-clip probe set includes real UrbanSound8K-derived `city_noise` clips as short as 60ms (same class of issue as the audio_jepa kaldi.fbank short-clip lesson, 2026-08-09, just a different model this time). My original smoke test missed this because it tested the 60ms clip only inside a padded batch alongside a 1s clip (which masked the problem), never as a standalone `embed()` call the way `extract_embeddings.py` actually calls it (one clip at a time, batch size 1). Empirically tested the real threshold: fails up to 300ms, first reliably safe at 400ms. Fixed with a 1.0s zero-pad floor in `embed_batch()` (a safety margin, not tied to any PANNs design constant, unlike audio_jepa's fixed-10s window) — re-extracted cleanly afterward, and applied the identical fix in the new `xares_eval/encoders/panns_cnn14_encoder.py` wrapper too, since X-ARES's own dataloader doesn't know about this model-specific minimum either.
- Built `xares_eval/encoders/panns_cnn14_encoder.py` and `bird_mae_encoder.py` for the eventual BirdCLEF re-run, following the same pooled-single-frame-sequence pattern CLAP's wrapper already established (both models pool out the time axis entirely inside `forward()`, so there's no real per-frame sequence to expose — see `clap_encoder.py`'s docstring for the original rationale). Both smoke-tested clean via `check_audio_encoder`.
- **Ran the full RSA/CKA/TwoNN comparison at 9 models** (`results/rsa_matrix.csv`, `cka_matrix.csv`, `intrinsic_dimension.csv`, heatmaps). Took considerably longer than the original 7-model run (~35+ min, backgrounded rather than foreground after a 2-minute timeout) — expected given `compute_rdm`'s O(n^2 * dim) cost on 10,000 clips, and PANNs' 2048-d embedding is the largest in the roster.
  - **Prediction 1 (PANNs, architecture axis): not supported.** Computed directly: mean RSA among the 8 transformer-based models (28 pairs) = 0.2826; mean RSA between `panns_cnn14` and each of those 8 = 0.3336 — PANNs' agreement with the transformer roster is *higher* than the transformers' agreement with each other, the opposite direction the prediction was looking for. No evidence architecture family (CNN vs. transformer) is an independent geometry-shaping axis, at least not in the direction of causing systematic *disagreement*. A clean negative result, stated as such, not reached-for.
  - **Candidate alternative pattern, worth flagging rather than following up on unprompted**: PANNs' two *highest* individual correlations are with `audio_jepa` (0.60) and `clap` (0.56) — both very different paradigms (JEPA-family, contrastive) but both trained on broad, general-audio-scale data (PANNs itself: AudioSet, a broad supervised-tagging corpus). This lines up with the still-not-started Stage 1(b) breadth-hypothesis reframing (narrow-speech / narrow-music / broad-mixed groupings) better than with either the paradigm or the architecture-family framing — PANNs, audio_jepa, and CLAP would all sit in the same "broad-mixed" bucket under that reframing. Not chased further now (Stage 1(b) is still explicitly out of scope for this session), but this is a second independent data point in its favor, worth remembering when that check finally happens.
  - `bird_mae`'s RSA pattern is comparatively flat/uniform across the roster (0.28–0.45, no sharp outlier pairing either way) — nothing sharp enough here to comment on yet; the real BirdMAE test (prediction 2) is functional (BirdCLEF classification), not geometric, and needs the Slurm re-run's actual numbers, not the RSA matrix.
  - TwoNN intrinsic dimension: `panns_cnn14` = 12.524 (unremarkable, sits between `clap` 12.314 and the rest of the pack — no architecture-driven outlier there either), `bird_mae` = 14.348 (also unremarkable). Neither model's ID stands out the way, say, `music2vec`'s 21.5 (highest of all 9) or `audio_jepa`'s 10.8 (lowest) already did.
- BirdCLEF re-run submitted for `panns_cnn14` (job 54058115) and `bird_mae` (job 54058116) — both completed cleanly. Full 9-model BirdCLEF table (MLP / KNN, chance = 1/50 = 0.02):

  | Model | MLP | KNN |
  |---|---|---|
  | bird_mae | **0.345** | 0.183 |
  | panns_cnn14 | 0.308 | 0.134 |
  | clap | 0.284 | **0.189** |
  | hubert | 0.260 | 0.068 |
  | mert | 0.250 | 0.068 |
  | musicfm | 0.247 | 0.113 |
  | music2vec | 0.121 | 0.049 |
  | audio_jepa | 0.089 | 0.043 |
  | wav2vec2 | 0.049 | 0.035 |

  - **Prediction 2 (BirdMAE, the sharper test): result is real but more nuanced than either clean alternative posed.** BirdMAE *does* beat CLAP on MLP (0.345 vs. 0.284) — domain-relevance winning out over CLAP's discriminative/contrastive-pressure edge, the "more expected" outcome the prediction named as the alternative. But CLAP is still (barely) ahead on KNN (0.189 vs. 0.183 — a ~3% relative gap on n=1000, arguably within noise, not a clean win either way). So it's not a flat "BirdMAE wins" — it wins the trained-head comparison and ties/slightly loses the untrained raw-geometry comparison.
  - **The complication worth stating plainly, not glossing over: PANNs shows the *exact same qualitative pattern* relative to CLAP, and PANNs is not domain-matched to birds at all** (general AudioSet supervised tagging) — MLP 0.308 > CLAP's 0.284, KNN 0.134 < CLAP's 0.189. If domain-relevance were the operative explanation for BirdMAE's MLP win, there's no reason a general-purpose, non-bioacoustic model should show the identical MLP-wins/KNN-loses relationship to CLAP. It does. This points to a cleaner, more general explanation than "domain match beats discriminative pretraining": **CLAP's contrastive embedding space is more linearly/metrically separable out of the box (higher KNN, no training needed), while both PANNs (supervised classification) and BirdMAE (reconstruction MAE) produce features that a trained MLP head can exploit *more* than CLAP's despite being less separable untrained.** This is a genuine extension of the project's standing cohesion-vs-separability headline finding (2026-08-10, Stage 1a): it's not just "coarse cohesion vs. fine-grained separability don't transfer" — training objective also seems to trade off *raw embedding-space separability* (what KNN measures) against *trainable-head exploitability* (what MLP measures), and this trade shows up whether or not the pretraining domain matches the downstream task. Domain-relevance (BirdMAE's actual bird training data) may still matter at the margin — it does have the single highest MLP score of all 9 — but the PANNs comparison shows domain match isn't *necessary* to get the same qualitative MLP/KNN split, so it can't be the primary explanation on its own.
  - Not a fully resolved account yet — this is now three separable axes in play (coarse cohesion, fine-grained separability, and this new trained-head-exploitability-vs-raw-separability split) with only 9 models and 4 downstream tasks to trace them through. Flagging as the sharpest current lead for the eventual Stage 6 diagnostic (already candidate-marked for Wang & Isola alignment/uniformity metrics) rather than claiming it's settled.
- **Process fix, logged directly into CLAUDE.md's Stage 2 gate**: "test the shortest edge-case clip standalone (batch size 1), not only inside a batch alongside a longer clip" is now a permanent, explicit smoke-test requirement — this exact blind spot (a short clip padded up to a longer batch-mate's length never exercises the genuinely-short code path) caused a full-scale-only crash twice now with the same root shape: audio_jepa's kaldi.fbank window (2026-08-09) and panns_cnn14's pooling stack (2026-08-10, this session). Not treating it as a one-off anymore.

## 2026-08-10 (cont'd) — Stage 1(b) breadth-hypothesis check, finally run

- User pointed out this had been deferred three separate times without ever being run, each time with a new independent signal pointing at it: the original 10k-scale pattern break, audio_jepa aligning with clap over same-paradigm models, and now (this session) PANNs' strongest correlations landing on audio_jepa/clap specifically. Cheap and already-collected — no new extraction, just characterizing each of the 9 models' training-corpus breadth and testing it against the RSA matrix that already exists. Ran it: `audio_comp/pipelines/breadth_hypothesis_check.py`.
- Method: partition the 9 models two ways — the implicit domain+paradigm framing used through Stage 0, and the breadth framing (narrow-speech / narrow-music / narrow-bioacoustic / broad-mixed / self-distillation-narrow-music, extending the original Stage 1(b) sketch with `panns_cnn14`→`broad_mixed` (AudioSet is genuinely broad, 527 diverse everyday-sound classes) and `bird_mae`→new `narrow_bioacoustic` bucket) — then compute mean-within-group RSA minus mean-between-group RSA for each, plus a permutation test (20,000 reshuffles of group labels, same group sizes held fixed) for a significance read given how few models/pairs there are to work with.
- **Results**:
  - domain+paradigm: gap = 0.385 (mean_within 0.658 over only 2 pairs — `hubert`-`wav2vec2` and `mert`-`musicfm`, the only two non-singleton groups this framing produces), permutation p = 0.0095.
  - breadth: gap = 0.360 (mean_within 0.604 over 5 pairs), permutation p = 0.0003.
- **Read carefully, not just "which gap is bigger" — the raw gap is nominally slightly larger under domain+paradigm, but that's a thin, potentially fragile result.** Domain+paradigm's entire gap rests on exactly 2 pairs, both of which breadth *also* captures identically (`narrow_speech` and `narrow_music` contain the same two pairs). The real difference between the framings is what each does with `audio_jepa`, `clap`, `panns_cnn14`, and `bird_mae`: domain+paradigm scatters all four into singleton groups with zero explanatory power (5 of its 7 groups are singletons — 5 of 9 models contribute no within-group pairs at all), while breadth groups `audio_jepa`+`clap`+`panns_cnn14` into `broad_mixed` and captures a real, significant 3-way mutual agreement there (pairwise RSA 0.54–0.60, all well above the roster's overall 0.29 average) that domain+paradigm has no account for whatsoever. Only `bird_mae` and `music2vec` remain ungrouped under breadth (7/9 models covered) vs. 5/9 under domain+paradigm.
- **Verdict, per the Stage 1(b) gate ("if it doesn't explain better, keep domain+paradigm"): breadth wins, adopted as the working explanation.** Not because its raw gap is bigger (it's marginally smaller) but because it explains meaningfully more of the roster's structure with a far stronger significance signal (p=0.0003 vs. 0.0095) — it's a strict extension of what domain+paradigm already captured (the two narrow-domain pairs) plus real structure domain+paradigm couldn't touch (the broad_mixed cluster). This closes out Stage 1(b), marked "not started" in CLAUDE.md until now — three independent signals across this project (the original 10k pattern-break, audio_jepa/clap alignment, PANNs' correlations) all turned out to point at something real once actually tested, not just plausible-sounding in hindsight.

## 2026-08-10 (cont'd) — Wang & Isola alignment/uniformity computed for real, replacing the silhouette proxy

- Implemented `audio_comp/geometry/alignment_uniformity.py` (both metrics, L2-normalized/unit-hypersphere per the original paper) and `audio_comp/pipelines/alignment_uniformity_check.py` (computes both for all 9 models on the existing probe-set embeddings, correlates against downstream KNN/MLP using only already-collected data — no new extraction). **Adapted, not literal, definition of alignment**: the original paper's "positive pairs" are augmented views of the same instance; this probe set has no such structure, so alignment here uses same-*category* clips (5 coarse categories) as the positive-pair proxy — stated plainly, not implied to be the textbook metric unmodified.
- Two correlation scopes, both free (no new Slurm jobs): BirdCLEF (all 9 models, the only task every current model shares) and FMA-genre+UrbanSound8K average (the original 7-model roster, pre-`panns_cnn14`/`bird_mae` — matches the exact task pairing the original silhouette-vs-MLP check used, so this is a direct upgrade of that check with the real metric).
- **Results, and an honest methodological caveat first**: alignment and uniformity turned out to be **perfectly rank-correlated with each other across all 9 models** (Spearman = -1.0, Pearson = -0.991, p<1e-6) — meaning the same-category-proxy adaptation of alignment isn't capturing anything independent of overall embedding-space dispersion here, unlike the original paper's design where true instance-level positive pairs make alignment and uniformity meaningfully complementary. In this project's data the two numbers are really one axis (call it "embedding-space dispersion"), not two — report them as such, not as independent cross-validating evidence.
  - **FMA+UrbanSound8K (7 models, original roster): uniformity predicts MLP very strongly** — spearman(uniformity, MLP) = -0.964, p<0.001 (more uniform/dispersed embedding space → higher MLP classification accuracy, the theoretically expected direction). KNN correlation is weaker and only marginal: -0.750, p=0.052.
  - **BirdCLEF (9 models, includes panns_cnn14/bird_mae): the pattern flips direction** — uniformity correlates *more* with KNN (-0.633, p=0.067) than with MLP (-0.400, p=0.286), neither reaching significance at n=9. This is the direction the PANNs/BirdMAE mechanistic hypothesis (previous entry) would predict ("uniformity should track KNN specifically, the untrained-separability side of the split") — but it directly contradicts what the FMA+UrbanSound8K scope showed.
  - **Net verdict: genuinely mixed, not a clean validation of the "uniformity predicts KNN better than MLP" mechanistic hypothesis.** True in one scope, false in the other, and both scopes are underpowered (n=7, n=9) to resolve the disagreement definitively — this isn't being explained away, it's an honest split result. What *is* solidly established: uniformity/dispersion is a real, strong, quantitative predictor of downstream MLP performance in the original 7-model roster (p<0.001, not marginal) — a substantially stronger and more specific result than the earlier silhouette proxy managed (rho=0.357, p=0.43, not significant, 2026-08-10 earlier entry). The real metric is a clear upgrade over the proxy even though it doesn't cleanly settle the sharper KNN-vs-MLP question yet.
  - Not chased further this session — resolving the scope disagreement would need either a larger/more powered model roster (more Tier 1 additions) or a genuinely independent (non-same-category) way to compute alignment, not more analysis of the same 9-model, 2-scope data. Logged as the honest current state of the Stage 6 diagnostic candidate, not a finished result.

## 2026-08-10 (cont'd) — true instance-level positive pairs for alignment (fixing the degeneracy) + AST/AudioMAE (Tier 1, continued)

- User's read on today's alignment/uniformity result: the -1.0 rank correlation is fixable, not just a limitation to note — same-category pairs conflate "close because genuinely similar" with "close because same broad label" (every "ship" clip paired with every other "ship" clip, regardless of whether they're actually acoustically similar). Recommended a true instance-level positive-pair proxy (augmented views of the same individual clip) instead, in parallel with continuing Tier 1 (AudioMAE + AST specifically — AST isolates "is it supervision specifically" from "is it PANNs specifically" by adding a second supervised model with a *different* architecture; AudioMAE adds a third non-contrastive candidate to check whether PANNs/BirdMAE's shared MLP-wins/KNN-loses pattern is a general non-contrastive-objective property or three isolated cases).
- **Built the true-pair fix**: `audio_comp/data/build_augmented_probe_subset.py` samples 100 clips/category (500 total, stratified, seeded) from the probe set and pitch-shifts each by +2 semitones (`librosa.effects.pitch_shift` — a standard augmented-positive-pair choice in the contrastive audio literature, raw-waveform-level so it needs zero model-specific code, unlike SpecAugment-style masking). Extracted embeddings for all 500 original + 500 augmented clips across all 9 models (`scripts/slurm/extract_embeddings_augmented.sbatch`, reusing `extract_embeddings.py` unmodified via a different manifest/data-root). Added `alignment_score_paired()` to `audio_comp/geometry/alignment_uniformity.py` — the literal Wang & Isola definition (mean squared distance between true (original, augmented) pairs), not an adaptation, kept alongside the old category-proxy version for the historical comparison rather than deleting it.
- **Result: real but partial decorrelation, and one new, actually-significant correlation.** Paired-alignment vs. uniformity: Spearman -0.917 (p=0.001) — down from the category-proxy's perfect -1.000, so the fix genuinely helped, but the two metrics remain strongly related, not fully independent (plausibly because pitch-shift is a fairly mild perturbation, so a positive pair's distance is still heavily influenced by the same overall embedding-space-dispersion factor driving uniformity — a true independent measurement might need a more aggressive/varied augmentation set). The BirdCLEF scope now shows a **genuinely significant** correlation for the first time: spearman(paired_alignment, KNN) = +0.717, **p=0.030** (vs. MLP's +0.583, p=0.099, not significant) — the direction the PANNs/BirdMAE mechanistic hypothesis predicts, and now with real significance at n=9, not just a suggestive direction. But the FMA+UrbanSound8K scope (7 models) still reverses it: paired_alignment vs. MLP = +0.821 (p=0.023, significant) vs. KNN = +0.536 (p=0.215, not significant) — the same scope-dependent flip persists even with the corrected metric. (Sign note: positive correlation here, not negative, because pitch-shift alignment measures distance directly rather than the log-negative-exponential uniformity does — a model less invariant to pitch-shift, i.e. worse/higher raw alignment, is also typically a more generally-dispersed embedding space, i.e. better/more-negative uniformity; the two track each other in the "performance" direction once the sign conventions are worked through, consistent with the strong residual anti-correlation between them.)
- **Honest verdict: this was worth doing — real progress, not a wasted detour — but doesn't resolve the core question.** The degeneracy is confirmed fixable (partial decorrelation achieved) and the corrected metric surfaces a new significant KNN correlation that the degenerate proxy couldn't show cleanly. But the scope-dependent reversal (BirdCLEF favors KNN, FMA+UrbanSound8K favors MLP) survives the fix, meaning it's more likely a genuine roster-composition effect (which models happen to be in each scope) than purely an artifact of the degenerate proxy, as originally hypothesized. Resolving it now most plausibly needs the larger, more powered roster the AudioMAE/AST work below is building toward, rather than a better positive-pair definition alone.
- **AST** (`audio_comp/models/ast.py`): checkpoint provenance verified directly against the primary source (`github.com/YuanGongND/ast`, the paper's own repo, Interspeech 2021) — BSD-3-Clause, matches `MIT/ast-finetuned-audioset-10-10-0.4593`'s HF card license exactly ("MIT" is the authors' institution, not the license name). Clean HF-native integration (`ASTModel`/`ASTFeatureExtractor`, real per-frame `last_hidden_state` unlike CLAP/PANNs/Bird-MAE, uses the model's own `pooler_output`). Uses the AudioSet-*trained* checkpoint, not a "base, non-finetuned" variant — documented explicitly why this isn't a violation of the project's usual "avoid -ft checkpoints" rule: AST has no two-stage self-supervised-then-finetuned structure the way wav2vec2/hubert do, its supervised AudioSet training *is* its designed pretraining, and CLAUDE.md's Tier 1 table specifically wants this checkpoint to test supervision's effect independent of architecture (now that architecture itself has been ruled out as an explanatory axis, 2026-08-10 earlier entry). Smoke-tested clean, including the shortest edge-case clip standalone (60ms, batch size 1) per the process fix logged earlier today.
- **AudioMAE** (`audio_comp/models/audiomae.py`, `scripts/setup_audiomae.sh`): checkpoint provenance verified directly against `github.com/facebookresearch/AudioMAE` — CC-BY-4.0, explicitly stated to cover the whole project including weights (no judgment call needed here, unlike `beats`). Checkpoint (ViT-B, AudioSet-2M pretrained-only, not finetuned) downloaded from the repo's own README-linked Google Drive file via `gdown`. **Exact model-construction kwargs read directly out of the checkpoint's own saved `args` Namespace** (pretraining scripts save the full argparse config into the checkpoint dict) and cross-checked against the state dict's own tensor shapes (`pos_embed` (1,513,768) => 512 patches + cls token, matching img_size=(1024,128) at patch_size=16) — not assumed from the README or guessed, the same discipline as every other checkpoint-provenance check this session.
  - **Four separate, genuine version-skew issues hit and fixed, all with narrow documented shims, none with a global downgrade**: (1) `util/misc.py` does `from torch._six import inf` — removed in PyTorch 2.0 (project runs 2.11.0); (2) the repo's `Block(..., qk_scale=...)` call needs an old timm API modern timm removed — pinned `timm==0.4.9` project-wide (safe: confirmed via `pip show timm`'s empty `Required-by` that nothing else in the roster depends on timm, so no shared-dependency risk like the flash_attn/torch incident); (3) that same old-timm-required `Block.qk_scale` conflicts with the checkpoint's `decoder_mode=1` Swin-based decoder needing a *newer* timm `SwinTransformerBlock` API (`feat_size`/`drop_attn` args) — no single timm version has both APIs (tested 0.3.2, 0.4.9, 0.4.12, 1.0.28); resolved by constructing with `decoder_mode=0` instead (the decoder is never used — `forward_encoder_no_mask()` only touches the encoder) and loading the state dict with `strict=False`, with an explicit runtime assertion that every unmatched key is decoder-prefixed, so the encoder's correctness is *verified*, not assumed; (4) `util/pos_embed.py`'s sin-cos position-embedding initializer uses `np.float`, removed in numpy >=1.24 — restored as a one-line alias, harmless since that initial value is immediately overwritten wholesale by `load_state_dict` anyway. Smoke-tested clean afterward, including the standalone 60ms edge case.
- Both added to `configs/models.yaml`'s `active_models` (now 11 total). Full-probe-set extraction submitted for both (jobs 54068294 `ast`, 54068295 `audiomae`) — results pending, 11-model RSA/CKA/TwoNN re-run and BirdCLEF re-run to follow once extraction completes.

## 2026-08-10 (cont'd) — 11-model results land: breadth confirmed stronger, and the KNN/MLP story gets a real correction

- Full-probe extraction succeeded for both new models (10,000 embeddings each, `ast` 768-d, `audiomae` 768-d). X-ARES encoders written (`xares_eval/encoders/ast_encoder.py`, `audiomae_encoder.py` — both use the real per-frame sequence, unlike CLAP/PANNs/Bird-MAE's pooled-single-frame workaround: AST has a genuine ViT patch-token sequence, AudioMAE's `forward_encoder_no_mask()` returns one before the adapter's own mean-pool), smoke-tested clean, BirdCLEF re-run submitted alongside the 11-model RSA/CKA/TwoNN re-run.

- **Breadth-hypothesis check, re-run at 11 models — even more decisive than at 9.** `ast` and `audiomae` both trained on AudioSet (broad, diverse general-audio data, same argument already applied to `panns_cnn14`), so both join `broad_mixed` (now 5 members: `audio_jepa`, `clap`, `panns_cnn14`, `ast`, `audiomae` — categorized by documented training-data breadth, not by how well each one happens to fit the group; `audiomae`'s actual RSA correlations with the rest of the group are noticeably weaker (0.27-0.52) than the other four's mutual correlations (0.55-0.84), and it's included anyway rather than moved to a category invented to fit better — that weaker fit is itself honest information, not smoothed over). Result: breadth's permutation p-value is now **0.0000** (0/20,000 reshuffles matched or exceeded the observed gap) vs. domain+paradigm's p=0.0082 — even stronger than the 9-model result's already-clear 0.0003 vs. 0.0095. Breadth's raw gap (0.330) is now a bit further below domain+paradigm's (0.370) than at 9 models (`audiomae`'s weaker fit pulls the group average down), but the significance gap widened anyway, because breadth is now explaining 12 real within-group pairs (the 5-way `broad_mixed` cluster alone contributes 10) against domain+paradigm's same thin 2 — a much harder pattern to produce by chance. Breadth now covers 9/11 models in real (non-singleton) groups vs. domain+paradigm's 4/11. **This is now the fourth independent thing in this project pointing at breadth** (the original 10k-scale pattern break, `audio_jepa`/`clap` alignment, PANNs' correlations, and now AST's correlations landing in the same cluster) — the working explanation keeps getting more entrenched every time a new model is added, not less.

- **BirdCLEF, full 11-model table (MLP / KNN, chance = 0.02):**

  | Model | MLP | KNN |
  |---|---|---|
  | **ast** | **0.394** | **0.239** |
  | bird_mae | 0.345 | 0.183 |
  | panns_cnn14 | 0.308 | 0.134 |
  | clap | 0.284 | 0.189 |
  | hubert | 0.260 | 0.068 |
  | mert | 0.250 | 0.068 |
  | musicfm | 0.247 | 0.113 |
  | audiomae | 0.167 | 0.039 |
  | music2vec | 0.121 | 0.049 |
  | audio_jepa | 0.089 | 0.043 |
  | wav2vec2 | 0.049 | 0.035 |

  - **AST wins outright on both metrics** — not a trade-off, a clean sweep, beating `bird_mae`'s previous-best MLP (0.345) and `clap`'s previous-best KNN (0.189). This directly answers the question AST was added to test ("is it supervision specifically, or is it PANNs specifically" driving PANNs'/Bird-MAE's MLP-wins/KNN-loses-to-CLAP pattern): **it's not supervision as a category.** AST is also supervised, also AudioSet-trained, and shows *none* of PANNs'/Bird-MAE's/AudioMAE's KNN weakness relative to CLAP — it beats CLAP's KNN outright. The clean "non-contrastive objectives trade raw separability for trainable-head-exploitability" story from the earlier PANNs/Bird-MAE entry does not survive AST's addition as originally stated.
  - **The AudioMAE-vs-Bird-MAE pair is the cleanest isolated domain-relevance test run in this project so far, and it says domain relevance is real.** Same training objective (reconstruction-MAE) on both, only the training domain differs (AudioMAE: general AudioSet; Bird-MAE: bird-only BirdSet) — and Bird-MAE wins by a wide margin on the bird-specific task (MLP 0.345 vs. 0.167, more than double; KNN 0.183 vs. 0.039, more than 4x). Unlike the earlier PANNs-vs-Bird-MAE comparison (different objectives, so domain-relevance and objective were confounded), this pair holds objective constant and isolates domain match cleanly. Worth restating precisely: this doesn't reinstate domain-relevance as *the* explanation for Bird-MAE's earlier win over CLAP (PANNs, non-domain-matched, still showed that pattern too) — it establishes that domain relevance is a real, additional, independently-acting effect on top of whatever the objective-driven pattern is.
  - **Revised account, replacing "non-contrastive trades KNN for MLP" with something that survives AST:** what actually separates the roster isn't contrastive-vs-not, it's closer to **discriminative training pressure (contrastive, like CLAP, *or* supervised classification with real labels, like AST) produces embeddings that are well-separated in raw distance (high KNN) *and* trainable-head-exploitable (high MLP) — these two properties aren't in tension for discriminatively-trained models.** Reconstruction-based training (Bird-MAE, AudioMAE) produces comparatively weaker raw separability (lower KNN) while still being reasonably MLP-trainable, i.e. *that* pairing does show the earlier-observed trade-off. PANNs (supervised but CNN, weaker on both metrics than AST despite the same AudioSet supervision) suggests architecture may modulate how well supervised training pressure gets converted into separable geometry, but isn't disqualifying on its own — PANNs still clearly outperforms every reconstruction-trained and self-supervised model except Bird-MAE and CLAP. Flagging this as the current best account, not a settled one — it now needs to survive whatever Perch 2.0 or EnCodecMAE show, the same way the original account didn't survive AST.
  - `audiomae`'s weakness here (worst of the AudioSet-trained models, barely above the weakest self-supervised models) is consistent with, not contradicted by, its notably weaker RSA fit within `broad_mixed` above — both point the same direction: general-domain reconstruction training produces a geometry that's the least "typical" member of the broad-training cluster, on both the RSA-agreement axis and the downstream-function axis.
- Results appended to `results/xares_results.csv` and `results/rsa_matrix.csv`/`cka_matrix.csv`/`intrinsic_dimension.csv` via the existing pipelines, unmodified.
