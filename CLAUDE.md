# CLAUDE.md — Cross-Model Audio Representation Geometry & JEPA Relational Distillation

This file orients any Claude session (or collaborator) working in this repo. Read this before touching code.

## One-line summary

Test whether independently-trained audio foundation models agree on the *relational* geometry of sound (pairwise distances between clips), and if they do, test whether a JEPA-style model trained to reproduce that consensus geometry behaves differently — on a real downstream task — than a model distilled from any single teacher.

## Why this project exists

Standard task loss/accuracy can't tell us whether a representation is well-formed when there's no single correct answer to check against. This project is one concrete, falsifiable test of an alternative: does *cross-model agreement on relational structure* track something real, and can that structure be deliberately reproduced rather than just observed. It's the applied, decision-relevant version of a longer theoretical thread (Platonic Representation Hypothesis, CKA/RSA literature, JEPA-as-representation-shaping) — see "Background reading" below if more context is needed, but this file is self-contained for execution purposes.

**Eventual application domain: underwater acoustics** (vessel-contact detection). Do not default to speech/music-only framing when designing the probe set or downstream task — general-audio foundation models are the available *teachers*, not necessarily the target domain.

## Explicit scope

### In scope
- **Phase 1**: Build representational dissimilarity matrices (RDMs) for a fixed probe set of audio clips, one RDM per foundation model, across several models spanning different training paradigms (contrastive, masked-modeling, JEPA-family, generative-derived).
- **Phase 1**: Compare RDMs pairwise via RSA (Spearman correlation between RDMs) and CKA (as a cross-check, not the sole metric — CKA is known to be gameable, see Davari et al. 2023).
- **Phase 2** (gated on Phase 1 findings — see decision rule below): Build a consensus RDM from models that agree, and train a JEPA-style predictor with a relational-distillation objective (reproduce the consensus RDM's structure, not any single teacher's raw embeddings).
- **Phase 2**: Compare downstream task performance (vessel/no-vessel or whatever probe task is finalized) across: (a) consensus-distilled model, (b) single-teacher-distilled models (one per teacher), (c) from-scratch JEPA baseline with no distillation target.

### Explicitly out of scope for this file/repo
- The full compartmentalized acoustic architecture (spectral/modulation/environmental compartments, Kalman-style consolidation) — tracked separately, not part of this experiment.
- SST/oceanographic downscaling work (eddyflow) — unrelated project, do not conflate.
- KAEL / τ-vector continual-identity work — related conceptually, not part of this repo's deliverables.
- Training a foundation model from scratch — infeasible at available compute; this project only uses existing pretrained checkpoints as teachers/probes.

## Decision rule between Phase 1 and Phase 2

Do not start Phase 2 until Phase 1 produces a clear read. Three possible outcomes and what each implies:
1. **Strong cross-model RDM agreement** (most model pairs show high RSA correlation) → proceed to Phase 2 as scoped.
2. **Partial agreement** (some paradigm clusters agree, others don't — e.g. contrastive models agree with each other but not with masked-modeling models) → still proceed to Phase 2, but build the consensus RDM only from the agreeing cluster, and treat the disagreement itself as a reportable finding.
3. **No meaningful agreement** (RDMs uncorrelated across models) → do not proceed to Phase 2 as scoped. This is a legitimate, publishable negative result on its own (evidence against convergence in the audio domain specifically) — pivot to writing this up rather than forcing a distillation experiment onto a null result.

## Extended roadmap (added 2026-08-09, supersedes the Phase 2 sketch above with a staged plan)

Phase 1 reached decision-rule outcome 2 (partial/cluster agreement) at 2000 clips/category, 7 models (see journal.md, 2026-08-09 entries). Rather than jumping straight to the single "Phase 2" experiment originally sketched above (consensus-distilled JEPA vs. single-teacher-distilled vs. from-scratch baseline), the plan is now staged so the project is publishable at a defensible floor even if later stages don't pan out. Contribution framing: the novelty target is understanding of representation space across audio foundation models, not a new architecture or training method. Floor = a Datasets & Benchmarks-track resource (reusable toolkit + curated multi-paradigm roster). Ceiling = a predictive account of what determines representational agreement and functional adaptability, validated against real downstream and OOD fine-tuning performance.

- **Stage 0 — Foundation (complete).** Registry/adapter repo, RDM/RSA/CKA/TwoNN toolkit, `inspect_geometry.py` collapse-vs-idiosyncrasy diagnostic, 7 models running end-to-end on the 10,000-clip probe set. Two classification corrections already load-bearing: music2vec is data2vec-lineage not JEPA (no separate predictor network); audio_jepa (Tuncay et al.) confirmed genuine JEPA architecture, distinct from Fei et al.'s unreleased original A-JEPA.
- **Stage 1 — Consolidate current findings (cheap, do first, no new extraction needed).** (a) **Done, 2026-08-10** — downstream functional probe via real X-ARES toolkit integration (`xares_eval/`) on the 3 overlapping tasks (FMA-genre, UrbanSound8K, LibriSpeech-ASR), all 7 models. **Headline finding (not a footnote to a null result): coarse domain-level cluster cohesion and fine-grained within-domain linear separability are different geometric axes that don't transfer.** This is more specific and more useful than either "geometry predicts function" or "geometry doesn't" — it tells you which geometric property to look for depending on what you're trying to predict, which is exactly what a Stage 6 diagnostic needs to be built on. Evidence (see journal, both 2026-08-10 entries): audio_jepa's coarse-domain cohesion (silhouette 0.60 on our own 5-category probe set, far ahead of every other model — nearly double CLAP's 0.31) does *not* transfer to X-ARES's fine-grained within-domain classification (only mid/lower-pack on both tasks; Spearman rho between our silhouette ranking and X-ARES MLP score = 0.357, p=0.43, not significant). audio_jepa's rank *does* improve modestly under kNN vs. MLP on both tasks, direction-consistent with the source paper's own cohesion/separability claim — real but not dramatic. CLAP's fine-grained dominance (best MLP on both tasks) despite only 2nd-place coarse silhouette points toward contrastive/discriminative training pressure (Wang & Isola alignment/uniformity-style) as a better explanatory variable than raw ID/cohesion. **Now formally computed, 2026-08-10** (`audio_comp/geometry/alignment_uniformity.py` — see Stage 6 below for the result, which upgrades but doesn't fully resolve this). (b) Breadth-hypothesis check — **Done, 2026-08-10, re-run at 11 then 12 models same day.** Reframe from domain+paradigm to training-distribution-breadth (narrow-speech: hubert/wav2vec2; narrow-music: mert/musicfm; narrow-bioacoustic: bird_mae/birdnet; broad-mixed: audio_jepa/clap/panns_cnn14/ast/audiomae; self-distillation-narrow-music: music2vec) explains the RSA matrix's structure at least as well as domain+paradigm and meaningfully more of it, and the case only got stronger as models were added: at 9 models, gap was comparable (breadth 0.360 vs. domain+paradigm 0.385) but far more statistically robust (permutation p=0.0003 vs. p=0.0095); at 11 models (adding `ast`/`audiomae`, both AudioSet-trained so both join `broad_mixed`), breadth's p-value hit 0.0000 (0/20,000 random reshuffles matched the observed gap) vs. domain+paradigm's p=0.0082, covering 9/11 models in real groups vs. domain+paradigm's 4/11. **At 12 models (adding `birdnet` to `narrow_bioacoustic`), breadth's p-value holds at 0.0000** even though `birdnet` is a genuinely weak fit within its own group — its RSA correlation with `bird_mae` (same narrow bioacoustic domain, opposite training objective) is only 0.21, much weaker than same-domain-same-objective pairs elsewhere (hubert-wav2vec2 0.80, mert-musicfm 0.51) — real evidence that training *objective* shapes geometry even within a fixed narrow domain, not smoothed over just because the headline significance survived. `audiomae` remains a weaker fit within `broad_mixed` too (RSA 0.27-0.52 vs. the other four's mutual 0.55-0.84) — both weak-fit cases are reported as real limitations of a pure data-breadth account, not hidden. **This is now the fourth independent signal pointing at breadth** (original 10k pattern-break, audio_jepa/clap alignment, PANNs' correlations, AST's correlations) — **adopted as the working explanation per the gate**, more entrenched with every model added, not less, even as individual new models complicate it in specific, honestly-reported ways. CKA cross-check on `birdnet` specifically diverges notably from RSA (CKA 0.24-0.66 vs. RSA 0.01-0.32) — a concrete illustration of why this project treats CKA as secondary/never load-bearing (Davari et al. 2023), not resolved in either direction.

**Re-run at 18 models, 2026-08-13, and the picture is now genuinely mixed, not a continued win for breadth** — reported honestly rather than force-fit to the prior trend. Extending both groupings for the 7 new Tier 2 models (`data2vec_audio` and `music2vec` split into sibling `data2vec_speech`/`data2vec_music` buckets under domain+paradigm; `wavlm`/`unispeech_sat`/`sew`/`wav2vec2_conformer` join `masked_modeling_speech` under domain+paradigm and `narrow_speech` under breadth, since all four train on the same 960h LibriSpeech corpus as plain wav2vec2/hubert; `mms`/`whisper` get a new `broad_speech` breadth bucket — domain-narrow (speech only) but extremely distribution-broad within that domain, a real third axis this project's breadth framing didn't have a bucket for before, see `breadth_hypothesis_check.py`'s docstring for the full reasoning): **domain+paradigm's gap is now larger than breadth's** (0.2394 vs. 0.2043) — a reversal from every prior model count, where breadth's gap led or was at least comparable. Both remain highly statistically significant (domain+paradigm p=0.0006, breadth p=0.0001) and breadth still covers more model-pairs in real within-group comparisons (28 vs. 16, since domain+paradigm still has many one-model singleton buckets), so this isn't "domain+paradigm wins outright" either. Most likely explanation: most of the 7 new additions are narrow-corpus speech models that land in essentially the same cluster under *both* framings (`masked_modeling_speech` ≈ `narrow_speech` membership-wise for 5 of the 7), diluting the specific cases where the two framings used to disagree and breadth used to win. Per this project's own standing gate ("if it doesn't explain better, keep domain+paradigm as the working explanation rather than forcing the reframe into the write-up") — **breadth can no longer be reported as the outright winner at this model count**, though it hasn't been refuted either; both should be reported together going forward, with this reversal stated explicitly, not smoothed over. The `broad_speech` bucket (`mms`+`whisper`) is itself a live, not-yet-analyzed question worth a follow-up look: does it behave more like `narrow_speech` (domain match) or `broad_mixed` (breadth match)? (c) Written decision-rule outcome summary (outcome 2, now with breadth reframing + downstream-probe result folded in) — this alone is a publishable floor result. **Not started.**
- **Stage 2 — Curated model roster expansion (engineering-heavy, gap-filling not count-filling; formalized as a checkpoint-provenance + tiered plan 2026-08-10, superseding the original single-paragraph sketch below the table).** Add models that test a currently-missing axis, each verified against its primary paper before entering any comparison table (the music2vec lesson applies without exception). Every registered model now carries a `checkpoint_status` field (`audio_comp/models/base.py`) — `official_open_weights`, `official_public_weights_license_unclear`, `community_conversion`, or `code_only` — validated at registration time (`register_model()`) and enforced at resolution time (`get_model_class()` raises if a model without `official_open_weights` or a verified `official_public_weights_license_unclear` status is requested for actual use). This is a real gate, not just a documentation convention: a model can be registered (so the framework already models it) without being usable in `configs/models.yaml`'s `active_models` list until its status is upgraded. All 8 models currently registered (`clap`, `hubert`, `wav2vec2`, `mert`, `musicfm`, `music2vec`, `audio_jepa`, `beats`) are `official_open_weights` — `beats` specifically checked 2026-08-10: unilm's root LICENSE is MIT and the BEATs subdirectory's README defers to it with no separate/contrary statement for the checkpoint weights, read as MIT-covered by absence of any carve-out rather than an explicit per-checkpoint citation (judgment call, documented in `beats.py`'s docstring — re-verify if this becomes a redistribution question). `beats` staying in `deferred_models` is now confirmed to be **purely an engineering gap** (no native `transformers.from_pretrained` path, needs its own loader same as `musicfm`/`audio_jepa` already got) — **not** a checkpoint-availability blocker; worth implementing, not done in this pass.

**Loader written 2026-08-15** (`audio_comp/models/beats.py`, `scripts/setup_beats.sh`) — vendors 3 source files from `github.com/microsoft/unilm/tree/master/beats` (`BEATs.py`/`backbone.py`/`modules.py`; confirmed by reading `BEATs.py`'s own imports that `Tokenizers.py`/`quantizer.py` are only needed for training the acoustic tokenizer itself, not for using a pretrained encoder — no fairseq/hydra dependency either, unlike `audio_jepa`'s vendored code). Checkpoint is `BEATs_iter3+ (AS2M)`, the *Pre-Trained Model* column entry for that iteration row — verified twice against the README's table structure that this is genuinely self-supervised-only, not one of the separate AudioSet-fine-tuned checkpoints in the same row (the "(AS2M)" name describes the iteration/tokenizer scale, not a fine-tuning label set — a real ambiguity worth double-checking, not assumed). **Still blocked on one manual step**: BEATs' checkpoints are OneDrive personal-share links, which return HTTP 403 to a plain `curl`/`wget` (verified 2026-08-15 — no programmatic download path exists, unlike every other model in this roster). Source is vendored and the adapter is fully written; needs the user to download the checkpoint via browser and place it at `$AUDIO_COMP_EXTERNAL/beats/BEATs_iter3_plus_AS2M.pt` before it can be smoke-tested and moved from `deferred_models` to `active_models`.

  **Tier 1 (near-term — each fills a specific, currently-open representational gap):**

  | Model | Checkpoint status | Gap it fills | Status |
  |---|---|---|---|
  | AudioMAE | official_open_weights | Reconstruction-target paradigm (raw spectrogram, not latent) — nothing in the current roster tests this | **wired up + validated 2026-08-10** (`audio_comp/models/audiomae.py`; needed 4 narrow version-skew shims, see module docstring; weakest of the AudioSet-trained models on BirdCLEF — see journal, consistent with its comparatively weak RSA fit within the breadth reframing's `broad_mixed` cluster) |
  | AST | official_open_weights | Supervised training — tests whether label supervision matters independent of paradigm | **wired up + validated 2026-08-10** (`audio_comp/models/ast.py`; **wins BirdCLEF outright on both MLP and KNN** — refutes the earlier "non-contrastive objectives trade raw separability for trainable-head-exploitability" reading of the PANNs/Bird-MAE result, since AST is also supervised and shows none of that trade-off; see journal for the revised account) |
  | PANNs CNN14 | official_open_weights (verified 2026-08-10 against `github.com/qiuqiangkong/audioset_tagging_cnn` directly: MIT LICENSE.MIT, checkpoint on the authors' own Zenodo record) | Pure CNN architecture — every current model is transformer-based; untested ResNet-vs-ViT inductive-bias axis | **wired up + validated 2026-08-10** (`audio_comp/models/panns_cnn14.py`, active in `configs/models.yaml`; interim 9-model RSA/CKA/TwoNN pass and BirdCLEF re-run both complete — see journal for the architecture-axis prediction result, not supported) |
  | BirdMAE | official_public_weights_license_unclear (verified 2026-08-10: no LICENSE file in the GitHub repo — confirmed via GitHub's license API returning 404, not just undetected — and no license field on the HF card; genuinely unresolved, not a formality) | Domain-specific bioacoustic encoder; **also directly serves the `bird_sounds`/BirdCLEF X-ARES extension track (`xares_eval/birdclef/`) — one integration effort, not two** | **wired up + validated 2026-08-10** (`audio_comp/models/bird_mae.py`, active in `configs/models.yaml`; needed a version-skew shim for a transformers 4.38→5.15 incompatibility in the checkpoint's own vendored code — see module docstring; BirdCLEF re-run shows a real but nuanced result, see journal — beats CLAP on MLP, not on KNN, and PANNs shows the identical pattern despite no domain match, complicating a pure domain-relevance explanation) |
  | Perch 2.0 | official_open_weights (Apache-2.0, Kaggle Models/TF-Hub) | Second independent bioacoustic-domain check; same dual-purpose note as BirdMAE | **checked 2026-08-10, deferred, not integrated.** Verified against the primary paper (Chen et al., arXiv:2508.04665) before assuming: its training objective is a **hybrid** (supervised multi-taxa classification + self-distillation via a prototype-learning classifier + a "source-prediction" criterion), not clean discriminative/supervised — doesn't fill the intended discriminative/bioacoustic 2x2 cell as cleanly as hoped. Also TensorFlow-only (Kaggle SavedModel, no official PyTorch/JAX/ONNX). `BirdNET` (below) filled the same conceptual gap with a cleaner objective match instead; Perch 2.0 left on the table if a second bioacoustic-domain-model check is wanted later — the isolated-venv infrastructure built for BirdNET would work for it unchanged. |
  | **BirdNET** | official_open_weights (code MIT; **model weights CC BY-NC-SA 4.0** — more restrictive than most of the roster, comparable to mert/music2vec's CC-BY-NC-4.0; non-commercial research use, which this is, unrestricted) | Discriminative/supervised **and** domain-matched (bioacoustic) in one model — completes the objective-type × domain-match 2x2 alongside AST/PANNs (discriminative, general) and Bird-MAE/AudioMAE (reconstruction, bioacoustic/general) | **Added + validated 2026-08-10, architectural exception — not in the `audio_comp.models` registry at all.** RSA fit is weak across the board (max 0.32 with musicfm, one negative correlation with audiomae) — including with bird_mae, its own `narrow_bioacoustic` groupmate (0.21, far below same-domain-same-objective pairs like hubert-wav2vec2's 0.80), real evidence that objective shapes geometry even within a fixed domain. CKA reads it as much better-integrated (0.24-0.66) — a concrete RSA/CKA divergence, not resolved either way per this project's standing CKA-is-secondary rule. See journal for the full breadth-hypothesis re-run result (p-value holds at 0.0000 despite birdnet's weak individual fit). No official PyTorch/JAX/ONNX (TensorFlow/TFLite only, confirmed against `github.com/kahst/BirdNET-Analyzer` and Zenodo's official v2.4 release directly), and rather than add TensorFlow to the main venv (real CUDA/cuDNN conflict risk, the same class the flash_attn/torch incident already taught this project to avoid), it runs in a fully separate, isolated CPU-only venv (`$SCRATCH/birdnet-venv`, `scripts/setup_birdnet_env.sh`) via a standalone script (`scripts/birdnet_extract_embeddings.py`, run outside this repo's normal pipeline) that writes a `.npz` in the exact schema `extract_embeddings.py` produces — every downstream pipeline (RSA/CKA/TwoNN, breadth check, alignment/uniformity) picks it up by globbing the embeddings directory with zero changes needed. Not gated by `registry.py`'s `checkpoint_status` enforcement (can't be, no `BaseAudioEncoder` subclass exists or can exist for a cross-process model) — provenance is documented here and in the extraction script's docstring instead. `birdnet_analyzer`'s own model-download URL (tuc.cloud) 404'd 2026-08-10; downloaded the official Zenodo v2.4 TFLite release directly instead. No X-ARES/BirdCLEF integration yet (would need a subprocess bridge from the PyTorch-based `xares_eval` venv) — scoped to the RSA/CKA/breadth/alignment pipeline for now. |
  | EnCodecMAE | official_open_weights | Neural-codec-derived representation — discrete/compressed representation type, distinct from BEATs' self-distilled tokenizer approach | **Wired up + smoke-tested 2026-08-15** (`audio_comp/models/encodecmae.py`, `pip install -e` from `github.com/habla-liaa/encodecmae`, model name `ec-ec-large_st` — verified directly by catching `load_model()`'s own error listing valid names, not guessed from the HF repo's `large-st` usage snippet). MIT license, confirmed no dependency conflicts (`encodec==0.1.1` is Meta's standalone codec package, a different namespace from `transformers.EncodecModel` already used elsewhere in this project). Native 24kHz. Caught and fixed a real short-clip crash during the mandatory standalone-60ms smoke test — `extract_features_from_array()`'s internal chunking only processes windows over 2048 samples, so a 60ms clip at 24kHz (1,440 samples) produced zero chunks and crashed on an empty list; fixed by zero-padding short clips before calling the model, same category of bug as the audio_jepa/panns_cnn14 short-clip lessons this checklist item exists to catch. |

  **Tier 2 (lower priority — doesn't fill a currently-open gap, started ahead of Tier 1's remaining `EncodecMAE` on 2026-08-12 specifically because it doesn't need cluster GPU-queue time the way a real training run does, and the queue is badly congested right now — see journal.md):**

  | Model | Checkpoint status | Note |
  |---|---|---|
  | WavLM | official_open_weights | Another speech masked-modeling variant — roster already has 2 (hubert, wav2vec2). **Wired up + smoke-tested 2026-08-12** (`audio_comp/models/wavlm.py`, `microsoft/wavlm-base-plus`, active in `configs/models.yaml`). License verified directly against `microsoft/UniSpeech`'s root LICENSE (the repo the model's own HF card links to — a different repo than BEATs' `microsoft/unilm`, not assumed to share its MIT license): **CC BY-SA 3.0 Unported**, a license type not otherwise in this roster. Not yet re-run against the full probe set / RSA-CKA sweep. |
  | BYOL-A | official_open_weights, fine-tuned variants inconsistent | BYOL without masking — useful but secondary to AudioMAE/PANNs. Not yet attempted — needs a custom loader (GitHub checkpoint, no native `transformers` support), deprioritized 2026-08-12 in favor of faster native-`transformers` additions first while pushing roster size up quickly; still open. |
  | Whisper | official_open_weights | Better suited to a separate speech-specialized analysis than the main paradigm comparison, but added anyway 2026-08-12 as a breadth/count push. **Wired up + smoke-tested** (`audio_comp/models/whisper.py`, `openai/whisper-base`, Apache-2.0). Uses `WhisperModel.encoder(...)` only — never `.generate()`/the decoder. Distinct paradigm note: trained with ASR (transcript) supervision, not masked-modeling or contrastive — closer in spirit to AST's supervised data point than to wav2vec2/hubert, just with a different supervision signal. |
  | NatureLM-audio | official_open_weights | Multimodal audio-language model — **needs an explicit written decision on what layer/pooling defines "the representation" before it can enter an RDM comparison at all**, not a plain encoder like the rest of the roster. Not attempted 2026-08-12 — the pooling-definition prerequisite makes it a slower add than the others, deprioritized this pass. |

  **Additional Tier 2 models added 2026-08-12** (beyond the original CLAUDE.md Tier 2 list — added opportunistically while the Digital Alliance cluster GPU queue was badly congested, to push roster size toward ~20 without needing GPU-queue time for onboarding itself; each verified against its own primary HF card/repo, not assumed from a similar-sounding model already in the roster):

  | Model | Checkpoint status | Note |
  |---|---|---|
  | Data2Vec-audio | official_open_weights | `facebook/data2vec-audio-base` — genuine gap-filler, not just a count-filler: gives data2vec-family (self-distillation, EMA teacher) a **speech**-domain data point alongside music2vec's music-domain one, same paradigm/different domain. Checked specifically that this is the base self-supervised checkpoint, not `-960h` (ASR-fine-tuned). Apache-2.0. Wired up + smoke-tested (`audio_comp/models/data2vec_audio.py`). |
  | MMS | official_open_weights | `facebook/mms-300m` — wav2vec2 architecture/objective, trained on ~500k hours across 1400+ languages. The most extreme training-distribution-breadth data point in the roster by a wide margin — directly relevant to the standing breadth-hypothesis finding (Stage 1(b) below). CC-BY-NC-4.0 (same tier as mert/music2vec). Wired up + smoke-tested (`audio_comp/models/mms.py`). |
  | UniSpeech-SAT | official_open_weights | `microsoft/unispeech-sat-base` — wav2vec2-style masked prediction plus a speaker-aware (SAT) pretraining objective. Same repo/license as WavLM (`microsoft/UniSpeech`, CC BY-SA 3.0 Unported) — not re-verified separately since it's the identical repo/file already checked for WavLM. Wired up + smoke-tested (`audio_comp/models/unispeech_sat.py`). |
  | SEW | official_open_weights | `asapp/sew-tiny-100k` — Squeezed and Efficient Wav2vec, an efficiency-focused architecture variant (squeezed context network) distinct from the plain-transformer wav2vec2/hubert family. Apache-2.0. Wired up + smoke-tested (`audio_comp/models/sew.py`). |
  | wav2vec2-Conformer | official_open_weights | `facebook/wav2vec2-conformer-rel-pos-large` — wav2vec2's own objective/training data with a Conformer encoder instead of a plain transformer. A second, more targeted architecture-axis test than the PANNs pure-CNN comparison (Stage 3 interim finding: architecture family wasn't supported as an independent axis there) — this one holds paradigm *and* training data fixed, varying only the encoder block design. Apache-2.0. Wired up + smoke-tested (`audio_comp/models/wav2vec2_conformer.py`). |

  **Checked and explicitly deferred 2026-08-12 (not silently skipped):**
  - **SpeechT5** — the base (non-fine-tuned) pretrained checkpoint is hosted under `ajyy/SpeechT5`, not an official `microsoft/` namespace; `microsoft/speecht5_asr` and similar are ASR-fine-tuned, which this project's base/self-supervised-only policy excludes. Not clean enough to add without further provenance digging — left for a future pass.
  - **EnCodec** (`facebook/encodec_24khz`, MIT-licensed, verified) — the `transformers` `EncodecModel` public API (`encode()`/`decode()`/`forward()`) only exposes discrete post-quantization codes (`audio_codes`), not a documented path to the continuous pre-quantization latent. Using the discrete codes directly would break this project's uniform continuous-hidden-state → mean-pool → Euclidean/cosine-RDM pipeline; getting the continuous latent would require relying on undocumented internal submodules. Deferred rather than forcing a fragile integration — real path exists (internal API use, or embedding-average the codebook lookups) if revisited.

  Roster size after this batch: **19 registered, 18 active** (`beats` stays deferred pending its custom loader) — up from 12 active at the start of 2026-08-12.

  **Gate for every addition, Tier 1 or 2, before any adapter code is written:** weights downloadable without private access; license permits research use; produces deterministic embeddings through a documented adapter; checkpoint_status verified against the model's **primary paper/repo directly, not a secondary source** — this is a hard requirement given the music2vec and A-JEPA/Audio-JEPA mix-ups already hit in this project, not optional diligence. Gate before Stage 3 continues to apply on top of this: every new adapter smoke-tested on short/edge-case clips (per the audio_jepa kaldi.fbank lesson), checked for dependency-version side effects (per the flash_attn/torch lesson). **The shortest edge-case clip must be tested standalone (batch size 1), not only inside a batch alongside a longer clip** — a short clip padded up to a longer clip's length inside a batch never actually exercises the short-input code path, and `extract_embeddings.py`/X-ARES both call `embed()`/`forward()` on individual clips in production. This blind spot caused a full-scale-only crash twice now with the same root shape (audio_jepa's kaldi.fbank window, 2026-08-09; panns_cnn14's pooling stack, 2026-08-10) — treat it as a permanent, non-optional item in the smoke-test checklist, not a lesson to relearn a third time.
- **Stage 3 — Re-run the geometry toolkit at ~11 models.** Full RDM/RSA/CKA/TwoNN sweep on the same fixed probe set; re-test the breadth hypothesis with the new axes; re-run `inspect_geometry.py` per new model individually. **This is the point at which the Datasets & Benchmarks-track floor is fully met.** **Interim 9-model pass done 2026-08-10** (`panns_cnn14` + `bird_mae` only, not the full Tier 1 roster) — not the formal Stage 3 completion (that's ~11 models, 4 more Tier 1 additions still needed), but real signal already: PANNs' RSA agreement with the transformer-based models (mean 0.334) is *higher*, not lower, than the transformers' agreement with each other (mean 0.283) — no evidence architecture family is an independent geometry-shaping axis in this data, a clean negative result for that specific hypothesis. PANNs' two highest individual correlations (`audio_jepa` 0.60, `clap` 0.56) line up with the still-unstarted Stage 1(b) breadth-hypothesis grouping (broad-training-distribution models) better than paradigm or architecture do — second independent point in that reframing's favor.
- **Stage 4 — Functional validation at the expanded roster.** Repeat Stage 1's downstream probe across all ~11 models; test whether the surviving explanatory variable predicts downstream accuracy, not just mutual RSA. **Interim check done 2026-08-10** (BirdCLEF only, not yet the full 3-task X-ARES suite), now complete at all 11 Tier-1-so-far models. Sequence of readings, each superseding the last as more models arrived: PANNs+Bird-MAE first suggested "non-contrastive objectives trade raw separability (KNN) for trainable-head-exploitability (MLP)" vs. CLAP. Adding AST (also supervised, but transformer not CNN, and it **wins BirdCLEF outright on both MLP and KNN** — 0.394/0.239, both best of 11) refuted that reading: supervision-as-a-category doesn't explain the earlier pattern, since AST shows none of it. **Current best account**: discriminative training pressure — contrastive (CLAP) *or* supervised classification with real labels (AST) — produces embeddings that are both raw-separable (high KNN) *and* trainable-head-exploitable (high MLP); these aren't in tension for discriminatively-trained models. Reconstruction training (Bird-MAE, AudioMAE) does show the earlier-observed trade-off (weaker KNN, still reasonable MLP). PANNs (supervised but CNN, weaker than AST on both) suggests architecture may modulate how well supervised pressure converts to separable geometry, without being disqualifying. Separately, the AudioMAE-vs-Bird-MAE pair (same reconstruction objective, only domain differs) is the cleanest isolated domain-relevance test run so far and says domain relevance is real and additive (Bird-MAE more than doubles AudioMAE's MLP and quadruples its KNN on the bird-specific task) — without reinstating domain-relevance as *the* explanation for Bird-MAE's earlier CLAP win, since PANNs (non-domain-matched) already showed that pattern too. A genuine, still-evolving extension of the Stage 1(a) cohesion-vs-separability headline finding, explicitly flagged as not settled — it needs to survive whatever Perch 2.0/EnCodecMAE show next, the same way the original PANNs/Bird-MAE reading didn't survive AST. Full log in journal.md, 2026-08-10 entries.
- **Stage 5 — OOD fine-tuning extension (first half of the novelty ceiling).** Target: real vessel/underwater-acoustic data — the confidential, AIS-labelled dataset (see `[[confidential_vessel_data]]`) is now the sole focus for this axis, per explicit user direction 2026-08-16. Matched lightweight adaptation (LoRA/small adapter, not full fine-tuning) on a fixed-size OOD training slice; measure final adapted performance and the few-shot learning curve; correlate against Stage 3's frozen-geometry metrics (ID, cohesion/separation index, RSA-cluster membership). Note for any write-up: this tests representation-*space* intrinsic dimension predicting adaptability — a different object from Aghajanyan/Gupta/Zettlemoyer (ACL 2021)'s fine-tuning *parameter-update* intrinsic dimension finding; same term, different space, state explicitly. Falsification is a legitimate outcome here too: no correlation just means geometry is descriptive but not predictive of adaptability. **DeepShip/ShipsEar (the earlier public-data preview of this axis) is deprioritized and out of active scope — see Appendix A for why and what was found before it was set aside.**

  **LoRA coverage extended from 5 to 14 of 19 active models, 2026-08-16** (`audio_comp/pipelines/lora_model_configs.py`), for the confidential-vessel-data run specifically (see `[[confidential_vessel_data]]`) but usable by any Stage 5 script. Verified every active model's actual attention-module naming via `named_modules()` before adding it, per this project's standing verification rule — not assumed from architecture family. Groups: `q_proj`/`v_proj` (11 models: the original 5 plus `data2vec_audio`/`mms`/`sew`/`unispeech_sat`/`wavlm`/`whisper`), `linear_q`/`linear_v` (`wav2vec2_conformer` — Conformer's relative-position attention uses different naming than plain wav2vec2 despite identical training data/objective), fused `qkv` (`bird_mae`), and `query`/`value` scoped by regex to `clap`'s `audio_model` submodule only (avoids injecting unused LoRA params into its unused text tower). All 14 smoke-tested end-to-end on GPU (forward+backward pass, plus the mandatory standalone-60ms-clip check) — caught a real bug this way: `bird_mae`'s `last_hidden_state` is already pooled to `(batch, hidden)`, unlike every other model's `(batch, seq, hidden)`, so the shared `mean_pool()` was silently averaging over the *hidden* axis; fixed by skipping pooling for that one model. **`panns_cnn14` (no attention layer) and `audio_jepa`/`audiomae`/`musicfm`/`encodecmae` (bespoke non-HF-extractor preprocessing, not yet reimplemented for a differentiable LoRA path) stay explicitly excluded** — real, bounded follow-up work, not silently dropped from "all models." A second condition, **top-K-layer unfreezing** (`audio_comp/pipelines/topk_unfreeze_configs.py`), covers 4 of the remaining 5 (`panns_cnn14`/`audio_jepa`/`audiomae`/`musicfm`; `encodecmae` stays out even here — its own inference API is non-differentiable as written). **Kept as an explicitly separate, never-blended condition from LoRA** — mixing full-block unfreezing with rank-8 LoRA adapters would confound adaptation-method strength with representation quality, the exact class of issue this section's "matched protocol" language exists to prevent. `data/AIS/run_all_vessel_experiments.py` runs all three conditions (frozen/lora/topk) in one `RemoteStream` session — 19+14+4 = 37 total runs.

  **MIMII LoRA extension run, 2026-08-16, v1 then corrected** (`stage5_lora_finetune_mimii.py`, leave-one-machine-*type*-out, 4 folds, ~200 balanced clips/class/machine). Built to test whether Stage 5 v1's DeepShip overfitting diagnosis (63 clips, too little data) held up with ~30x more training data per fold. **v1 (5 models) found every model at/near chance** (mean accuracy 0.458-0.499) — but while extending to all 14 LoRA-compatible models, found a real bug: `run_fold()` was passing the *model's expected* sample rate into `lora_prepare_inputs()` as if it were the waveform's true native rate, making `resample()` a silent no-op whenever they differed (harmless for 16kHz-expecting models, a real corruption for `mert`'s 24kHz). **The identical bug, unfixed, exists in the original DeepShip `stage5_lora_finetune.py`** — confirmed DeepShip's clips are natively 32kHz against every one of its 5 models' 16/24kHz expected rate, meaning DeepShip's LoRA fine-tuning trained on ~1.3-2x-speed corrupted audio the whole time, confounding the "overfitting" explanation for Stage 5 v1's headline result. Per explicit user direction, DeepShip is being deprioritized (small, public) in favor of the confidential vessel data — `stage5_lora_finetune.py` stays unfixed and unrun; **the v1 "matched LoRA underperforms frozen probing" finding should now be read with this audio-corruption confound stated alongside the overfitting one, not as clean**. Confirmed via direct code inspection that none of the confidential-vessel-data scripts share this bug (all correctly use `ClipCache`'s per-clip true native rate). Fixed MIMII's version and re-ran all 14 LoRA models plus a new frozen-probing counterpart (`stage5_frozen_finetune_mimii.py`) across all 19 active models under the identical split, to disambiguate "hard task" from "LoRA-specific failure." **Corrected result: both conditions cluster near chance** (frozen 0.44-0.57, LoRA 0.44-0.50) — unseen-machine-type generalization is hard regardless of adaptation, a cleaner negative result than v1's ambiguous one. **11 of 14 models still show LoRA underperforming frozen even so** (mean delta -0.027) — independently replicating DeepShip v1's direction on a dataset 30x larger with the sample-rate confound removed, suggesting the "LoRA underperforms frozen" pattern isn't purely a DeepShip-specific artifact, though the effect size here is small given both conditions sit near chance.

  **Fine-tuning methodology (formalized 2026-08-10): two separate conditions, never blended.**
  1. **Official fine-tuned condition.** Use released fine-tuned checkpoints where available. Log, per model: fine-tuning dataset, which layers were updated, number of output classes, the checkpoint-selection rule (e.g. best-val-epoch vs. final), and whether the encoder itself was tuned or only a task head on top of a frozen encoder. Report this condition **separately** — it answers "how good is the model's own best publicly-released adaptation," a different question from the matched condition below, and mixing the two into one comparison table would silently conflate training-recipe differences with representation-geometry differences.
  2. **Matched fine-tuning condition — this is the only one used for the main scientific comparison** (correlating frozen-representation geometry against OOD adaptability, per the paragraph above). Fine-tune every eligible encoder under one identical protocol: same downstream dataset, same train/val/test split, same number of epochs, same optimizer/scheduler, same trainable-layer policy (e.g. LoRA rank and target modules, or which layers are unfrozen), same seeds. Only comparable across models because every axis except the frozen starting representation is held fixed — the entire point of Stage 5's falsifiable claim depends on this.
- **Stage 6 — Practical predictive diagnostic (the actual novelty ceiling).** Only if Stage 5 finds a real correlation: build a lightweight tool that, given a new unseen foundation model, computes RDM/ID/cohesion on a small unlabeled probe set (no fine-tuning, no labels) and predicts likely OOD adaptability + nearest existing cluster, via the relationship learned across the Stage 3 roster. This is what would justify a venue beyond the Datasets & Benchmarks floor. **Candidate diagnostic metric, motivated by Stage 1's actual result (2026-08-10):** raw TwoNN intrinsic dimension / coarse silhouette did not predict X-ARES fine-grained classification performance, but CLAP's specific pattern (best fine-grained classifier, only 2nd-place coarse cohesion) is consistent with contrastive/discriminative training pressure being the more relevant variable.
  **Alignment/uniformity formally computed, 2026-08-10** (`audio_comp/pipelines/alignment_uniformity_check.py`, 9-model roster, no new extraction needed — replaces the silhouette proxy). Real methodological caveat first: alignment (adapted here to use same-*category* clips as the positive-pair proxy, since the probe set has no augmented-view structure like the original paper's setup) and uniformity turned out **perfectly rank-correlated with each other** (Spearman -1.0) in this data — they're one axis here ("embedding-space dispersion"), not the two complementary ones the original theory intends; report accordingly. Result: **uniformity predicts MLP very strongly in the original 7-model roster** (spearman -0.964, p<0.001 — a real, quantitative upgrade over the silhouette proxy's non-significant 0.357/p=0.43) — but the sharper mechanistic hypothesis from the `panns_cnn14`/`bird_mae` BirdCLEF comparison ("uniformity should predict KNN specifically better than MLP") is **not cleanly supported**: true in the 9-model BirdCLEF scope (uniformity-KNN -0.633 vs. uniformity-MLP -0.400, neither significant at n=9) but backwards in the 7-model FMA+UrbanSound8K scope (uniformity-MLP -0.964 vs. uniformity-KNN -0.750). Genuinely mixed, both scopes underpowered (n=7, n=9) to resolve — not explained away, logged as the honest current state. Worth revisiting once the roster is larger (more Tier 1 additions) rather than re-analyzing the same 9 models further.

  **Re-run at n=17, 2026-08-15, and the earlier mixed result is now resolved, not just re-confirmed.** Once X-ARES coverage gaps were filled for all 18 roster models (FMA-genre/UrbanSound8K/LibriSpeech/BirdCLEF), the same script picked up 17 models automatically (no code changes needed — it already filtered to models with complete task coverage). Both scopes now agree on the sharper mechanistic hypothesis: **uniformity predicts KNN more strongly than MLP in both** — BirdCLEF (uniformity-KNN -0.679, p=0.003 vs. uniformity-MLP -0.583, p=0.014) and FMA+UrbanSound8K (uniformity-KNN -0.679, p=0.003 vs. uniformity-MLP -0.471, p=0.057, just short of significance). The FMA+UrbanSound8K scope's earlier *backwards* result (uniformity-MLP stronger than uniformity-KNN at n=7) is gone at n=17 — this reads as the n=7 result having been an underpowered fluke, not a real disagreement between the two scopes; both task-scopes now point the same direction. This is the strongest, most consistent signal for the KNN-specific uniformity hypothesis this project has produced — genuine forward progress on a diagnostic candidate for Stage 6, not just a bigger-N repeat of the same ambiguity.

**Cross-stage risks to restate at every stage, not just once:** CKA is gameable (Davari et al. 2023) — never load-bearing alone. Compute/training-scale confounds apply to every new model in Stage 2, same as the audio_jepa undertraining confound. Probe-set domain coverage is general-audio only until Stage 5's OOD data arrives — don't claim domain generality beyond what's tested. Aggregator/secondary-source labels are not verification — every paradigm/architecture claim gets checked against its primary paper (the music2vec correction is the standing example of why).

## Candidate teacher models (starting list)

Pull individual pretrained checkpoints (mostly HuggingFace) — no single unified benchmarking codebase exists for this list, it will need to be assembled by hand. Aim for paradigm diversity, not just model count:

| Model | Paradigm | Notes |
|---|---|---|
| CLAP | Contrastive (audio-text) | |
| MERT | Masked modeling (music) | |
| HuBERT / wav2vec 2.0 | Masked modeling (speech) | |
| BEATs | Masked modeling | |
| A-JEPA | JEPA-family (online distillation) | Already JEPA-paradigm — useful within-paradigm comparison point |
| music2vec | data2vec-family (self-distillation, EMA-updated teacher) | **Not JEPA-family** — see correction below. Originally mislabeled here as JEPA-family. |
| MusicFM | Masked modeling (BEST-RQ) | |

Do not treat this table as final — confirm checkpoint availability and license before committing to any model.

**Correction (2026-08-09):** music2vec was originally labeled "JEPA-family" in this table and that framing carried through several turns of Phase 1 analysis, including treating its cross-model isolation as informative about H1. That doesn't hold up. music2vec (data2vec-style) has a student encoder that operates directly on masked input and predicts the EMA teacher's averaged top-K layer representations — no separate predictor network. JEPA as specified (and as A-JEPA/Audio-JEPA's own methods sections describe) has three distinct components: a context encoder, an EMA-updated target encoder, and a **separate predictor network** P_φ conditioned on the context representation, trained specifically to predict target-region representations. Both lineages descend from BYOL and are self-distillation-with-EMA-teacher, but "has a decoupled predictor network" is the actual dividing line, and music2vec is on the data2vec side of it. Practical consequence: as of this correction, there are **zero working JEPA-family models** in the active comparison (music2vec is data2vec-family; the original A-JEPA has no public checkpoint; the substitute `ltuncay/Audio-JEPA` is registered but not yet wired up — see `audio_comp/models/audio_jepa.py`). Once `audio_jepa` is wired up it will be the *first* JEPA-family data point, not a second one to compare against music2vec — H1 as originally phrased (mutual agreement *among* JEPA-family models) is untestable until a second working JEPA-family checkpoint exists.

**Confound to flag before `audio_jepa` goes into any comparison:** the `ltuncay/Audio-JEPA` substitute is trained on meaningfully less compute than the other active teachers — 100k steps (~14h on 4 V100s, 5,338h of AudioSet) vs. wav2vec2/data2vec's 400k steps on larger batches. Its own paper reports it substantially underperforming both baselines on several linear-probe tasks (e.g. Speech Commands V1: 0.152 vs. data2vec's 0.927). If `audio_jepa` comes back RSA-isolated from the other five once wired up, that's confounded between two explanations that matter for very different reasons — "JEPA-paradigm geometry is genuinely different" (the thing this project wants to test) vs. "this particular checkpoint is comparatively undertrained" (unrelated to the paradigm question) — and RSA alone can't distinguish them, the same way it couldn't for music2vec's isolation (see `audio_comp/pipelines/inspect_geometry.py` and the 2026-08-09 journal entry for how that was disambiguated).

**A specific, testable prediction for when `audio_jepa` is wired up:** the Audio-JEPA paper states its objective favors embedding cohesion over linear separability — strong kNN performance alongside weak linear-probe performance on the same tasks. That predicts a checkable geometric signature: run `inspect_geometry.py` on `audio_jepa` once live and check for a *low* TwoNN intrinsic dimension and/or unusually tight within-category clustering relative to the other five. Note this is the **opposite direction** from music2vec, which had the *highest* intrinsic dimension of the six active models. If `audio_jepa` shows low ID/high cohesion, that's a real, paper-grounded basis for why JEPA's geometry might differ from the masked-modeling clusters — much stronger evidence than an unexplained isolated RSA number.

## Probe set requirements

- Must include enough clips to estimate RDMs reliably (published cross-model RSA/CKA work typically uses thousands of samples for global comparisons — smaller sets are noisier).
- Should span the domain the downstream task will eventually test (i.e., include underwater/vessel-relevant sounds if available, not only speech/music), since general-audio FMs are not guaranteed to organize non-speech/music sound sensibly — this is itself part of what Phase 1 is testing, not an assumption to bake in.
- Fixed across all models for the whole experiment — do not vary the probe set per model.

## Metrics

- **RSA** (Spearman correlation between RDMs) — primary metric.
- **CKA** — secondary/cross-check only. Do not report CKA alone; pair with RSA or an ID-based diagnostic.
- **Intrinsic dimension (TwoNN)** per model, as an independent structural check — useful for interpreting *why* two models might disagree (e.g. one collapsed to low ID).

## Expected results — stated as falsifiable predictions, not desired outcomes

- H1: JEPA-family models (A-JEPA, music2vec) show higher mutual RDM agreement with each other than with non-JEPA-family models, at matched probe set. **[Correction 2026-08-09: music2vec is data2vec-family, not JEPA-family — see correction note above. H1 as phrased needs a second working JEPA-family checkpoint before it's testable; currently only the `audio_jepa` substitute qualifies, and it isn't wired up yet, so this is a single data point, not a within-paradigm comparison.]**
- H2: Consensus-distilled model (Phase 2) shows downstream performance at least as good as the best single-teacher-distilled model, if H1's convergence signal is real and functionally meaningful.
- Falsification of H2 (consensus underperforms every single teacher) is itself a valid, reportable outcome — do not treat it as an experiment failure requiring rework; it's evidence that idiosyncratic per-model structure carried functional signal that averaging destroyed.

## Deliverables

1. RDM matrices + RSA/CKA comparison heatmaps across all teacher models (Phase 1).
2. Written summary of which decision-rule outcome (1/2/3 above) was reached, before any Phase 2 code is written.
3. (If Phase 2 proceeds) Trained consensus-distilled model, single-teacher-distilled models, and from-scratch baseline, plus downstream task comparison table.
4. Short report suitable for a workshop submission (Interspeech/ICASSP/bioacoustics venue) or as a section of a larger thesis chapter — framing depends on which decision-rule outcome was reached.

## Known risks / limitations to keep stated in any write-up

- CKA is gameable (Davari et al., 2023) — never the sole evidence for a claim.
- Most candidate teacher models are speech/music-trained, not underwater-acoustic-trained — any convergence found may not transfer to the eventual application domain, and this should be tested explicitly, not assumed.
- RDM/RSA comparisons are sensitive to probe-set composition and size — report probe-set construction details explicitly in any write-up, and treat small-probe-set results as preliminary.
- **`ship_vessel` probe-set category is noisier than its label suggests (checked 2026-08-13, prompted by a user question rather than caught proactively — should have been verified at Phase 1 kickoff)**: sourced entirely from `peng7554/DS3500` (see `audio_comp/data/sources/ds3500.py`), which is real ShipsEar recordings (Spain's Atlantic coast, 2012-2013, only 1,948 base samples) expanded via synthetic ray-theory deep-sea channel simulation — its "distance to vessel" metadata (1-11km) is a **simulated** parameter, not measured. About **9.6% of DS3500 is "Class E, environmental noise"** — no vessel present at all — meaning a real slice of the 2000 clips labeled `ship_vessel` in `probe_set_manifest.csv` contain no vessel sound, adding label noise to that category's RDM/RSA numbers specifically. Contrast with the confidential Stage 5 v2 data (`[[confidential_vessel_data]]`), which has genuinely measured AIS-derived distance and transponder-verified class labels from the actual target deployment environment — the two are not redundant, and DS3500's limitations are exactly why the confidential data is load-bearing for the OOD-adaptability question, not just a nice-to-have.
- A negative Phase 1 result is a valid stopping point, not a failure — do not force Phase 2 onto a null result to "have something to show."

## Background reading (for context, not required to start Phase 1)

- Kriegeskorte et al. 2008 — RSA / representational dissimilarity matrices, the core method for Phase 1.
- Kornblith et al., ICML 2019 — CKA.
- Davari et al., ICLR 2023 — CKA gameability limitation.
- Park et al., CVPR 2019 — Relational Knowledge Distillation, closest precedent for Phase 2's distillation objective.
- Huh, Cheung, Wang & Isola, ICML 2024 — Platonic Representation Hypothesis, the broader theoretical claim this project tests a concrete instance of.
- Sengupta, "Representation Without Reward: A JEPA Audit for LLM Fine-Tuning," arXiv 2605.15394 (May 2026) — closest precedent for testing whether representation-level objectives produce decoder/task-visible change, methodology template for Phase 2's evaluation design.

## Working conventions

- State assumptions explicitly in code comments and commit messages — this project has several open design choices (probe set composition, consensus-RDM construction method, downstream task selection) that are not yet finalized.
- Do not silently expand scope into the excluded-topics list above without discussing first.
- Prefer reproducible, seeded runs — cross-model comparisons are only meaningful if noise from randomness is distinguishable from genuine disagreement.
- **Verify any factual claim about a model — paradigm, architecture, checkpoint provenance, license — against its primary paper or repo directly before it enters code, a comparison table, or a write-up, even when the claim comes from an instruction, a prior note in this file, or your own earlier output in the same session.** A label someone (including a past version of you) already wrote down is not verification; it's a claim that hasn't been re-checked. This is a standing rule, not a one-off reaction to a single incident — it's what caught music2vec's paradigm mislabel (2026-08-09), the A-JEPA/Audio-JEPA substitution needing to stay explicitly flagged, and `beats`' license note being stale relative to the actual unilm LICENSE file (2026-08-10, caught specifically by re-checking rather than applying an instructed label at face value). Cost of checking is a few minutes; cost of an unverified claim propagating into a comparison table is a silent, hard-to-trace correction later.

## journal.md — mandatory, ongoing

Maintain `journal.md` at the project root as a running daily log. This is
not optional and not just for milestones — **log continuously, including
failures and small steps**, not only completed features.

Every time you (Claude) do any of the following, append an entry:
- Try an approach that fails, including *why* it failed if known
- Make a small implementation change (not just big features)
- Discover something about the data (buoy quirks, artifacts, surprises)
- Make a design decision, including ones later reversed
- Finish a meaningful chunk of work

Entry format — append, never rewrite history:

```
## YYYY-MM-DD

- [what was tried / done] — [outcome] — [why, if a failure or a decision]
```

Keep entries terse and factual — this is a lab notebook, not a report.
The goal is that six months from now, either of us can reconstruct *why*
a decision was made or *what* was already tried and ruled out, without
re-deriving it. When in doubt about whether something is worth logging,
log it — the cost of an extra line is near zero; the cost of losing a
dead end that gets re-tried later is not.

## Appendix A — DeepShip/ShipsEar, deprioritized 2026-08-16

**Status: out of active scope.** Per explicit user direction ("throw out
deepship/shipsear out of the window right now... the only focus should
be our real and confidential data"), DeepShip/ShipsEar-derived work is
parked here rather than deleted, in case it's worth revisiting later
(e.g. if the confidential data's own volume/coverage turns out to be a
bottleneck, or a reviewer wants a public-data replication point).

**Why it was set aside**: DeepShip's public GitHub subset is small (63
clips total) and only partially released (full dataset requires emailing
the author); ShipsEar/DS3500 (the probe-set's `ship_vessel` category
source) is a fixed, single-region academic benchmark with real
composition issues (~9.6% no-vessel "environmental noise" clips, see the
`ship_vessel` probe-set entry in Known Risks above — that entry is left
in place since the probe set itself is still active). Neither compares
to the confidential AIS-labelled data's genuinely measured distance,
transponder-verified labels, and real target-deployment-environment
audio (see `[[confidential_vessel_data]]`). Once the confidential data
became the practical focus, continuing to invest in DeepShip/ShipsEar
stopped being worth the effort relative to the real target domain.

**What was found before it was set aside (Stage 5 v1, 2026-08-11)**:
matched LoRA fine-tuning (`stage5_lora_finetune.py`, 5 models —
wav2vec2/hubert/mert/music2vec/ast) underperformed frozen-embedding
linear probing in every comparable case (deltas vs. frozen DeepShip MLP:
mert -0.118, wav2vec2 -0.109, music2vec -0.013, hubert -0.019;
spearman(frozen, adapted) = 0.000, n=4). Diagnosed at the time as
overfitting on an extremely small fine-tuning set (63 clips, ~40/fold
for training, one class with only 3 vessels total) — corroborated by
`wav2vec2`'s own 1-epoch smoke test (0.511) beating its 10-epoch result
on the same fold (0.367).

**Known confound, discovered 2026-08-16, never fixed**: while extending
the *MIMII* LoRA pipeline to more models, found that `stage5_lora_finetune.py`
passes the model's *expected* sample rate into `prepare_inputs()` as if
it were the waveform's true native rate, making `resample()` a silent
no-op whenever they differ. DeepShip's clips are natively 32kHz while
every one of its 5 models expects 16 or 24kHz — meaning this run's LoRA
fine-tuning trained on ~1.3-2x-speed corrupted audio the entire time.
This confounds the overfitting explanation above; the true cause is
likely some mix of both, not cleanly separable without re-running (not
done, per the deprioritization above). **If DeepShip/ShipsEar work is
ever picked back up, fix this bug first** (see MIMII's corrected
`stage5_lora_finetune_mimii.py`/`MIMII_NATIVE_SAMPLE_RATE` for the
pattern) — the v1 result above should not be treated as clean until then.
